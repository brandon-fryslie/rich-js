{
  "schema": 2,
  "versions": [
    {
      "version": "master",
      "ref": "refs/heads/master",
      "sha": "631f89e1d7fd320aae675ef065f3f69a0364441b",
      "timestamp": "2026-05-23T21:05:00.854Z",
      "commits": [
        {
          "sha": "631f89e1d7fd320aae675ef065f3f69a0364441b",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-23T15:04:34-06:00",
          "message": "test(coverage): API→demo coverage verifier (rich-demos-l2x.1) (#32)\n\n* test(coverage): API→demo coverage verifier (rich-demos-l2x.1)\n\nTurn 'every public export is demonstrated in examples/' into a machine\ncheck, keystone of epic rich-demos-l2x.\n\nThe verifier compiles src/ + examples/ as one TypeScript program and\nuses the type checker to resolve symbols across the boundary. Public\nexports are derived from getExportsOfModule() on the four entry\nmodules in package.json's exports field — never a hand-maintained\nlist. References in examples/ are resolved through SymbolFlags.Alias\nchains back to the same declaration sites, so deep imports cover the\nbarrel export for the same underlying symbol.\n\nThe allowlist (test/coverage/coverage-allowlist.ts) is the explicit\nescape hatch: every entry is either *burndown* (tagged with the\nflagship demo that will own coverage) or *permanent* (justified). The\ntest enforces three invariants over (universe, references, allowlist):\n\n  1. Every uncovered export is allowlisted with a justification.\n  2. Every allowlist entry points to a real public export.\n  3. No allowlist entry is already covered by a demo.\n\nInvariants 2 and 3 prevent the allowlist itself from becoming a silent\nfallback — dead entries fail loudly, redundant entries fail loudly.\n\nSeeded the burndown list (~100 entries) from today's uncovered set,\neach tagged with the flagship that will absorb it (themes-and-color-\nstudio, renderables-gallery, widgets-playground, markup-and-text-lab,\nlive-progress-console). Flagship-build tickets remove their entries\nas coverage lands.\n\nBehavior verified by injecting each of the three failure cases and\nobserving the precise error message.\n\n[LAW:verifiable-goals] 'All public functionality is demoed' is now a\nbuild that fails when a new export has no demo. [LAW:one-source-of-\ntruth] The public-export set comes from the entry modules; no hand-\nmaintained list. [LAW:no-silent-fallbacks] Three orthogonal invariants\nprevent allowlist drift.\n\nRefs: rich-demos-l2x.1\n\n* ci(deploy): docs via gh-pages-multiplexer (master + PR previews)\n\nReplace the single-deployment actions/deploy-pages workflow with the\ngh-pages-multiplexer action. This deploys to versioned subdirectories\non the gh-pages branch, generates an index page, injects a nav widget,\nand posts sticky PR preview comments.\n\nTriggers: push to master AND every pull_request — same job, same\nsteps; the only variance is the version slot value (`master` for\nmaster pushes, `pr-<N>` for PRs).\n\nPath A integration per the action's install guide: VitePress already\nreads process.env.VITEPRESS_BASE in docs/.vitepress/config.ts, so the\nbuild emits absolute URLs rooted at the deploy slot at build time and\nthe action runs with base-path-mode: none (no post-hoc rewriting).\n\nThe slot value (`steps.version.outputs.slot`) is the single source\nof truth read by both the VITEPRESS_BASE env var and the action's\n`version:` input — drift between them would 404 every deep asset.\n\nVerified locally with VITEPRESS_BASE=/rich-js/pr-32/: every href and\nsrc in the built dist resolves under that path with no <base> tag.\n\nAfter this lands, the repo's Pages source must be flipped from\n'workflow' to 'branch / gh-pages' for the multiplexer's pushes to be\nserved. The branch will be created by the first successful run.\n\n[LAW:single-enforcer] One workflow owns deployment; ci.yml continues\nto own validation. [LAW:one-source-of-truth] slot value is computed\nonce, consumed twice. [LAW:dataflow-not-control-flow] the workflow\nsteps run unconditionally; the slot value carries the variance.\n\n* fix(coverage,deploy): address PR-32 review findings\n\nSix findings from Copilot, all valid:\n\n1. extract.ts: ENTRY_MODULES is now derived from package.json#exports\n   at test-load time, not a hand-list. The dist→src mapping inverts the\n   tsc rootDir/outDir convention; each derived source path is asserted\n   to exist on disk so a misconfigured exports entry fails the verifier\n   load loudly. [LAW:one-source-of-truth] now honored by the\n   implementation as well as the comment.\n\n2. extract.ts: types: ['node'] (not []) so Node-builtin references in\n   src/ and examples/ resolve through the proper @types/node ambient\n   declarations rather than degrading to any. Not coupled to\n   tsconfig.demo.json — coupling would make the verifier brittle to\n   unrelated build-config changes.\n\n3. extract.ts: isUnderSrc() uses path.relative + isAbsolute/startsWith('..')\n   instead of raw-string startsWith. Robust to OS path separator and\n   symlink/realpath differences.\n\n4. coverage.test.ts: fix redundant.push indentation to match the\n   enclosing if-block body.\n\n5. coverage.test.ts: formatGap renders origin.file relative to\n   REPO_ROOT — portable error output, not tied to checkout path.\n\n6. deploy.yml: skip deploy job on fork PRs. GitHub forces GITHUB_TOKEN\n   to read-only for fork pull_request events (correctly), so the\n   gh-pages push and sticky-comment write would otherwise fail noisily.\n   The if-condition lives at the exact trust boundary\n   (head.repo.full_name == github.repository). Fork PRs still run\n   ci.yml; preview-for-fork-PRs requires the workflow_run two-stage\n   pattern, deferred.\n\nAll checks: vitest green (8867 pass / 7 skip), tsc clean, yaml valid.\n\n* fix(coverage): canonical lib name, single examples walk, accurate allowlist comment\n\nThree more findings from Copilot's second pass:\n\nA. extract.ts: lib option uses canonical name 'ES2022' (matching\n   tsconfig.json) instead of 'lib.es2022.d.ts'. The d.ts-filename form\n   bypasses TS's lib-name resolution path; the name form is what the\n   compiler processes through parseCompilerOptions.\n\nB. extract.ts + coverage.test.ts: examples/ is now walked exactly once.\n   makeProgram() returns the exampleFiles list it actually constructed\n   the program from, and the test consumes that list directly for\n   collectReferencedOrigins. [LAW:one-source-of-truth] — two walks of\n   the same tree are two derivations of the same logical set, and\n   could in principle diverge. One walk, one set, two consumers.\n\nC. coverage-allowlist.ts: comment no longer lies about ordering. The\n   entries are grouped by flagship and alphabetical within each group;\n   the comment now says that. [LAW:comments-explain-why-only] — the\n   correct version also explains why (contiguous block deletion when\n   a flagship lands its demos).\n\n* fix(coverage): single-source tsconfig + diagnostic precondition\n\nTwo findings from round 3:\n\nA. Verifier never checked TS diagnostics. If getSymbolAtLocation\n   returned undefined because of an upstream TS error (broken import,\n   missing type), collectReferencedOrigins would silently skip the\n   binding and record it as uncovered — a false-uncovered failure mode\n   masquerading as a missing demo. [LAW:no-silent-fallbacks].\n\n   Added assertProgramClean(program) that runs ts.getPreEmitDiagnostics,\n   filters out node_modules, and throws the formatted diagnostic list.\n   Behavior verified: a planted 'export const _PROBE: number = \"x\"'\n   in src/index.ts triggers the precondition failure with a precise\n   file:line. Removing it returns the suite to clean.\n\nB. Hand-listed compiler options drifted from tsconfig.json in subtle\n   ways. Programmatic lib: ['ES2022'] does not pull in the same\n   ambient declarations as tsconfig's [\"ES2022\"] — Object.hasOwn was\n   unrecognized, and the diagnostic pass flagged real source code as\n   broken. [LAW:one-source-of-truth].\n\n   loadCompilerOptions() now reads tsconfig.json via ts.readConfigFile\n   + ts.parseJsonConfigFileContent, so the verifier's type-system view\n   IS the build's view. Surgical local overrides: noEmit (we never\n   emit), drop rootDir/outDir (our rootNames span src/ + examples/\n   while tsconfig restricts to src/), turn off noUnused* (irrelevant\n   to symbol resolution and would flag legitimate example patterns),\n   merge 'node' into types (both halves touch Node builtins).\n\nPushed back on round-3 finding C (SHA-pin third-party action) and\nresolved with reasoning: the action is first-party (same owner), the\nvendor's own README recommends @v1, and the action doesn't ship\nimmutable semver tags. The threat model SHA-pinning addresses doesn't\napply.\n\n* fix(coverage): canonicalNameFor stability + cross-platform node_modules filter\n\nA. canonicalNameFor's old strategy (alphabetically smallest exposedAs\n   across all entries) could shift if a new earlier-named re-export\n   was added — the doc-comment claimed stability the implementation\n   could not honor.\n\n   New rule encodes the stability claim into the type:\n   walk ENTRY_MODULES in package.json#exports order; return the\n   exposure from the highest-priority entry the origin appears in\n   (alphabetic tie-break only within one entry, for the rare same-\n   module rename case). Since ENTRY_MODULES[0] is the primary public\n   surface (./ -> src/index.ts), and most multi-exposed symbols are\n   surfaced there first, the canonical key cannot shift by adding a\n   new exposure to a lower-priority entry. A rename within the\n   primary entry DOES shift the key — correct, because that is a\n   meaningful change to the public API.\n\n   Verified: ThemeName, createRichTextEngine, richTextFuncs (and other\n   multi-exposed symbols) all resolve their canonical name to the\n   src/index.ts exposure name.\n\nB. assertProgramClean's node_modules path filter used .includes('/node_modules/')\n   — likely fine because TS normalizes paths to / internally, but\n   brittle on a custom host that hands back native Windows separators.\n   Now splits on /[\\\\/]/ and checks for 'node_modules' as a path\n   segment. Robust across platforms and reads more clearly as 'is\n   this path under a node_modules directory.'"
        },
        {
          "sha": "7162eec80728085ba63a052a7d75923d421c5580",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-22T14:03:31-06:00",
          "message": "fix(columns): render all rows of multi-line children via shared merge (#31)\n\nColumns silently truncated every Renderable child to lines[0], so Panels\nand multi-row Tables lost all but their first visual row — contradicting\nthe spec, which promises any renderable is valid.\n\nLift the line-by-line side-by-side merge out of Layout._renderRow into\nSegment.mergeHorizontal, the single horizontal composer. Columns now\nbuilds one cell per grid slot (empty slots are blank cells) and merges\neach grid row through it; Layout delegates to the same primitive, losing\nits duplicated merge loop and its if/else short-cell branch.\n\nCloses rich-columns-9wd"
        },
        {
          "sha": "3b32db9bc5f8fa699b5559f65bb1c41e1edc6802",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-22T13:35:12-06:00",
          "message": "feat: branded CellCol/CodeUnit types; cell-aware TextInput wrap and cursor (#30)\n\n* feat: branded CellCol/CodeUnit types; cell-aware TextInput wrap and cursor\n\nTwo distinct integer spaces — code-unit index (JS string length/slice) and\nterminal cell-column offset (hardware cursor position) — were both typed as\nplain `number`, allowing silent mixing that breaks on CJK/wide characters.\n\nIntroduce CellCol and CodeUnit branded types in cells.ts so the compiler\nrejects any mix-up at the call site. Add cellFit() (largest prefix ≤ N cells,\nno padding) and cellColToCodeUnitOffset() (single conversion point between\nthe two spaces). Wrap setCellSize/splitText/chopCells to require CellCol.\n\nFix all four TextInput bug sites:\n- charGreedyWrap: replace Math.min(cap, line.length-pos) with cellFit()\n- _renderSingleLine: viewport arithmetic in cell space via splitText\n- _emitRowContent: indicator reservation and cursor split via splitText\n- _cursorVisualCol / _clampColForRow: return CellCol / accept CellCol → CodeUnit\n\nUpdate downstream callers (segment.ts, panel.ts, dropdown.ts) with asCellCol()\ncasts at each call site. Tests extended with wide-char wrap and navigation cases.\n\n* fix: propagate CellCol brand to splitCells, handleMouse, and test call sites\n\nsplitCells parameter typed as CellCol — callers in adjustLineLength,\ndivide, and screen.ts updated with asCellCol() casts. handleMouse converts\nclick x-offset to code-unit via cellColToCodeUnitOffset, fixing wide-char\nclick positioning. Stale \"No reservation\" comment updated to reflect that\n_emitRowContent now reserves indicator cells rather than overwriting.\nTest charGreedyWrap call sites wrapped with asCellCol so tests participate\nin the branded-type safety the PR introduces.\n\n* fix: password-mode click hit-testing and cellColToCodeUnitOffset docstring\n\nhandleMouse must map the click's cell offset against the same display\nstring _renderSingleLine uses — password bullets (1 cell = 1 code unit)\nand NEWLINE_GLYPH substitution (also 1:1) — so wide chars in value do\nnot shift the resulting cursor position. cellColToCodeUnitOffset docstring\ncorrected: returns the largest code-unit offset whose prefix cell width\n≤ cellCol, not \"first reaches\" (which implied advancing into wide chars).\n\n* fix: surrogate-pair-safe move, delete, and cursor rendering\n\nmoveCharLeft/moveCharRight detect low/high surrogates at the cursor\nboundary and step 2 code units when the adjacent pair forms an emoji,\nso the cursor never lands inside a surrogate pair. Same logic applied\nto deleteCharBack and deleteCharForward. _emitRowContent cursor rendering\nswitches from slice(n, n+1) to String.fromCodePoint(codePointAt(n)) to\nextract the full code point without splitting a surrogate pair.\n\n* Revert \"fix: surrogate-pair-safe move, delete, and cursor rendering\"\n\nThis reverts commit 5e6a760bfc8df1ae273e98ff4b4e6d5248eaefb2.\n\n* feat(cells): add CodePoint branded type as third integer space\n\nCursor positions must never fall inside a surrogate pair. Previously\nthis was either unguarded or required inline codePointAt/charCodeAt\nchecks at every mutation site — a [LAW:single-enforcer] violation\nscattered across the body.\n\nCodePoint = CodeUnit & { _codePoint } is the compile-time expression\nof that invariant. cursorPosition, WrapRow.start, VisualRow.valueStart,\n_lineStart()/_lineEnd()/_clampColForRow() all return CodePoint.\nnextCodePoint/prevCodePoint are the single authority for stepping across\nsurrogate boundaries; no caller may advance ±1 code unit directly.\n\n[LAW:types-are-the-program] the illegal state (cursor mid-surrogate) is\nnow unrepresentable by construction, not guarded at runtime.\n\n* fix(text-input): correct viewport cursor math for wide-char snap; clamp continuation rowPrintWidth\n\nViewport: splitText snaps backward when startCell falls mid-wide-char,\nkeeping the whole char in afterStart. cursorDisplayCellCol was subtracted\nagainst the requested startCell rather than the actual start of afterStart.\nFix: actualStartCell = rawCellWidth - cellLen(afterStart), which equals\nstartCell in the normal case and self-corrects on snap.\n\nNarrow terminal: rowPrintWidth for continuation rows was rawWidth -\nmarkerWidth with no floor; when layout violates the widget's minimum width\nthe value goes negative, propagating into contentCellWidth arithmetic in\n_emitRowContent. Clamped to Math.max(0, ...).\n\n* fix(text-input): clamp contentCellWidth >= 0; fix WrapRow.start type in test\n\n_emitRowContent: contentCellWidth = rowPrintWidth - indicatorWidth was\nunguarded; when rowPrintWidth is clamped to 0 (ultra-narrow layout) and an\nindicator exists, this goes negative and causes invalid cell-width arithmetic.\nClamped to Math.max(0, ...).\n\ntest: WrapRow.start is CodePoint, not ReturnType<typeof asCodeUnit>; asCodeUnit\nis no longer imported after the CodePoint refactor.\n\n* fix(text-input/screen): cell-space fallback for Up/Down; brand spliceCells params CellCol\n\nmoveLineUp/Down fallback (pre-render): the \"ASCII-only\" comment hid a real\nwide-char bug — col was computed as cursorPosition - lineStart (code-unit\narithmetic), not cellLen of the prefix. Target position was placed with\nMath.min(col, lineLen) (also code-unit). Both are now cell-space:\ncol = cellLen(value.slice(lineStart, cursorPosition));\nposition = prevLineStart + cellColToCodeUnitOffset(line, clamped col).\n\nscreen: lineCellLength return type → CellCol; spliceCells start/length params\n→ CellCol; call site wraps x with asCellCol. [LAW:types-are-the-program] —\nthe cell-column/code-unit distinction is now enforced at the function boundary.\n\n* fix(text-input): snap handleMouse past low surrogate; code-point-aware transposeChars\n\nhandleMouse (password mode): \"•\".repeat(value.length) creates 1 bullet per\ncode unit, so the resulting code-unit offset can land on a low surrogate\n(inside a pair) if value contains emoji. Added low-surrogate check at the\nmapped offset — advances 1 CU to the next code-point boundary if needed.\n\ntransposeChars: was swapping single code units (value[p-1], value[p]) which\nsplits surrogate pairs. Rewrote using prevCodePoint/nextCodePoint to extract\nfull code-point substrings for both char-before and char-at-cursor, then\nreassembles. End-of-value path similarly fixed. New cursor = cpAfter, matching\nold `p + 1` for ASCII and correct for supplementary characters.\n\n* fix(text-input): code-point-aware word/delete scanning\n\nmoveWordLeft, moveWordRight, deleteWordBack, deleteWordForward all walked\nby raw UTF-16 code-unit increments (p--/p++) and classified characters via\nvalue[p-1]/value[p] — both wrong for surrogate pairs (emoji, supplementary\nchars occupy 2 code units).\n\nRewrite all four to loop with prevCodePoint/nextCodePoint and classify via\nvalue.slice(prev, p)/value.slice(p, next) so each step moves exactly one\nUnicode scalar value and the char fed to isWordChar/isWhitespace is always\na complete code point, never a lone surrogate.\n\nDeclare loop variable p: CodePoint (not number) — CodePoint extends CodeUnit\nso it's directly assignable to the helpers' CodeUnit parameter without a cast,\nand the type expresses the invariant that p never lands mid-surrogate pair.\n\n[LAW:types-are-the-program] the loop variable type now enforces the\ncode-point-boundary invariant by construction.\n\n* fix: eliminate O(N²) allocation in charGreedyWrap; fix clamp comment\n\ncharGreedyWrap called cellFit(line.slice(pos), cap) on every iteration,\nmaterializing the entire remaining tail as a new string each row — O(N²)\ntotal allocation for a line of N characters.\n\nAdd cellFitFrom(text, startCU, cap): CodePoint to cells.ts — iterates from\nstartCU via codePointAt without slicing the tail, returning the end\ncode-unit offset as CodePoint. Caller slices only the small [pos, end)\nchunk for the row content. Loop variable typed CodePoint throughout.\n\ncharGreedyWrap rewired to cellFitFrom; force-take path uses nextCodePoint\nfor the same O(1) boundary advance.\n\nAlso corrected the _clampColForRow comment from \"one cell before the end\"\nto \"last valid code-point boundary strictly inside the row (which may be\nmore than one cell before the visual end when the last code point is wide)\"\n— the implementation was always correct, the comment was misleading.\n\n[LAW:types-are-the-program] cellFitFrom returns CodePoint (for...of / codePointAt\nalways stops on a code-point boundary); cellFit semantics unchanged.\n\n* fix(text-input): multiline mouse hit-testing uses row/column coordinates\n\nhandleMouse was using single-line coordinate math (subtract 1 for '[' bracket,\nignore event.y, map against the full value string) even in multiline mode.\n\nIn multiline mode there are no brackets and each visual row is an independent\ncontent slice. Fix: branch on _multiline.\n\n  - event.y selects the visual row (scrollStart + relY, clamped to rows)\n  - continuation rows skip the leading marker cells (markerWidth offset)\n  - cellColToCodeUnitOffset maps against row.content only\n  - row.valueStart is added for the absolute value position\n\nSingle-line path is unchanged. Low-surrogate snap after position resolution\napplies to both paths for surrogate-pair safety.\n\n[LAW:types-are-the-program] the two coordinate systems (bracketed single-line\nvs. marker-prefixed multiline) are now expressed as separate code paths; no\nshared path tries to know both shapes.\n\n* fix(text-input): clicks on minRows-padded empty rows snap to value end\n\nWhen minRows padding renders blank rows beyond _visualRows, clicking those\nrows clamped to the last real row and projected an x-offset into it —\nplacing the cursor at an arbitrary position in the last line of content\ninstead of at end-of-value.\n\nFix: detect rawRowIdx >= rows.length before indexing and set cursorPosition\nto value.length, which is the semantically correct result for clicking\nbelow the last content line.\n\n* fix: prevCodePoint validates full surrogate pair; fix demo tsconfig errors\n\nprevCodePoint checked only for a low surrogate at cu-1 before stepping back\n2 — an unpaired low surrogate at position 0 (cu=1) would produce cu-2 = -1,\na negative and invalid CodePoint. Fix: also confirm cu >= 2 and that the\ncode unit at cu-2 is a high surrogate (0xD800-0xDBFF). Unpaired surrogates\nare now treated as 1-CU characters.\n\nAlso fix tsconfig.demo.json compilation errors introduced by this PR's\nCellCol/CodePoint API changes:\n  - coverage.ts: wrap splitText/chopCells/setCellSize literals with asCellCol\n  - rich-config/index.ts: wrap cursorPosition=0 with asCodePoint\n  - rich-template-bindings/index.ts: add asCodePoint to import; brand all\n    WrapRow.start values in templateAtomWrap; use WrapRow type for rows array\n\n[LAW:types-are-the-program] example code participates in the brand contract —\npassing plain numbers where CellCol/CodePoint are required is a type error\nthat defeats the protection the brand provides.\n\n* fix: surrogate-pair snap, viewport scroll offset, minRows padding, cell-space wrapping\n\nFour correctness fixes surfaced by Copilot review iteration 9:\n\n1. Surrogate-pair snap in handleMouse (both multiline and single-line paths):\n   Previous guard checked only the low-surrogate half (0xDC00–0xDFFF), which\n   would incorrectly advance past a lone low surrogate in a malformed string.\n   Now validates both halves — low surrogate at `pos` AND high surrogate at\n   `pos-1` — matching the invariant already encoded in prevCodePoint.\n\n2. Single-line mouse click offset when viewport is scrolled:\n   _renderSingleLine computes `actualStartCell` but handleMouse was not\n   applying it, so clicks on a scrolled single-line input mapped to the\n   wrong code-unit. Added _singleLineViewportStart field (CellCol, captured\n   during render, read during hit-test) to keep the two functions in sync.\n   [LAW:one-source-of-truth]\n\n3. minRows padding rows emitted nothing:\n   _emitRowContent fast-path returns early for empty content with no cursor,\n   so the VisualRow approach produced just a newline with no preceding content\n   — invisible to Segment.splitLines. Replaced with a direct setCellSize(\"\")\n   segment that fills the row with whitespace, matching how the renderer\n   expects each row to have content.\n\n4. templateAtomWrap (example) compared CellCol budget to UTF-16 code-unit\n   counts and used raw slice indices sized by cell-column values. Replaced\n   all length-based comparisons with cellLen() and all slice-size arithmetic\n   with cellFit(), ensuring the custom WrapStrategy honours its CellCol\n   contract for wide characters."
        },
        {
          "sha": "284bcee8fba7e7b10de945fa39fc846e9da505be",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-21T17:47:34-06:00",
          "message": "refactor(demo): repurpose rich-config filter input as palette search (rich-widgets-dps.1) (#29)\n\n* refactor(demo): repurpose rich-config filter input as palette search (rich-widgets-dps.1)\n\nThe Dropdown gained a built-in type-to-filter, making the sibling \"Filter\nthemes\" TextInput a redundant second way to text-filter the same dropdown.\nRepurpose it as a live palette-color search (renamed inName → inSearch):\nits value narrows a new swatch-chip row to the selected theme's matching\npalette variables (27 vars), a dataset nothing else filters.\n\nThe dark-only Toggle remains the only external narrowing of the dropdown's\noption set — an orthogonal category axis, not a text filter — so the two\nfiltering layers compose instead of duplicating.\n\n* fix(demo): enforce palette-row width cap on the overflow marker\n\nThe +N overflow marker was appended unconditionally. If the last chip\nexactly consumed the remaining space, the marker could push the rendered\nline past PALETTE_ROW_WIDTH. Guard it with the same width invariant the\nchip loop already enforces; the header's N/total count preserves the\ninformation when the marker is omitted.\n\n* feat(themes): add cyberpunk dark-mode palette with partial-alpha accents\n\nFull ~160-var theme matching every other theme's shape ([LAW:one-type-per-behavior]).\nAll six accent base colors carry partial alpha (primary 85%, secondary/accent/warning\n80%, success/error 75%) so their entire darken/lighten/muted derivative trees are\nalso translucent. This exercises alpha-aware rendering paths and makes the\nluminance-vs-alpha concern in colorMath.ts concretely testable.\n\nSubstrate: near-black blue-black (#070714), pale lavender foreground (#E2E2FF).\nAccents: electric cyan, hot magenta, chartreuse, neon lime, neon amber, glitch red."
        },
        {
          "sha": "d8bd956ca1d51883ad1c12baf926ae8fd9bb9686",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-21T16:42:45-06:00",
          "message": "docs: theme transposition and contrast toolkit (#28)\n\n* docs: document theme transposition and the contrast toolkit\n\nAdd two guide pages under a new \"Color & Theming\" section:\n- Theme Transposition: OKLCH substrate, the ThemeKey transform, transposePalette,\n  semantic anchors (error/success/warning hold hue), themeKeyForRoot, and the\n  `just oklsh` explorer.\n- Contrast & Accessibility: relativeLuminance, contrastRatio, contrastFor, and\n  ensureContrast (lightness-slide that keeps a color's hue, AA default,\n  translucency flattening, honest black/white fallback).\n\nSurface both from the introduction. Examples are written against the public\nexports.\n\n* docs: import from the published package name @promptctl/rich-js\n\nEvery docs example imported from \"rich-js\", but the package publishes as\n\"@promptctl/rich-js\" — so copy/paste failed for npm consumers. Update all\nimport specifiers and the install command site-wide to the real name, so the\ndocs reference one correct entry point everywhere. The VitePress hero brand\n(\"rich-js\") is intentionally left as-is.\n\n* docs: correct the lightnessScale inversion note\n\nA lightnessScale of -1 alone clamps to black (L' = clamp01(-L)); inversion\nneeds both axes (scale -1 with shift 1 → L' = 1-L, as INVERT_LIGHTNESS does).\nReword the ThemeKey field comment and inversion note to say so.\n\n* docs: tighten two over-broad guarantees\n\n- OKLCH: hue is preserved where the color is chromatic; at zero chroma or the\n  lightness extremes it's moot and pinned to 0 — not \"always preserved\".\n- ensureContrast: it flattens fg first, so an already-passing translucent input\n  comes back opaque, not \"untouched\"; only an opaque fg is returned unchanged.\n\n* docs: ANCHORED_ROOTS prints as Set at runtime, not ReadonlySet"
        },
        {
          "sha": "98a1f5651ef7b41e247d80cd4e8cab9854f676bd",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-21T15:10:39-06:00",
          "message": "feat: theme transposition via OKLCH (#27)\n\n* feat(core): Oklch value type with sRGB↔OKLCH conversion\n\nA perceptually-uniform color space primitive — the substrate for any\ntransform that wants equal numerical deltas to mean equal perceptual\ndeltas (the property HSL famously lacks).\n\nWhy this lives here: the manipulation space is a different *behavior*\nthan the sRGB wire format that terminals consume. Same color, two\nrepresentations; mutual conversion at the seam. [LAW:one-type-per-behavior]\n\nThemeKey models the transform as data — hueShift / chromaScale /\nlightnessScale / lightnessShift. IDENTITY is a value, not a special\ncase; applyKey(IDENTITY) short-circuits to `this` the same way\nColorRgba.compositeOver short-circuits on alpha=1.\n[LAW:dataflow-not-control-flow]\n\nGamut handling lives once, inside Oklch.toRgba via chroma-bisection.\nOut-of-gamut OKLCH values get their chroma reduced rather than\nreturning invalid sRGB or throwing — callers see a valid ColorRgba or\nnothing. [LAW:single-enforcer]\n\nImports: core/color → core/oklch. No cycles. [LAW:one-way-deps]\n\n* feat(themes): transposePalette with semantic-anchor protection\n\nApply a ThemeKey uniformly across every var in a Palette while keeping\nthe semantic anchors (error / success / warning + their tonal\nderivatives) at fixed hue — UI lies about meaning otherwise.\n\nThe anchor protection is dataflow, not control flow. Two ThemeKey\nvalues are constructed once (caller's key, plus an anchor variant with\nhueShift=0); the per-color transform itself runs uniformly for every\nvar. The branch on isAnchored chooses *which key* to apply, not\n*whether* to apply one. [LAW:dataflow-not-control-flow]\n\nANCHORED_ROOTS lives in exactly one place. The buildPalette derivation\nguarantees every variant of a semantic root shares the root as its\nhyphen prefix, so one set covers the whole family.\n[LAW:one-source-of-truth]\n\nIDENTITY transpose is byte-exact (fast-pathed; skips the sRGB↔OKLCH\nround-trip). The dark flag flips when lightnessScale is negative —\nINVERT_LIGHTNESS turns a dark theme into its light \"octave\" without\ncaller branching on direction.\n\nComposition: transpose(transpose(p, k1), k2) lands within a few bytes\nof transpose(p, composed) — covered by tests.\n\n* feat(demo): rich-themes-transposed visual fixture\n\nThree-section walkthrough making OKLCH transposition's mathematical\nproperties visible:\n\n1. Hue circle — one source theme rotated 0/60/120/180/240/300°. Each\n   step is perceptually-equal because OKLCH is uniform on the hue\n   axis. The error/success/warning swatches stay constant-hue across\n   rotations — visible proof the anchor logic works.\n\n2. Chroma sweep — one source theme at five saturation levels. Reveals\n   gamut clamping (chroma >1.3 starts compressing near the sRGB\n   boundary) without breaking.\n\n3. Algorithmic invert vs. hand-authored — solarized-dark transposed\n   by INVERT_LIGHTNESS next to the human-authored solarized-light.\n   Surfaces the genuine gap between mathematical L→1-L (medium-light)\n   and a designer's polish (very-bright paper-white). The point is\n   not \"the algorithm fails\" — it is \"the algorithm is honest and a\n   human adds a brightness premium on top of pure inversion.\"\n\nIdentity sanity check at the end confirms transposePalette(p, IDENTITY)\nis byte-exact across all gruvbox vars — proof the fast-path works.\n\nRun with `npm run themes:transposed`.\n\n* fix(themes,core): honest dark flag + achromatic-hue pinning in applyKey\n\nTwo corrections from PR review, both instances of the same principle:\nthe type/predicate should reflect *observable properties*, not\nkey-coefficient heuristics or per-operation conventions.\n\n1. transposePalette derives `dark` from the resulting background's\n   OKLCH lightness, not from the sign of `key.lightnessScale`. The old\n   heuristic lied for `{lightnessScale: 1, lightnessShift: 0.8}` on a\n   dark theme (produces a bright palette but reported dark=true) and\n   for `{lightnessScale: -1, lightnessShift: 0}` (crushes everything\n   toward L=0 but reported a \"flip\" to dark=false). The strongest\n   theorem is \"dark iff background.l < 0.5\" — honest under every\n   transform. [LAW:types-are-the-program]\n\n   Throws if the palette has no `background` var. Trusting source\n   `palette.dark` after an arbitrary L-transform would be a silent\n   fallback producing a flag that lies — loud failure is preferred.\n\n2. Oklch.applyKey now pins newH = 0 when chroma collapses below\n   ACHROMATIC_EPS, mirroring fromRgba's existing convention. Without\n   this, applyKey({chromaScale: 0, hueShift: 60}) returned an\n   achromatic color with a non-zero hue, so the round-trip\n   fromRgba(result.toRgba()) drifted on the hue field. The achromatic-\n   hue threshold (1e-7) is now a module constant used in both places.\n   [LAW:one-source-of-truth]\n\nTests cover both: a brightness-shift on a dark theme reports\ndark=false, a crushing flip reports dark=true, applyKey on collapsed\nchroma + non-zero hueShift pins h=0 and survives round-trip.\n\n* fix(console): preserve print() line-end in record buffer\n\nThe trailing newline of `print(text, end)` was written directly via\n`_write(end)`, bypassing `_writeSegments` and therefore the record\nbuffer. Consequence: `exportText` and `exportHtml` produced output\nwhere consecutive prints ran together on a single line, defeating\nthe export's purpose as a visual fixture.\n\nThe line-end now flows through the same `_writeSegments` funnel as\nevery other emitted text. One path for all output; recording captures\neverything that was emitted by construction. [LAW:single-enforcer]\n\nTests added for `exportText` and `exportHtml` newline preservation.\nNo existing tests asserted the missing-newline behavior, so this is\na strict correctness fix.\n\n* feat(demo): rich-themes-transposed exports HTML for visual verification\n\nSetting EXPORT_HTML=path runs the same render pipeline but records\nevery print into a CSS-styled HTML file at the end. Lets the demo be\nopened in a browser to visually verify color relationships — the\nterminal output is the primary view but isn't reachable in agent tool\noutput (ANSI escape codes don't render through the transcript).\n\nThe render code runs identically regardless of export — the\nvariability is in the Console options (record flag) and the final\nsaveHtml call at the boundary. [LAW:dataflow-not-control-flow]\n\nWidth bumped to 120 cols so swatch rows fit on one line in both\nterminal and HTML output (rows were wrapping the warning cell at the\ndefault 80-col width).\n\n* chore(justfile): add themes-transposed recipes\n\nTwo flat recipes — one for the terminal view, one for HTML export\nwith a configurable output path (default /tmp/transpose-demo.html).\nBoth delegate to the canonical npm scripts; no build commands are\nduplicated in the justfile. [LAW:one-source-of-truth]\n\n* chore(justfile): namespace the transpose demo under \\`oklsh\\` module\n\nRecipe names \\`themes-transposed\\` and \\`themes-transposed-html\\` were\nverbose. Move to a just module so the commands become short and\nnamespaced:\n\n  just oklsh::tui   — terminal view only\n  just oklsh::web   — terminal + HTML export (default /tmp/transpose-demo.html)\n  just oklsh        — default → web (one pass produces both outputs)\n\nThe default aliases \\`web\\` rather than running \\`tui\\` and \\`web\\` in\nsequence. \\`web\\` is a superset of \\`tui\\` (same stdout output plus the\nHTML file), so a single run emits everything — no duplicated terminal\noutput, no extra demo invocation. [LAW:dataflow-not-control-flow]\n\n* feat(themes): interactive OKLCH explorer + root-note keys + contrast floor\n\nAdd an interactive TUI (examples/rich-themes-transposed/explore.ts) that\npages through every theme on the left and transposes the selected one live\non the right. The transposition controls (root hue / chroma / lightness /\ncontrast / tonic) are orthogonal to theme selection: one flat ExplorerState\nrecord means selecting a theme touches only themeIndex, so the controls keep\ntheir values by construction — letting you hold a \"root note\" and flip\nthrough all themes in that key.\n\nReframe transposition around a musical root note: themeKeyForRoot computes\nthe hue shift that lands a chosen tonic var on a target hue. It is a second\nconstructor for the existing ThemeKey, not a new transform — transposePalette\nis untouched.\n\nGuarantee readability with a single authority: contrastRatio/ensureContrast\njoin contrastFor in colorMath (one WCAG luminance function). Every text/bg\npair in both the explorer and the static snapshot routes through\nensureContrast, so an inverted or darkened palette can no longer render\ndark-on-dark.\n\nThe model is pure (reduce/renderFrame/previewCells), so the contract is\nverified without a TTY: all themes listed, selected one marked, orthogonal\ncontrols preserved on theme change, and every preview cell clears the\ncontrast floor across a full state sweep — plus an end-to-end stdin→repaint\ntest through the real EventRouter.\n\n* feat(themes): replace explorer preview with a representative app dashboard\n\nThe abstract swatch row demoed the primitive, not the payoff — transposition\nonly proves itself in a real layout, where decorative chrome rotates while\nsemantic colors hold their meaning in context. Build the preview from the\nlibrary's own renderables (Panel, Table, ProgressBar): a service dashboard\nwith nav, a progress meter, a status table, and action buttons.\n\nDecorative roles (primary border, accent nav/progress, secondary button)\nrender as fills so their hue is visible and rotates with the root note;\nsemantic roles (success/warning/error) drive the service statuses and stay\nanchored, so a \"down\" service is red in every key. All text routes through\nensureContrast against its actual fill.\n\nCompose the body by splitting each child into lines and concatenating the\narrays rather than relying on Group's segment concatenation, which merges\nadjacent single-line renderables onto one row.\n\nContract: the mock renders its chrome and service rows, and the failing\nservice's hue drifts 0° across all six root rotations.\n\n* fix(themes): stamp the app-surface background onto every content cell\n\nSegments carry independent styles with no cascade, so foreground-only text\nfell through to the terminal's ambient background — invisible on dark themes,\nvisibly broken on light ones (text rows rendered on the dark page while the\npanel's blanks/padding were light). Stamp the theme background as a base on\nevery body cell (`surfaceBase.add(seg.style)` lets a pill's own bg win) and\npad each line to width. One enforcer owns \"the surface is this color\"; no\nrenderable has to remember it. Border glyphs stay on the page bg by design.\n\n* feat(themes): add a dense full-color showcase view (125/151 vars)\n\nThe dashboard exercised ~8 roles; a theme defines 151. Add a second preview\nview — a Textual-style workspace that maps each var family to the UI element\nit was designed for: markdown-h1..h6 → a document, *-darken/lighten-* → tonal\nramps, surface/panel/boost/background → an elevation ramp, block-cursor /\nscrollbar / border → a sidebar, footer-* → a command bar, input-* → a search\nfield, text-* → type hierarchy. One render touches 125 distinct vars, so\ntransposing recolors a genuinely color-dense page.\n\nView is data: a `view` field selects between `showcase` (default) and `app`\nvia a VIEWS registry, toggled with `v`. renderFrame indexes the registry\nrather than branching. Both views are pure, so both stay TTY-testable.\n\nSurfaces compose through the single stamp enforcer: each card stamps its own\nbg, then the page stamps `background` to fill margins and gaps. Contract:\nthe showcase touches 100+ vars, renders for every theme without throwing, and\nevery readable cell clears the 4.5:1 floor across all themes (500+ cells).\n\n* feat(themes): relative/absolute root-hue mode with a control toggle\n\nThe root-hue slider held an absolute target across theme changes, so it never\nreflected the newly selected theme. Add a hue-mode toggle:\n\n  relative (new default) — the stored value is an offset from the theme's\n    natural tonic hue. 0° = the theme as authored, and the effective hue\n    tracks whichever theme is selected, so paging themes shows each untouched.\n  absolute — the stored value is the target hue, held across themes, so every\n    theme's tonic lands on the same hue (\"all in one key\").\n\nA single derived `effectiveTargetHue(state)` interprets the mode; the stored\n`rootHue` is still preserved across theme changes (orthogonal controls don't\nreset). Toggling converts the stored value so the effective hue — and the\nrendered colors — stay fixed: the toggle changes representation, not output.\nThe relative-mode slider shows a signed offset and the resolved absolute hue.\n\n* fix(themes): relative root-hue shows one stable number, not a derived value\n\nIn relative mode the control rendered both the offset and the resolved\nabsolute hue (\"+60° → 277°\"). The resolved value is derived from the theme,\nso it changed on every theme switch — making a stable, independent control\nlook like it was mutating. Show only the offset: one number that stays put\nacross themes. Absolute mode already displays the actual target hue, which\nmatches the rendered result.\n\n* feat(themes): make ensureContrast adjust lightness within the hue\n\nEscalating a failing foreground to black/white is a cop-out — the text stops\nbelonging to the theme. Instead, slide the foreground's OKLCH lightness toward\nthe contrast-raising pole while holding hue and chroma, bisecting for the\nlightness nearest the original that clears the floor. A blue on a dark-blue\npanel becomes a readable lighter blue, not white — the smallest perceptual\nchange that achieves accessibility, the way professional tone systems do it.\n\nReuses the same bisection idiom as toRgba's gamut clamp, and lets that clamp\ndesaturate near the poles so the hue survives and the result stays in-gamut.\nBlack/white remains only as the degenerate fallback for mid-toned backgrounds\nwhere no lightness of any hue can physically reach the target — there the pole\nis also the maximum, matching contrastFor.\n\n* refactor(themes): drop absolute root-hue mode; root hue is always an offset\n\nRoot hue is now a single offset from the theme's natural tonic hue: the stored\nvalue stays fixed across theme changes while the effective hue tracks each\ntheme. Removed the hueMode discriminator, the toggle control, toggleHueMode,\nand the mode branches in effectiveTargetHue, reset, and the display.\n\n[LAW:no-mode-explosion] one behavior, no second mode to reason about; the\n\"held across themes\" state the type used to permit is now unrepresentable.\n[LAW:one-type-per-behavior] there was only ever one behavior wanted; the\nabsolute mode was a second type carrying no value. Net subtraction.\n\n* refactor(themes): remove the inert tonic selector from the explorer\n\nTransposition is a uniform hue rotation, so once root hue became a pure offset\nthe tonic cancelled out: keyFor's hueShift equalled the offset regardless of\nwhich var was the tonic, making the selector a no-op. A control that can't\nchange the output advertises a capability that doesn't exist, so remove it\nalong with TONIC_VARS, tonicIndex, naturalRootHue, and effectiveTargetHue.\nkeyFor is now a direct uniform hueShift = offset.\n\n[LAW:no-mode-explosion] / justify-every-knob — drop the control that carried\nno value case. themeKeyForRoot stays in the library as the absolute\n\"land a var on hue X\" primitive; the explorer just no longer needs it.\n\n* feat(api): export the WCAG contrast toolkit for downstream clients\n\nThe contrast work built for the transpose demo lived in an internal module\n(themes/colorMath), reachable only by importing a non-public path. Promote the\naccessibility primitives to the public surface, where any client styling\nterminal output can use them:\n\n  relativeLuminance(c)         WCAG relative luminance\n  contrastRatio(a, b)          WCAG contrast ratio in [1, 21]\n  contrastFor(bg)              best black/white for a background\n  ensureContrast(fg, bg, min)  make a color readable while keeping its hue,\n                               by sliding only its OKLCH lightness (AA default)\n\nPure, dependency-light blocks with near-zero carrying cost — useful well beyond\nthis repo. The demo and its test now consume them from the public index rather\nthan the internal path. No behavior change to any shipped feature; additive\nsurface only.\n\n* fix(color): harden color primitives flagged in review\n\n- oklch.toRgba is now the real normalization boundary the constructor doc\n  promises: clamp L into [0,1] and C to ≥0 before conversion, so a negative\n  chroma canonicalizes to achromatic instead of flipping hue 180° via the\n  bisection. toLinearRgb/findInGamutChroma take explicit (l,c,h).\n- isIdentityKey treats a whole-turn hueShift (360, -360, …) as the no-op it is,\n  so applyKey short-circuits and transposePalette keeps its byte-exact fast\n  path for mathematically-identity keys.\n- ensureContrast flattens a translucent foreground over the background before\n  measuring, so the \"clears minRatio\" guarantee holds for the displayed color\n  (e.g. text-disabled #FFFFFF60) and returns an opaque result. contrastRatio\n  documents that it assumes opaque inputs.\n- console.print reuses Segment.line()'s cached newline for the default end\n  instead of allocating one per call.\n- transpose docstring grammar.\n\n* fix(themes): address second review pass\n\n- ANCHORED_ROOTS: drop the Object.freeze wrapper — it is a no-op on Set\n  membership and implied a runtime guarantee it never provided. The\n  ReadonlySet type is the real (compile-time) immutability enforcement.\n- transposePalette docstring: clarify the throw-on-missing-background applies\n  to the transposing path; the identity fast-path preserves the source dark\n  flag verbatim and needs no background, so it never throws.\n- transpose tests: compare hues by circular distance (min(|d|, 360-|d|)) so\n  anchor-hue assertions don't break across the 0°/360° boundary.\n- framesToSegments: reuse Segment.line()'s cached newline in the explorer's\n  hot render path instead of allocating one per line break.\n\n* fix(themes): validate themeKeyForRoot input; correct ensureContrast doc\n\n- themeKeyForRoot throws a clear RangeError on a non-finite targetHueDeg at the\n  boundary, instead of letting NaN propagate to Oklch's constructor several\n  calls downstream where the error is far less actionable. [LAW:errors]\n- ensureContrast docstring: chroma is held only where it stays in gamut — near\n  the poles gamut clamping may reduce it (hue is always preserved). Match the\n  prose to the actual guarantee.\n\n* fix(color): ensureContrast falls back to true max-contrast black/white\n\nThe \"physically impossible\" branch returned the gamut-clamped OKLCH pole\n(L=0/1 at the original hue), which is only *near* black/white — not the true\nmaximum-contrast color against the background. Return contrastFor(bg) instead:\npure black/white is the honest maximum and is at least as strong as the\nclamped pole. Tightened the test to assert exact equality with contrastFor,\nand corrected its comment (a mid-grey of 128 tops out near 5.3:1, not 4.58)."
        },
        {
          "sha": "f0bb9cc48949b0bd49a88aeb0f354a23cc6901e5",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-19T08:13:04-06:00",
          "message": "feat(themes): theme registry + authored Textual palette data (#26)\n\n* feat(themes): theme registry + authored Textual palette data\n\nAdds a lookup-by-name API (`getThemePalette(\"dracula\")`) backed by 18\nauthored Textual theme palettes shipped as per-theme TS modules under\n`src/themes/data/`. Each module ships Textual's full ~150-variable\npalette (`accent`, `accent-darken-2`, `block-cursor-background`,\n`footer-key-foreground`, `boost`, etc.) verbatim from upstream.\n\n[LAW:one-source-of-truth] Theme hex values live in exactly one place:\nthe data file. The existing `TerminalTheme` constants (`MONOKAI`,\n`NORD`, `DRACULA`, …) are rewritten to pull their 8 base colors out of\nthe registry's `Palette`, so editing `data/<name>.ts` updates both the\nregistry's full palette and the matching `TerminalTheme` constant.\n\n[LAW:no-defensive-null-guards] `getThemePalette` exposes two overloads:\nthe typed-`ThemeName` form returns non-null `Palette`; the untyped\n`string` form (e.g. user input from a CLI flag) returns `Palette | null`\nand forces the caller to handle the miss.\n\n[LAW:single-enforcer] Hex-string → ColorRgba parsing for theme data is\nthe single hex-loading boundary, kept inside `registry.ts` so\n`themes/palette.ts` remains a type-only consumer of `core/color.ts`\nand the `core/color → themes/palette` one-way edge survives.\n\nAlso tightens tsconfig (`noImplicitOverride`, `noImplicitReturns`,\n`noFallthroughCasesInSwitch`, `noPropertyAccessFromIndexSignature`)\nwhich surfaced 9 missing `override` keywords across `highlighter.ts`,\n`text-input.ts`, and `claude-sessions/_common.ts`.\n\n3 themes get new named exports — `TEXTUAL_DARK`, `TEXTUAL_LIGHT`,\n`TEXTUAL_ANSI` — matching the data files added.\n\nDemo: `examples/rich-themes/` (`npm run themes`) walks all 18 themes,\nprints header / swatch / resolver rows for each. Catches data-fidelity\nand rendering regressions by running. Fixed a latent\n`RichText.append(other)` style-drop along the way (constructor-style\nisn't carried as a span; the demo now uses `.append(text, style)`).\n\nRecovered from `origin/origin/feat/theme-registry` (which was built\nbefore PR #12 unified `ColorQuad` → `ColorRgba`). Alpha plumbing now\ngoes through the existing `ColorRgba` rather than the obsolete\n`ColorQuad`.\n\n* docs(themes): refresh ThemePaletteData jsdoc to match current path\n\nThe previous comment described `ColorQuad`-era hydration via\n`Palette.fromHex` — both removed in this PR. Restating outdated\nmechanics in a doc comment is a stale WHAT-statement that drifts the\nmoment the implementation moves. New comment names only the durable\nboundary (`getThemePalette` as the single hydration site) and the\nauthored→runtime distinction; mechanics live in the code.\n\n[LAW:comments-explain-why-only]\n\n* fix(themes): cheap base-color helper, strict hex validation, comment refresh\n\nThree related fixes from review:\n\n1. `terminalThemes.ts` no longer pays for full-palette hydration at\n   module init. New `getThemeBaseColors(name)` in `registry.ts` parses\n   only the 8 substrate keys per theme (~120 hex parses vs ~2,250) and\n   does NOT touch the registry cache. The lazy contract is restored:\n   the full Palette only materializes when the registry is actually\n   queried. The private `ThemeDef` interface is gone — `ThemeBaseColors`\n   is now the one named shape for theme substrate data, used both for\n   data-backed themes and inline-stated ones.\n   [LAW:one-type-per-behavior]\n\n2. `parseHex` now validates the post-`#` body against `/^[0-9a-fA-F]+$/`\n   before delegating to `parseRgbHex`/`parseRgbaHex`. Previously\n   `parseInt(\"0G\", 16) === 0` would silently hydrate a typo like\n   `\"#FF0G00\"` into a valid-looking but wrong ColorRgba. The core hex\n   parsers trust their input shape (TS contract from callers); the\n   registry's `parseHex` is the trust boundary for untrusted authored\n   data and now fails loudly with the contextual theme/key error.\n   [LAW:single-enforcer]\n\n3. The `data/index.ts` barrel comment claimed adding a theme is \"drop a\n   file, add it here, done\" — accurate before `terminalThemes.ts` grew\n   named exports per data file, stale now. New comment describes the\n   actual 2-step (or optional 3-step) flow and notes step 3 isn't\n   required for registry use.\n   [LAW:comments-explain-why-only]\n\n* feat(themes): every theme is data-backed (default + svg-export + frappe + macchiato)\n\nThe 4 inline-only themes — DEFAULT_TERMINAL_THEME, SVG_EXPORT_THEME,\nCATPPUCCIN_FRAPPE, CATPPUCCIN_MACCHIATO — were the source of an\nasymmetry that forced consumers to code defensively: 15 themes had full\n~150-var palettes queryable via `getThemePalette(name)`; these 4 had\nonly their 8 base colors and `getThemePalette` returned null. Picking a\ntheme by name from `listThemePalettes()` could produce \"really shitty\nlooking colors\" depending on which theme the user picked.\n\nThis commit adds data files for all 4. `catppuccin-frappe` and\n`catppuccin-macchiato` are ports of Textual's upstream BUILTIN_THEMES.\n`default` and `svg-export` are rich-js-specific themes synthesized by\nfeeding their existing base colors into Textual's own\n`ColorSystem.generate()` derivation — the same pipeline that produced\nthe 18 already-shipped data files. Round-trip verified: regenerating\n`dracula.ts` from BUILTIN_THEMES[\"dracula\"] produces a byte-exact match\nagainst the existing file, so the new 4 follow the same conventions\n(151 vars, sorted keys, hex case, alpha resolution).\n\n[LAW:one-type-per-behavior] Every theme now has the same shape — name,\n`dark` flag, 151-var Palette in the registry, `TerminalTheme` constant\nin `terminalThemes.ts`. Consumers can pick any theme by name and get\nidentical capabilities. No more \"is this theme registry-backed?\"\nbranching at callsites.\n\n[LAW:one-source-of-truth] All inline base-color literals are gone from\n`terminalThemes.ts`. The data file is the only place a theme's hex\nvalues live. `terminalThemes.ts` is uniformly a derived view —\n`<NAME> = defineTheme(getThemeBaseColors(\"<name>\"))` for every export.\n\n[LAW:dataflow-not-control-flow] Variability between themes lives in\nvalues (the hex strings in data files), not in whether a particular\nAPI works for a particular theme. Every callsite runs the same\noperations against any theme.\n\n`listThemePalettes()` now returns 22 names. The 1-byte drift in some\nsynthetic-theme accents (e.g. default.error #800000 → #7F0000) is\nHSL-roundtrip precision loss inside Textual's derivation; it's\nvisually identical and matches the same drift baked into the 18\nexisting data files. The bg/fg of `DEFAULT_TERMINAL_THEME` and\n`SVG_EXPORT_THEME` are preserved exactly (grayscale → no HSL drift),\nso existing color tests continue to pass.\n\n* fix(themes,demo): correct flatten primitive + stale barrel count\n\nTwo cleanups from review:\n\n1. `examples/rich-themes/index.ts` was using `alphaBlend(bg, bg, bg.alpha)`\n   to \"flatten\" translucent colors against the theme background. That's a\n   no-op interpolation (both endpoints are `bg`), and only happened to\n   produce correct output because every input has alpha=1 today. Replaced\n   with `bg.compositeOver(substrate)`, which short-circuits when alpha=1\n   and properly RGB-blends otherwise. The substrate is now an explicit\n   parameter so the intent is visible at every callsite.\n   [LAW:dataflow-not-control-flow]\n\n2. `data/index.ts` doc comment claimed \"18 bundled\" — now `THEMES`\n   contains 22 (20 Textual-ported + `default` + `svg-export`). Comment\n   refreshed to match.\n   [LAW:comments-explain-why-only]\n\nThe third review thread (HSL-roundtrip drift in MONOKAI.success and\nGRUVBOX.accent — `#A6E22E`→`#A5E22E`, `#FABD2F`→`#F9BD2F`) is the\nintended consequence of [LAW:one-source-of-truth] in the previous\ncommit and is documented in the thread; no code change.\n\n* test(themes): remove cache-purity test that didn't assert behavior\n\nThe \"does not pollute the registry cache\" test claimed to verify that\n`getThemeBaseColors` leaves the registry's private Map untouched, but\nearlier tests in the file populate that cache for every theme, so the\nasserted theme (`rose-pine-dawn`) is already cached by the time this\ntest runs. The test would still pass even if `getThemeBaseColors`\nstarted internally calling `getThemePalette`.\n\nThe cache-purity claim has no externally-observable surface (the cache\nis a private `Map` in `registry.ts`). A test that asserts internal\nstate without a seam is structure-masquerading-as-behavior; the right\nresponse is to remove it, not add observability infrastructure to make\nit honest. The contract `getThemeBaseColors` actually offers — returns\nthe eight base colors, parses cheaply — is covered by the sibling\ntest \"returns the eight base substrate colors for every theme\".\n\n[LAW:behavior-not-structure]"
        },
        {
          "sha": "4886225103bfd68f42a1190689b0e5055c6e42d7",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-18T23:56:00-06:00",
          "message": "feat(widgets): standalone Dropdown demo (examples/dropdown-demo) (#25)\n\nAdds a focused demo exercising the three axes of variability Dropdown\nowns: a short fixed list (baseline collapse/expand), a long fixed list\n(type-to-filter narrowing across 18 items), and a list whose options\ncycle every 3s (overlay re-anchors and width invariant re-measured by\nScreen). A status row reads {selectedIndex, expanded, highlightedIndex}\nlive for each Dropdown via direct observable reads — Screen subscribes\nthrough MobX, no mirror state. Run with 'npm run demo:dropdown'.\n\nThe mutating Dropdown swaps its options array via runInAction; the\nwidget's filteredOptions getter and Screen's measure pass re-derive\neverything from the new data with no demo-side re-anchor call.\n[LAW:one-source-of-truth] [LAW:dataflow-not-control-flow]"
        },
        {
          "sha": "dae9b5bf93bb684d025a7385998add585c3b3564",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-18T23:39:56-06:00",
          "message": "test(template-bindings): pin demo's edit→reactive-output contract (#24)\n\nPR #23 listed \"interactive manual test\" and \"Alt-modifier delivery\" as\nunchecked Test Plan items. Both reduce to deterministic, machine-\nverifiable contracts; the manual-only framing was an unforced\n[LAW:verifiable-goals] violation.\n\nAlt-modifier delivery: already covered by event-router.test.ts:229-252\n(ESC+letter → meta:true, Alt+Backspace, Alt+Space canonical-name,\nAlt+uppercase shift+meta) and text-input.test.ts:523-543 (Alt+B/F word\nmotion, Alt+D delete-word, Alt+Backspace kill-word). Terminal-app\ninterception of Alt+letter for diacritics is environment-specific\nbehavior, not a rich-js contract.\n\nDemo reactive pipeline: this commit adds 6 tests pinning the\nTextInput.value → autorun → renderTemplate(value) → next-frame chain\nthe rich-template-bindings demo relies on:\n\n- first frame renders the initial template\n- stdin keystroke → router → focused widget → value mutation\n- value mutation → next frame's render reflects the new template\n- broken template degrades gracefully (no autorun teardown)\n- one value-change-per-tick → one frame each\n- three value-changes-per-tick → one frame (microtask debounce)\n\n[LAW:dataflow-not-control-flow] The chain has no branches on\n\"did typing happen\" — value flows into render every frame; the test\nasserts the value-flow shape, not call counts of internal methods.\n\n[LAW:single-enforcer] One frame per microtask is pinned by the\ndebounce test so future refactors can't silently regress to\nrender-per-mutation or skip-some-mutations."
        },
        {
          "sha": "b9938e4b1128ecc7f3718acfdcd7e351dca128ab",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-18T12:53:29-06:00",
          "message": "Interactive template-bindings demo + textarea functionality in rich-js (#23)\n\n* Widgets work\n\n* chore(release): v0.2.0\n\n* feat(demo): template-bindings showcase — all 11 sections\n\nAdds examples/rich-template-bindings/index.ts: a comprehensive one-shot\nterminal demo covering every feature of the template-bindings module:\n\n  § 0  Header panel\n  § 1  Text attributes (canonical names, short aliases, negation)\n  § 2  Named foreground colors + full 256-color ramp\n  § 3  Generic color forms (hex, rgb, color index, named)\n  § 4  Background colors via on()\n  § 5  Composition — Table showing template vs rendered side-by-side\n  § 6  OSC 8 hyperlinks\n  § 7  Palette functions (DRACULA theme — semantic names + derived)\n  § 8  Palette modifiers (GRUVBOX — darken/lighten gradients, alpha fade)\n  § 9  Auto-contrast — 10 backgrounds, WCAG-optimal text color\n  § 10 Theme gallery — 13 themes, same template, distinct palette colors\n  § 11 Showcase — realistic CI build report (TOKYO_NIGHT) in a Panel\n\nKey implementation note: applyStyleToFragment stores styling in RichText._style\n(base style), but RichText.append transfers only _text and _spans. exec() encodes\n_style as a span via stylize() so styling survives fragment joining; spans are\nadded after the base-style span so span-level styles win on conflict.\n\nAlso adds `npm run template-bindings` script to package.json.\n\n* feat(demo): rewrite template-bindings demo as interactive TUI\n\nEvery displayed item now has an editable TextInput containing the Go\ntemplate that live-generates it — editing triggers instant reactive\nre-render via MobX observable subscriptions on TextInput.value.\n\nPageUp/PageDown navigates 11 sections; Tab/Shift+Tab cycles inputs\nwithin the active section. Non-current sections are invisible and\ntheir TextInputs are disabled so Tab stays section-scoped.\n\nTheme Gallery (§9) appears before the Build Report showcase (§10).\nThe showcase combines 6 per-line TextInputs into a single Panel output\nthat re-renders whenever any line changes.\n\n* feat(demo): side-by-side layout, Ctrl+N/P navigation\n\nLayout: editable template box on the left, live rendered output on the\nright — both in one StaticItem per row. TextInputs are always invisible\n(visible=false) so the Screen's 'maxWidth passed to all widgets' constraint\nis avoided; the StaticItem reads input.value / cursorPosition / focused\n(all MobX observables) to render the faux box with a reverse-video cursor.\n\nNavigation changes from PageUp/PageDown to Ctrl+N (next) / Ctrl+P (prev)\nwhich don't conflict with TextInput cursor-movement handlers.\n\n* feat(demo): textarea layout for template editors\n\nReplace the single-line input boxes with proper multi-row textareas:\ntemplate wraps character-by-character at the left column boundary, no\nhorizontal scrolling. Cursor tracks 2D position via\ncurLine=floor(pos/lc), curCol=pos%lc. Border turns cyan when focused.\n\nRight column renders the live output, clipped to exact cell width via\nSegment.adjustLineLength so the two columns always align.\n\n* feat(demo): multi-line templates with soft-newline syntax\n\nNewlines in a template act as formatting only — `\\s*\\n\\s*` collapses to\na single space before the engine sees the template. Authors can split\ncomplex expressions across lines for readability:\n\n    {{ link \"https://github.com/anthropics/anthropic-sdk-python\"\n       (underline (cyan \"Anthropic SDK\")) }}\n\nrenders the same as a one-line version, but the textarea shows it as\ntwo visual rows.\n\nTextarea height is now dynamic — sized to each template's wrapped line\ncount, clamped to [1, 10]. Long single lines soft-wrap at the nearest\nspace (with hard-break fallback for tokens longer than the box width).\nCursor mapping uses rowStarts so a 1D position resolves correctly\nacross both logical newlines and visual soft-wraps.\n\nFor a real newline in the rendered output, use a string literal:\n{{ \"\\n\" }}.\n\n* feat(demo): tag-aware wrap with continuation indent\n\nThe textarea now tokenizes each logical line into atoms — each `{{ ... }}`\naction is one atom, plain text between them is another — and packs atoms\ngreedily across rows, never breaking inside a tag. Continuation rows\nget a 2-space indent so the visual hierarchy stays obvious.\n\n  Before (mid-tag char-break):\n    │ ike\" }}  {{ overline \"overline\" }} │\n    │   {{ reverse \"reverse\" }}  {{ blin │\n    │ k \"blink\" }}                       │\n\n  After (tag-boundary wrap):\n    │ {{ bright_white (on \"red\" \" red \") }}                                 │\n    │   {{ bright_white (on \"green\" \" green \") }}                           │\n    │   {{ bright_white (on \"blue\" \" blue \") }}                             │\n\nOver-wide atoms fall back to character break (tags — no clean alternative)\nor word wrap at the last space (plain text). Cursor mapping carries each\nrow's `prefixLen` so 1D positions resolve to the right `(row, col)` even\nwhen continuation indents shift the visible content.\n\n* feat(demo): wrap-continuation arrow marker\n\nSoft-wrapped continuation rows now display a dim-cyan `↳` marker with\n2-space leading indent (4-cell prefix total: \"  ↳ \"), so the visual\nhierarchy of wrapped lines is obvious at a glance:\n\n    │ ← outer not_bold overrides inner   │\n    │   ↳ bold                           │\n\nUser-newline-separated rows stay marker-free — only wrap-induced rows\nget the indicator. VisualRow splits prefix and content into separate\nfields so the prefix renders as its own dim-styled Segment while the\ncontent carries the cursor/highlighting; cursor mapping still resolves\ncorrectly because prefix.length is the offset between visual col and\ntemplate position.\n\n* fix(demo): place wrap marker at column 0\n\nThe arrow now sits at the leftmost cell of the content area with a\nsingle trailing space (2-cell prefix), so content sits at column 2 —\nalready a visible 2-space indent relative to the parent row. Drops the\nextra leading whitespace that pushed the arrow into the middle of the\nindent and visually competed with user-newline rows whose source\npreserves its own indentation.\n\n  Before:  │   ↳ bold                           │\n  After:   │ ↳ bold                             │\n\n* fix(demo): distinguish soft vs hard newlines by tag context\n\nPreviously `exec()` collapsed every `\\n` (with surrounding whitespace) to\na single space before the engine saw the template. That meant explicit\nmulti-line templates like\n\n    {{ b \"b\" }}\n    {{ i \"i\" }}\n\nrendered as one line of output (`b i`) instead of two — surprising,\nsince the source clearly indicates two separate items.\n\nThe fix: collapse newlines ONLY when they appear *inside* a `{{ ... }}`\nblock. Outside the block, newlines pass through verbatim and become\nhard newlines in the rendered output. The result:\n\n  Inside a tag (formatting):       Outside a tag (content break):\n    {{ link \"https://...\"            {{ b \"b\" }}\n       (underline (cyan \"x\")) }}     {{ i \"i\" }}\n      ↓                                ↓\n    one styled span                  two lines: \"b\\ni\"\n\nWrap-induced visual continuations still produce zero newlines in the\noutput, because the wrapper never modifies the underlying value — only\nits display.\n\n* feat(template-bindings): style spec function + pipe-form docs\n\nAdds a `style` template function that takes a Style.parse spec\n(\"bold underline #ff6b6b\") and a child, collapsing the multi-style\nnesting case to a single call. The spec is a string, so it flows\nthrough Go-template $vars and through scope — naming a reusable style\nset requires no additional API:\n\n  {{ $alert := \"bold underline #ff6b6b\" }}\n  {{ style $alert \"alarm!\" }}\n  {{ style $alert .otherField }}\n\nSpec grammar is exactly Style.parse — the inside of [...] markup. No\nsecond parser, no second error surface. The fragment a template builds\nthrough `style` is byte-equivalent to the same spec inside markup.\n\nAlso documents Go template's pipe form (last-arg piping) under\nComposition — previously valid but undocumented and unused in demos.\nThe new \"Multi-Style\" demo section shows both forms side-by-side with\nthe equivalent nested form for comparison.\n\n* feat(demo): revamp template-bindings as 3 pipe-first scenes\n\nReplaces the 12 didactic sections (text attrs, named colors, hex/rgb,\nbackgrounds, composition, links, palette, modifiers, auto-contrast,\ngallery, showcase) with 3 richly composed scenes:\n\n  §1 Push         — multi-line git-push status panel with 5 reusable\n                    $style vars referenced across two synthetic commits;\n                    exercises text attrs, hex, named colors, palette\n                    semantics, `palette \"spec\"`, link, on-via-spec, the\n                    style function — every styled span in pipe form.\n  §2 Theme Matrix — one notice template rendered across all 13 themes;\n                    a single $bg participates in both `auto $bg` and\n                    `on $bg`, so contrast pick + background shift\n                    together when the user edits the hex once.\n  §3 Ramps        — three pipe-driven ramps for the value-axis features:\n                    palette darken/lighten (7-step), paletteOver alpha\n                    (with reusable $bg), and hex/rgb/color N/named.\n\nPipe form (\"x | a | b | c\") is now the only composition syntax in the\ndemo — zero nested function-call composition appears anywhere. The\nreusable `style $var` pattern leads every scene; each $var is\nreferenced ≥2x so the \"edit once, propagate everywhere\" payoff is\nvisible.\n\nFile shrinks from 767 → 645 lines (-122). Section count from 12 → 3.\n\n* feat(widgets): readline-style keymap + multiline mode on TextInput\n\nTextInput becomes a real editable text widget instead of a single-line\nstate container with three keys. The new behavior moves cursor logic\ninto reusable widget primitives so demos (and any other host) get a\nfull readline-style editor without rebuilding it externally.\n\nMotion\n  left/right · Ctrl+B/Ctrl+F           char left/right\n  up/down · Ctrl+P/Ctrl+N              line up/down (preserves column)\n  home/end · Ctrl+A/Ctrl+E             line bounds\n  Ctrl+Home/Ctrl+End                   document bounds\n  Ctrl+Left/Ctrl+Right · Alt+B/Alt+F   word back/forward\n\nEditing\n  backspace/delete · Ctrl+H/Ctrl+D     char back/forward\n  Ctrl+W · Alt+Backspace               kill previous whitespace-word\n  Alt+D                                kill next word forward\n  Ctrl+U · Ctrl+K                      kill to line start / end\n  Ctrl+Y                               yank kill buffer at cursor\n  Ctrl+T                               transpose chars (readline parity)\n\nMulti-line\n  new `multiline: true` option: Enter inserts `\\n`, Up/Down navigate\n  logical lines preserving \"preferred column\" intent; Ctrl+Enter still\n  submits. Single-line (default) keeps Enter=submit so existing usage\n  is unchanged. Newlines in value are rendered as `↵` so the cursor's\n  char-index ↔ display-column mapping is preserved without a multi-row\n  render path.\n\nEach motion/edit primitive is also a public method (moveLineStart,\nkillLineForward, etc.) so hosts can bind custom keys or invoke them\nprogrammatically.\n\nEventRouter gains Alt+letter decoding: `ESC <printable>` and\n`ESC <0x7f>` previously fell through as a dropped escape; now emit\n`{ key, meta: true }` so Alt+B/F/D and Alt+Backspace can reach\nwidgets. CSI-style modifiers (Ctrl+Left/Right etc.) were already\ndecoded — this fills the second branch so all modifier paths route\nthrough one widget-facing contract.\n\nDemo: TextInputs in rich-template-bindings now opt into `multiline`\nso users can edit the new pipe-first / reuse-first templates with\nfull editor controls. Section navigation moved from Ctrl+P/Ctrl+N\n(which now mean cursor up/down inside the textarea) to\nCtrl+PageUp/Ctrl+PageDown — no readline binding uses PageUp/PageDown,\nso the demo's global handler and the widget no longer fight for keys.\n\n* feat(widgets,template-bindings): textarea functionality moves to library\n\nFour library additions that retire ~250 lines of demo-only logic and put\nthe equivalent functionality on reusable rich-js components:\n\n1. Drop `exec()` from the demo. The engine handles in-action whitespace\n   (including newlines) per Go's text/template grammar — the source-\n   rewriting `exec()` did nothing the engine wasn't already doing, and\n   shifted error positions away from the source author's actual input.\n\n2. `RichText.fromFragments(rts, { end? }): RichText` flattens the engine's\n   `RichText[]` output into a single styled `RichText`, lifting each\n   fragment's wrapping style onto a span over its range so downstream\n   rendering preserves the styling. Plain `.append(rt)` only propagates\n   spans; this static fills the gap for the engine-output case.\n\n3. `renderTemplate(engine, source, scope?, options?): Segment[]` —\n   one-call \"compile + flatten + render to segments, degrade gracefully\n   on parse/eval errors\" helper in template-bindings. Replaces the\n   demo's bespoke `renderTmpl` try/catch with a single library import\n   any template-driven UI can adopt.\n\n4. `TextInput` becomes a real multi-line textarea:\n\n   - `wrap?: WrapStrategy` option — a plugin shape that returns visual\n     rows for a logical line at given width budgets. Library exports\n     `charGreedyWrap` (break at any char) as the conventional default;\n     consumers with domain-aware tokenizers (e.g. a template editor\n     wanting to break on `{{ ... }}` atom boundaries) supply their own.\n     No syntax-specific behavior is baked into the widget — variability\n     lives in the data the strategy returns, not in widget branches.\n   - `continuationMarker?: string` (default `\"↳ \"`), `maxRows`,\n     `minRows`. Soft-wrap is rendered with the marker styled dim;\n     `maxRows` scrolls the cursor row into view; `minRows` pads short\n     content with empty rows for stable layouts.\n   - Up/Down now move across *visual* rows (not just logical lines) by\n     reading the row decomposition that `render()` just cached. The\n     previous \"Up sometimes skips multiple wrapped rows\" half-widget\n     problem is gone. Cursor motion before the first render falls back\n     to logical-line motion so test setups that never render still work.\n\nDemo migration: `tokenize`, `wrapText`, `findCursor`, `VisualRow`,\n`WRAP_MARKER`, `MIN_HEIGHT`/`MAX_HEIGHT`, and ~200 lines of\n`buildRowSegments` go away. What remains is a small `templateAtomWrap`\nWrapStrategy (the template-aware tokenizer, now plugged into the\nwidget) and a much smaller `buildRowSegments` that just composes the\nside-by-side row chrome around `input.render()` and\n`renderTemplate(engine, value)`.\n\nTests: 1488 → 1511 (+23 new — fromFragments, renderTemplate,\ncharGreedyWrap, custom wrap strategy, visual-row Up/Down, minRows/\nmaxRows). Demo: 604 → 518 lines (~14% smaller despite gaining the\ntemplateAtomWrap function). Widget: 521 → 791 (the lifted logic).\n\n* refactor(demo): replace hand-drawn dual-pane chrome with Layout + Panel\n\n`buildRowSegments` no longer draws its own box-drawing characters. The\nside-by-side row composition is now:\n\n  Padding([0,0,0,2], Layout.splitRow(Panel(input), Panel(output)))\n\nEach pane gets its border and title from `Panel`; the left/right\nside-by-side merge comes from `Layout._renderRow`, which is purpose-\nbuilt for the multi-line line-by-line interleave Columns doesn't do.\n`Padding` provides the screen-edge indent.\n\nThe output renderable now threads `opts.maxWidth` through to\n`renderTemplate` so the template output soft-wraps to fit the right\nPanel's inner width — previously the demo rendered template output at\nthe hardcoded 400-cell default, which overran the column when Panel\ntried to enclose it.\n\nNotes:\n- Tried `Columns([Panel, Panel], { equal: true })` first as originally\n  pitched in the audit; turns out `Columns` only emits the first\n  visual line of each item (`columns.ts:101`) — it's a single-row chip\n  grid, not a multi-line side-by-side layout. `Layout` is the right\n  primitive for this case.\n- Panel uses its default `ROUNDED` box, so the dual-pane chrome now\n  matches the §1 \"git push\" Panel for visual uniformity.\n\nAfter this, every line in the demo is either a rich-js call, a content\nliteral, single-page-nav state, or boilerplate lifecycle. No hand-drawn\nchrome remains. 518 → 500 lines.\n\n* fix(widgets,demo): vertical motion no longer parks cursor at wrap boundary\n\nWhen `moveLineUp` / `moveLineDown` targeted a visual row whose length was\nless than the cursor's column, the clamp `min(col, target.length)` landed\ncursorPosition at `target.valueStart + target.length` — which equals the\nNEXT row's `valueStart` when the target has a continuation row after it\n(intra-wrap boundary). `_cursorVisualRow` resolves that boundary toward\nthe LATER row, so the cursor visibly stayed on the source row even though\nits position had moved. Worse, the new position was itself the boundary,\nso every subsequent Up/Down recomputed the same clamp and noop'd — cursor\nstuck until something independently shifted it (e.g. a no-net-displacement\nhorizontal jog that left visualCol at 0).\n\nThe trace captured the failure end-to-end:\n\n  Up: pos 825 → 812  col=13 → clamp 2 → 812 (= rows[23].valueStart)  ← trap\n  Up: pos 812 → 812  (repeated, all noop)\n  Right×3 + Left×3 → pos still 812, visualCol=0\n  Up: pos 812 → 810  col=0 → clamp 0 → 810  ← works because col=0\n\nFix: route both vertical motions through `_clampColForRow(col, targetIdx)`,\nwhich clamps to `max(0, target.length - 1)` when the target is followed by\na continuation (intra-wrap), and to `target.length` otherwise (last row of\na wrap, or non-wrapped logical lines — where the next position is the `\\n`\nseparator, not a row-overlap). Cursor now lands strictly inside the target\nrow, the boundary cannot be reached by vertical motion, and the \"state\ncarryover\" symptom disappears.\n\nAlso fix the related cosmetic surface in the demo: `templateAtomWrap` was\nemitting leading-whitespace as its own visual row (looked empty, since\nspaces don't paint visibly against Panel padding). Coalesce a pure-\nwhitespace leading atom with the following atom in tokenize so the wrap\nemits substantive first rows.\n\nTests: 3 new regression tests pinning the wrap-boundary fix\n(`tinyHeadWrap` strategy that deterministically builds the trap shape).\nFull suite: 1511 → 1514.\n\n* fix(widgets): textarea viewport stops scrolling on every cursor move\n\nThe multiline scroll position was a pure function of cursor position:\n`scrollStart = clamp(cursorRow - maxRows + 1, 0, total - maxRows)`.\nThis pinned the cursor to the viewport's bottom edge whenever\n`cursorRow >= maxRows`, so every Up/Down moved the viewport in lockstep\nwith the cursor and the cursor never appeared anywhere except the\nbottom row.\n\nFix: make `_scrollStart` persistent instance state and only adjust it\nwhen the cursor would leave the viewport (the conventional\nviewport-follows-cursor invariant). Clamp on render to handle value\nshrinking past the stale viewport top.\n\n* fix(widgets): multiline TextInput opens at top, not at value end\n\nPre-loaded multiline content used to land the cursor at value.length,\nwhich scrolled the viewport to the bottom on first render — the user\nsaw the end of their template instead of the beginning. Switch the\ndefault to cursor=0 for multiline mode (single-line stays at end,\nmatching the pre-filled-form convention).\n\n* feat(widgets): textarea shows ▲/▼ scroll indicators in rightmost column\n\nWhen `maxRows` is set, the widget now reserves the rightmost column of\nits content area for a scroll-direction arrow. The top visible row gets\n▲, the bottom gets ▼. Each is colored primary when scrolling is\npossible in that direction; dim foreground at the edge. Indicators only\nappear when `total > maxRows` — the slot is empty space otherwise.\n\nThe wrap budget shrinks by 1 whenever `maxRows` is set so content\nnever collides with the indicator column. When the cursor lands on the\nindicator column itself, cursor wins and the arrow is omitted for that\nframe — the cursor stays observable.\n\nAll changes live inside the widget; no demo changes required.\n\n* fix(widgets): scroll arrows are pure overlays, not a reserved column\n\nPreviously the indicator slot was always reserved (wrap budget shrank\nby 1 whenever `maxRows` was set), eating a column even when no\nscrolling was happening — and dim ▲/▼ appeared at the edges as\nplaceholders. Drop both:\n\n- Wrap budget stays at full `maxWidth` regardless of `maxRows`.\n- ▲ renders only when `canScrollUp` (scrollStart > 0).\n- ▼ renders only when `canScrollDown` (scrollStart + maxRows < total).\n- No idle/dim arrows. The arrow either carries information or isn't drawn.\n\nArrows now overlay content in the rightmost column on the rare frame\nwhere both an arrow is needed and content reaches that column. Cursor-\nwins still preempts the arrow on collision.\n\n* refactor(widgets): drop cursor-wins logic for scroll arrows\n\nThe cursor-wins guard added complexity for a rare overlap that only\nmatters when scroll is actually possible in the indicator's direction\nAND the cursor happens to sit on the rightmost column. Paint the\nindicator last; it overlays the cursor cell on collision. One frame of\nhidden cursor on a deliberate edge interaction is an acceptable\ntrade for fewer special cases in the rendering path.\n\n* feat(widgets,panel): [X/Y] scroll indicator via Panel border accessory\n\nTextInput gains `scrollIndicator: \"arrows\" | \"indices\" | \"none\"`\n(default \"arrows\", preserving prior behavior):\n\n  - \"arrows\": ▲/▼ in-widget content area, as before.\n  - \"indices\": suppresses arrows, publishes `[X/Y]` via the new\n    `scrollIndicatorText` getter — X is the cursor's 1-indexed visual\n    row, Y is the total visual rows. Returns undefined when nothing\n    to scroll.\n  - \"none\": no indicator emitted, getter returns undefined.\n\nPanel gains `bottomRightAccessory`:\n\n  - Accepts `string | RichText | () => string | RichText | undefined`.\n  - Function form is evaluated at border-emit time, after content has\n    been rendered — enables widgets to publish post-render state and\n    have it appear in the surrounding chrome.\n  - Coexists with `subtitle`: subtitle centers in the remaining width,\n    accessory hugs the right corner.\n\nThe template-bindings demo switches to `\"indices\"` and wires\n`bottomRightAccessory: () => input.scrollIndicatorText` so the user\ncan compare the two indicator styles.\n\n* feat(examples): rich-theme-designer — interactive theme/category/key explorer\n\nA new demo that composes three pickers — category × theme × palette key —\nto preview which palette color works best for which UI category, under\nwhich theme. The chosen (category, theme, key) is rendered live across\na small set of widgets (Panels with titles, two TextInputs exercising\narrow + indices scroll indicators, a swatch row showing every palette\nkey for the current theme).\n\nLibrary changes needed to make the categories independently stylable:\n\n  Panel:\n    - titleStyle    — title text in the top border (defaults to borderStyle)\n    - subtitleStyle — subtitle text in the bottom border\n\n  TextInput:\n    - indicatorStyle — ▲/▼ arrow color (defaults to palette `primary`)\n    - cursorStyle    — cursor cell style (defaults to on-primary on primary)\n    - contentStyle   — content text style (defaults to palette `foreground`)\n\nRun: npm run theme-designer\n\n* refactor(widgets): KeyEvent.stop() + priority-tier dispatch chain\n\nKeyEvent becomes a class carrying its own \"claimed\" signal (event.stop() /\nevent.stopped) instead of returning a boolean up the call stack. EventRouter\ndispatches through an ordered chain — high → focused widget → normal — and\nshort-circuits on event.stopped. The router holds zero key-specific policy:\nTab/Shift+Tab traversal is owned by FocusManager, which registers itself as\na normal-priority chain participant at construction.\n\nWidgets that consume a key call event.stop(); FocusManager only runs Tab\ntraversal when no participant upstream stopped it. Dropdown uses this to\nsuppress traversal while expanded and clears overlay state on blur via a\nhandleFocus override (single-enforcer for overlay lifecycle).\n\nPublic exports: KeyEvent moves to value export; KeyEventInit, KeyHandlerPriority,\nKeyHandlerOptions added. spec/widgets.md updated to describe the class shape,\nFocusManager.handleKey, and the dispatch chain.\n\nExamples migrated: rich-template-bindings registers Ctrl+C / Ctrl+PageUp at\npriority \"high\"; rich-config/terminal.ts constructs new KeyEvent instances.\n\n[LAW:types-are-the-program] [LAW:dataflow-not-control-flow] [LAW:single-enforcer]\n\n* refactor(examples): migrate rich-config + rich-theme-designer to EventRouter\n\nBoth demos previously hand-rolled the input dispatch they predate:\nenterRawMode/enableMouse/startReading from examples/rich-config/terminal.ts,\nplus a handleInput function that special-cased Ctrl+C, replicated\nFocusManager's Tab traversal, called fm.current.handleKey directly, and\nran an app-level hover loop. EventRouter owns all of that now — raw mode,\nmouse tracking, ANSI parsing, the priority-tier dispatch chain (high →\nfocused widget → normal), and topmost-hit click delivery — so the demos\nkeep only their genuine application policy:\n\n  • Ctrl+C shutdown          — high-priority router.onKey\n  • mouse_up → focus widget  — router.onMouse\n  • Button keyboard flash    — onSubmit schedules an 80ms active-clear\n                               (Button.handleMouse already clears active\n                               on mouse_up; one mechanism covers both\n                               submit paths)\n\nHover tracking disappears entirely from the demos because every\ninteractive widget already implements setHovered as the router's\nfast-path. The defensive \"clear active on all widgets\" loop is gone for\nthe same reason — only Button uses active, and Button manages its own.\n\nexamples/rich-config/terminal.ts is deleted: every export it carried\n(parseKey, parseMouse, enterRawMode, leaveRawMode, enableMouse,\ndisableMouse, startReading) is now a duplicate of functionality\nEventRouter owns, with no remaining importers.\n\nNet: -215 lines (-289/+74).\n\n[LAW:single-enforcer] [LAW:dataflow-not-control-flow]\n\n* docs(spec): align widgets.md with implemented API surface\n\nThe spec drifted from the contract in src/widgets/types.ts: it documented\n`handleClick(MouseEvent)` (the contract is `handleMouse(WidgetMouseEvent)`),\nmarked widget state fields as `readonly` (they are mutable observables —\nthe host and widget itself write through actions), and missed the drag\ncapture mechanism for Slider. Update the spec so a consumer reading it\nsees the same contract they'll find in the code.\n\n* test(widgets): drop unused imports; expose StubWidget emit helpers\n\n`emitChange` / `emitSubmit` are `protected` on WidgetBase — calling them\nfrom test code outside the class won't compile under strict TypeScript.\nAdd public `triggerChange` / `triggerSubmit` wrappers on the local\nStubWidget so the subscription tests assert behavior through the\nsubclass API rather than reaching past the access modifier. Same pass\nremoves Style + WidgetMouseEvent imports that were no longer referenced\nafter earlier test refactors.\n\n* fix(template-bindings): error path never throws on invalid errorStyle\n\nrenderTemplate promises to degrade gracefully on any failure in the\ncompile/evaluate/render chain — the catch branch returns a styled\n`[error: ...]` segment so live-render callers (preview panes, etc.)\nnever see an exception. The catch itself was calling Style.parse() on\nthe caller-supplied `errorStyle` spec, which throws on malformed input,\ndefeating the whole point of the helper.\n\nGuard the parse with a fallback to a hard-coded safe Style (red dim)\nso the helper holds its no-throw contract regardless of what the caller\npasses for errorStyle. The default case (no spec) skips parsing\nentirely.\n\n* fix(widgets,screen): cursor rewind moves up N-1 rows, not N\n\nAfter drawing N lines with N-1 separating newlines the cursor sits at\nend-of-row-N (no trailing newline). Emitting `\\x1b[NA` to rewind moves\nthe cursor up N rows, landing on the row ABOVE the frame — subsequent\nwrites then overwrite content the frame doesn't own.\n\nMove the right number of rows: `lastLineCount - 1`. Skip the CSI\nentirely when there's only one row to overwrite (some terminals treat\n`\\x1b[0A` as one row up, which would put us out of bounds the other\ndirection).\n\n* fix(widgets,slider): reject non-positive / non-integer width at construction\n\nSlider's rendering (`trackChar.repeat(width)`) and mouse math\n(`width - 1` denominator) assume `width` is a positive integer.\nTypeScript's `number` type can't enforce that, so a caller passing\n`0`, `-1`, or `1.5` silently produces corrupt output / NaN values.\n\nGuard once at the construction-time trust boundary so every downstream\ncall can assume validity. Existing callers passing the default or\nsensible explicit widths are unaffected; bad inputs throw a `RangeError`\nthe caller can attribute back to its own code instead of debugging a\nmis-rendered slider.\n\n* fix(widgets,button): clear active state after keyboard activation\n\nMouse activation pairs naturally (mouse_down sets active, mouse_up\nclears it). Keyboard activation had no equivalent clear — pressing\nEnter/Space set `active = true` and left it true forever, so the\nbutton kept rendering as visually pressed until something independent\nflipped it.\n\nSchedule the clear on the next microtask so the single-frame\n`active=true` paint still gives the user the visual \"pressed\" cue,\nthen matches the mouse end state. setActive() exists as a public\n@action specifically for transitions like this.\n\n* refactor(widgets): single-enforcer hover + reactive theme + cell-aware measurement\n\nThree intertwined widget-framework correctness fixes that live in the\nsame files, surfaced together by the v0.2 review pass.\n\nSingle-enforcer hover (WidgetBase + 5 widgets + InteractiveWidget)\n=================================================================\nFive widgets each had an identical `@action setHovered`. Lift it to\nWidgetBase as the canonical setter and add it to the InteractiveWidget\ncontract — EventRouter can call `widget.setHovered(inside)` on any\nimplementor without a function-existence probe or a \"synthesize\nmouse_move as a fallback\" branch. (The router-side cleanup ships in\nthe next commit, which depends on this one.)\n\nfocus()/blur() also route through handleFocus() so subclass overrides\n(Dropdown collapses its overlay on blur) see programmatic focus\nchanges the same way they see event-router transitions. Without this,\ncalling `dropdown.blur()` flipped `focused` but left the overlay\nexpanded — a real reachable bug. Test added.\n\nReactive theme propagation\n==========================\n`_theme` was a plain class field. setTheme() mutated it, but render()'s\nreads weren't tracked by MobX, so Screen's autorun never fired on swap.\nPromote to `@observable.ref private accessor _theme` and mark setTheme\n`@action` in slider / button / checkbox / toggle / dropdown so theme\nchanges drive the same re-render path as any other observable state.\n\nCell-aware widget measurement\n=============================\nbutton / checkbox / toggle measure() used `this.label.length`, which\ncounts UTF-16 code units — wide CJK and emoji labels mis-measure and\nbreak layout. Route through `cellLen` (the cell-width authority used\nby core/cells.ts and TextInput). Dropdown's width math\n(`maxLabelLen`, `headerText`, padding, clipping) went through\n`.length` / `.padEnd` / `.slice`; switch to `cellLen` / `setCellSize`\n/ `splitText` so the documented width invariant\n(maxLabelLen + 4 cells regardless of filter state) actually holds for\nwide/emoji content.\n\n* fix(widgets,event-router): ESC ESC collapse, drag capture, hover via single setHovered\n\nThree input-correctness fixes in the router, plus a parser cleanup:\n\nESC ESC collapses into one escape event\n=======================================\nSome terminals double-send the escape key as `\\x1b\\x1b`. The branch\ndocumented the case but only consumed `bytes: 1`, leaving the second\nESC to flow back through the lone-ESC flush path and emit a second\nevent — every Esc press triggered handleKey twice. Consume both bytes\nso one keypress yields one event.\n\nDrag capture for mouse_move / mouse_up\n=======================================\nRemoving mouse capture broke any widget whose interaction model relies\non dragging outside its bounds (Slider's `_dragging` mode). With pure\ntopmost-hit dispatch, a mouse_down on the slider followed by mouse_move\ninto a sibling routed motion to the sibling and orphaned the slider's\ndrag state; mouse_up on release went wherever the cursor happened to\nland, missing the slider's submit altogether.\n\nRestore drag capture: mouse_down records the hit widget; subsequent\nmouse_move / mouse_up route to the captured widget unconditionally\nuntil mouse_up clears the capture. Scroll events bypass capture (not\ndrag-scoped). The branch shape stays data-driven — same pipeline every\nevent, the captured-widget value picks the target. Tests cover both\nthe drag-out-and-still-deliver case and the scroll-bypasses-capture\ncase.\n\nHover via the canonical setHovered\n==================================\nPrevious fallback path probed `typeof widget.setHovered === \"function\"`\nand otherwise re-dispatched the mouse_move event, which never mutated\n`widget.hovered` — so the convergence check `inside !== w.hovered`\nre-fired on every move forever. Now that WidgetBase ships a canonical\nsetHovered (previous commit), the router just calls it. One setter,\nno probe, no second code path. The reverse-iter hit-test loop is\nextracted to a `topmostHit(widgets, x, y)` helper for readability.\n\nDead setEncoding block\n======================\nThe start() lifecycle had a `typeof setEncoding === \"function\"` block\nwith no body — checked existence but never called it. The buffer\nparser handles incomplete UTF-8 by deferring (`consumePrintable`\nreturns `incomplete` when a multibyte run is truncated), so the\nsetEncoding hint isn't load-bearing. Drop the dead check.\n\n* fix(widgets,focus-manager): single-dispatch focus transitions\n\nWidgetBase.focus() / blur() now route through handleFocus(), so calling\nboth widget.blur() AND widget.handleFocus({type:'blur'}) at the manager\nlevel fires every subclass override twice. Idempotent overrides\n(Dropdown's clear-filter-and-overlay) survived; non-idempotent\noverrides (a custom widget incrementing a focus counter) would not.\n\nDrop the redundant handleFocus calls in unregister / blur / setFocus —\nthe widget's own focus() / blur() is now the single entry point for the\ntransition. Test added that pins handleFocus running exactly once per\ntransition (focus → focus, blur → focus → blur).\n\n* fix(renderables,panel): cell-aware border clipping; document accessory span behavior\n\nThree places in Panel's border rendering clipped with plain\n`.slice(0, N)` even though the widths themselves were computed via\ncellLen. For wide / emoji characters the byte-count slice cut at the\nwrong cell boundary and broke border alignment. Switch all three sites\n(title fill, subtitle fill, accessory clip) to `setCellSize` so the\ncells written match the cells reserved.\n\nSeparately, the BorderAccessory doc claimed \"RichText carries its own\nstyling,\" but the implementation renders RichText accessories as\n`accessory.plain` with the wrapping `accessory.style` applied to the\nwhole string — per-range spans are dropped. The accessory is meant\nfor small status indicators (scroll position, count), not arbitrary\nspan carriers, so update the doc to match: only the wrapping style is\npreserved. Codifying the current behavior is more honest than\nsilently dropping spans; if a future caller needs span-level styling\nin a border, that's a separate Panel feature.\n\n* docs(spec): align FocusEvent name, Screen.mount signature, textual-js adapter\n\nThree remaining drift spots vs the implementation:\n- `interface FocusEvent` → `WidgetFocusEvent` to match the exported\n  type name in src/widgets/types.ts and the rest of the spec.\n- Screen.mount documented as `mount(...widgets: InteractiveWidget[])`,\n  but the type now accepts MountEntry — a bare widget OR a\n  `{ widget, placement }` pair. Document MountEntry / Placement so\n  consumers know how to invoke inline / fixed layout.\n- textual-js adapter mapping mentioned `widget.handleClick()`; the\n  contract is `widget.handleMouse(WidgetMouseEvent)`. Spell out which\n  textual-js events map to which mouse types.\n\n* perf(widgets,screen): compute frame once per tick via pendingFrame cache\n\nThe autorun called computeFrame() to subscribe to observables it reads;\ndraw() then called computeFrame() again to actually paint. Every\nreactive tick laid out the widget tree twice (render → splitLines →\noverlay pass), a real performance hit for non-trivial trees.\n\nCache the autorun's frame in `pendingFrame` and consume it in draw().\nThe autorun still subscribes to whatever the render path reads (MobX\ntracks reads inside computeFrame either way), so reactivity is\nunchanged — we just don't redo the work. draw() falls back to a fresh\ncompute when invoked outside the autorun (test paths, very first\nframe) so behavior survives those cases too.\n\n* fix(widgets): construction-boundary guards for FocusManager + Dropdown\n\nTwo trust-boundary issues that produce broken-but-self-consistent state:\n\nFocusManager.register is now idempotent\n=======================================\nThe widget list is a set in spirit. Double-registration would let\nnext() / prev() cycle through the same widget twice and leave a stale\ncopy behind after unregister(). Skip when the widget is already\npresent.\n\nDropdown clamps selectedIndex to a valid in-range value\n=======================================================\nOut-of-range `selectedIndex` (or empty options) used to render with\nan empty header label and an initial highlightedIndex that pointed\npast filteredOptions — the first Enter was a silent no-op. Clamp to\n[0, options.length - 1] when there are options; allow empty options\nas a legitimate \"not populated yet\" state with selectedIndex = 0\n(render emits an empty label; commit no-ops naturally because\nfilteredOptions is empty).\n\nTests added for both guards.\n\n* fix(widgets,static-item): measure via Segment.splitLines\n\nPrevious measure walked segments and treated `seg.text === \"\\\\n\"` as\nthe only line break. That ignored embedded newlines inside longer\nsegment.text — a renderable that emits \"a\\\\nlonger\\\\nb\" as one\nSegment was measured as 9 cells when the widest line is 6.\n\nRoute through `Segment.splitLines` (the line-break authority used by\nthe rest of the pipeline) and take the max `Segment.getLineLength`\nper line. Adds a test file with three cases covering one-segment-per-\nline, embedded newlines, and the options.measure delegate path.\n\n* refactor(widgets,types): drop unused \"click\" from WidgetMouseEvent.type\n\nEventRouter only ever emits mouse_down / mouse_up / mouse_move /\nscroll_up / scroll_down. \"click\" was never reachable — it was a leftover\nfrom an earlier design where the router might have synthesized clicks.\nKeeping unreachable values in the union forces every consumer to handle\na case that never arrives.\n\nClick semantics are derived by handlers (mouse_down + mouse_up on the\nsame widget). If we ever want a synthesized click event, we add it\ndeliberately and emit it from the router.\n\n* fix(widgets,event-router): stop() resets per-session parse + drag state\n\nstop() detached the data listener and cleared the ESC timer but left\nthe parse `buffer` and `capturedWidget` populated. A stop/start cycle\nmid-sequence (terminal hand-off, test teardown) leaked half-parsed\nbytes into the next session — the first feed after restart could\ncomplete a dangling CSI as a phantom key — and similarly leaked a\nheld drag capture so the first mouse event after restart routed to\nthe wrong widget.\n\nAlways clear `buffer` and `capturedWidget` in stop() so the router\nends in the same shape a fresh construction would. The cleanup is\nalso moved outside the `running` guard so repeated stop() (and stop\non a router that was never started) still leaves state empty —\nidempotent and defensive.\n\nTests: two new cases pin \"dangling CSI doesn't survive stop\" and\n\"drag capture doesn't survive stop.\"\n\n* fix(widgets,dropdown): filter caret respects asciiOnly\n\nrender() switched the arrow character on `options.asciiOnly`, but the\nfilter-mode caret in headerText was hard-coded to \"│\". In ASCII-only\nmode the header still emitted the box-drawing character — visible as\nmojibake in terminals that can't render U+2502.\n\nCompute the caret in render() (where the option flag lives) and pass\nit into headerText as data: \"│\" normally, \"|\" under asciiOnly,\n\"\" when not focused. headerText no longer needs to know about the\noption flag — same single-enforcer pattern as the arrow.\n\nTest added asserting the ASCII-only render emits \"|\" and never \"│\".\n\n* fix(widgets,screen): validate fixed Placement coords at mount\n\n`Placement.kind === \"fixed\"` accepts `x: number, y: number`, but the\nlayout pipeline assumes non-negative integers — negative coordinates\nindexed out of bounds in paintLines (`lines[y + i]` for y < 0 either\nasserts on `!` or skips silently) and fractional coordinates leave the\nframe in a wedged shape.\n\nmount() is the trust boundary; validate the placement there and throw\nRangeError with the offending coords. The layout pipeline downstream\ncan then assume validity. Test covers negative x, negative y, and\nfractional x.\n\n* fix(widgets,event-router): clear escTimer in stop() regardless of running state\n\nfeed() can arm escTimer on a lone-ESC remainder even when the router\nwas never started (it's a public API). The round-4 stop() reset\nguarded escTimer behind the `if (this.running)` block, so a pre-start\nfeed could leave a deferred escape firing into the next session.\n\nMove escTimer cleanup outside the `running` guard alongside the\nbuffer / capturedWidget resets (same rationale, same defensiveness).\nThe full reset is now: clear escTimer, empty buffer, null capture —\nunconditionally, every stop().\n\n* docs(spec): clarify onChange/onSubmit are semantic notifications, not render triggers\n\nThe InteractiveWidget spec described `onChange` as \"host subscribes to\nknow when to re-render\", but the implementation has long since moved\nto MobX-driven reactivity for render: WidgetBase.handleFocus,\nsetDisabled, setHovered, and the various widget hover/active paths\nmutate observable state directly without calling emitChange. A host\nrelying on onChange to drive renders would miss most of them.\n\nReframe the doc: onChange / onSubmit are app-level semantic signals\n(value changed, user activated) — not the render-trigger contract.\nRendering is driven by MobX observable reads in render(); non-MobX\nhosts re-render on their own tick / animation-frame schedule.\n\n* test(widgets,integration): pin theme reactivity end-to-end through Screen\n\nTheme reactivity now lives in @observable.ref _theme + @action setTheme\nacross all five widgets — but the unit tests on widget rendering don't\nexercise the Screen autorun, so a regression to a plain field would\nhave gone undetected at the widget level. (It has broken once already.)\n\nAdd a minimal Screen-driven test: mount a Button, capture stdout, swap\nits theme, flush microtasks, assert stdout received a new frame. Pinned\nend-to-end, not just at the widget surface.\n\n* fix(widgets,event-router): Alt+Space uses canonical \"space\" key name\n\nAlt-modified decoding routed `ESC <0x20>` through the printable-ASCII\nbranch, emitting `{ key: \" \", character: \"\", meta: true }`. The\nnon-Alt path decoded space via the SINGLE_BYTE_KEYS lookup to\n`{ key: \"space\", character: \" \" }` — so a handler matching\n`event.key === \"space\" && event.meta` would miss Alt+Space.\n\nConsult SINGLE_BYTE_KEYS first on the Alt path too. Alt+Space, Alt+Tab,\nAlt+Enter, Alt+Backspace now all get their canonical key names with\n`meta: true`. The existing standalone Alt+Backspace branch is folded\ninto the lookup (0x08 / 0x7f are already in SINGLE_BYTE_KEYS). Test\nadded pinning Alt+Space → \"space\".\n\n* test(widgets,screen): pin single-line redraw emits no cursor-up CSI\n\nThe redraw path skips the cursor-up CSI when `lastLineCount <= 1`\nbecause some terminals treat `\\\\x1b[0A` as one row up — a surprise\noff-by-one if the frame was a single line. The earlier 2-widget test\ncovers the N>1 case; this adds the boundary case so a future\n\"simplification\" that always emits `\\\\x1b[<N-1>A` doesn't silently\nregress single-line frames.\n\n* docs(spec): align Button + Dropdown rendering with implementation\n\nTwo leftover spec drift spots in the Widgets section:\n\nButton — the spec showed `[ label ]` flatly, but the implementation\nemits surrounding spaces when unfocused and switches to brackets only\nwhen focused. The full state composition (disabled > active > hover >\nnormal) and the active-differentiation-via-bold detail are documented\nso the spec matches what render() actually emits.\n\nDropdown — the spec showed `selected ▾`, but the actual collapsed\nshape is `[ <padded-label> ▾ ]` at the documented width invariant\n(maxLabelLen + 4). Filter-mode caret and asciiOnly fallbacks are also\ndescribed, plus the expanded overlay shape (rows beneath the header,\nhighlight + selected color pairings) since the spec is meant to be\nnormative.\n\n* fix(widgets,event-router): dispatchKey walks a snapshot of the chain\n\nA handler that unsubscribes itself (or another handler) during\ndispatchKey would splice the live keyChain array under the running\nfor-of loop, silently skipping the next handler. The chain is meant to\ndeliver one event to every registered participant (subject to stop());\nshifting indices breaks that contract.\n\nSnapshot the chain once at the start of dispatch. Late registrations\njoin for the next event, late removals take effect after the current\nevent finishes — same shape as DOM-style listener queues. Both\nhigh-priority and normal-priority passes reuse the same snapshot.\n\n* refactor(widgets,types): use Unsubscribe alias for InteractiveWidget subscriptions\n\nInteractiveWidget.onChange/onSubmit returned the spelled-out\n\\`() => void\\` while every other subscription in this file\n(FocusManager.onChange, EventRouter.onKey/onMouse) returns the\n\\`Unsubscribe\\` type alias. Single name for the same idea — easier to\nsearch, harder to drift, and matches the export already defined here.\n\n* test(widgets,event-router): correct the drag-capture-clear test comment\n\nThe comment claimed \"no mouse_move handler is called because no widget\nis captured.\" That's wrong — dispatchMouse does call target.handleMouse\nfor mouse_move via hit-test (target = b in this case), and the hover\nloop additionally flips b.hovered. The assertion only asks about \\`a\\`'s\nmouse_events (which IS the point — the previously-captured widget must\nnot still receive events) and was always correct; only the explanatory\ncomment was misleading.\n\n* docs(widgets,widget-base): bounds writer comment is descriptive, not normative\n\nThe comment claimed Screen.draw() is the only writer of \\`bounds\\`, but\ntest harnesses and custom layout loops also write it directly — the\nfield isn't a private-to-Screen invariant, it's a public layout-output\nslot any host can populate. Reword to \"DefaultScreen usage\" so the\nrationale (non-observable to avoid reactive surface no one uses) stays\nclear without implying an enforced invariant the type doesn't carry.\n\n* docs(widgets): align Button + Slider header comments with render()\n\nTwo doc-string drift spots in widget headers:\n\nButton — described active as \"color reversal\" but render() explicitly\ndoes NOT invert fg/bg. Active and hover share the same accent-on /\non-accent pair; active differentiates with bold. The header now\ndocuments that intentional design choice and the reason (inversion\nproduces unreadable mostly-accent-on-bg-tinted text for accents whose\ncontrast partner depends on luminance).\n\nSlider — claimed unfilled uses \"surface bg / foreground\" but render\nonly sets color: palette(\"surface\") with no bgcolor. The track sits\ntransparently against whatever the parent renderable paints\nunderneath; the comment now matches.\n\n* fix(widgets,screen): clip widget lines to remaining screen width; export overlay protocol\n\nTwo layout-correctness fixes:\n\nDefensive clipping in computeFrame\n==================================\ncomputeFrame computed shape (and thus bounds) from `widgetLines` BEFORE\nclipping, while draw() hard-clipped the final composite line at\nscreen.width. A widget that emitted more cells than its allotted slot\n(rogue widget ignoring maxWidth, or an inline placement overflowing the\nscreen edge) produced bounds wider than what actually got painted —\nbreaking hit-testing and the inline rightX that subsequent inline\nwidgets pack against.\n\nRestructure the layout loop: determine x first from placement, then\nclip each widgetLine to \\`screen.width - x\\` cells, then compute shape +\npaint + bounds. The shape now reflects the painted reality, and the\ninline rightX that downstream widgets pack against matches the\non-screen geometry. Same clip applied to the overlay pass so overlay\nbounds also match what gets painted.\n\nOverlayRenderable + hasOverlay public re-exports\n================================================\nBoth lived in src/widgets/types.ts but weren't re-exported from\nsrc/index.ts, so consumers had to deep-import to use the overlay\nprotocol — which only works for consumers who can navigate the\npackage's internal layout. Re-export them at the public surface\nalongside the other widget types/helpers.\n\n* fix(widgets,examples): consistent shift+tab character; honest demo focus targets\n\nShift+Tab character normalization\n=================================\ndecodeCSI's ESC[Z special case emitted \\`character: \"\\t\"\\` while the\nnon-shifted Tab path (via SINGLE_BYTE_KEYS lookup) emits \\`character:\n\"\"\\`. Handlers that distinguish via key + shift now see consistent\nevent shape; `character` reflects what the key would print, which for\nTab is nothing — distinguish via \\`key === \"tab\" && shift\\`.\n\nrich-theme-designer: focus targets vs theme targets\n===================================================\nThe preview TextInputs (tiArrows / tiIndices) were in \\`allInteractive\\`\nalongside the dropdowns, so focusableAt(x, y) would return them on\nclick — but they weren't mounted on the Screen, so FocusManager.focus\nrejected them and the click silently did nothing. The sample text\ntold the user \"Hit Tab to focus\" reinforcing the illusion.\n\nSplit into two lists: \\`focusTargets\\` (only mounted, focus-managed\nwidgets) and \\`themeTargets\\` (everything that resolves palette colors,\nincluding the read-only previews so theme swaps still repaint them).\nSample text reworded to make the preview's read-only status explicit.\n\n* fix(widgets,screen): stop() clears pendingFrame and renderScheduled\n\nstop() disposed the autorun but left \\`pendingFrame\\` populated with\nthe last FrameLayout (segments + bounds) and \\`renderScheduled\\` true if\na microtask was in flight. That retains memory after shutdown and\nleaves stale state behind that a subsequent start() would carry into\nits first frame.\n\nClear both in stop() — same idempotent-cleanup pattern as\nEventRouter.stop.\n\n* fix(widgets,dropdown): clamp seeded highlightedIndex on every expand\n\nThe constructor clamps selectedIndex at construction, but selectedIndex\nand options are public @observable fields — external code can write\nout-of-range values (and filtering can shift the valid range from\nunder code that captured an index earlier). Without clamping at every\nseed site, an out-of-range selectedIndex propagated into\nhighlightedIndex on expand, and the first Enter committed\nfilteredOptions[oor] = undefined as a silent no-op.\n\nAdd a private \\`clampIndex(n)\\` helper that owns \"is n a legal option\nindex\" and route both expand paths (keyboard + mouse) through it. Test\nadded: external write to selectedIndex = 99 with 3 options expand →\nhighlightedIndex is a legal index.\n\n* fix(test,widgets): restore InteractiveWidget import in focus-manager test\n\nThe earlier import cleanup dropped \\`InteractiveWidget\\` along with the\nunused \\`WidgetMouseEvent\\`, but the test still references it in two\n\\`changes: (InteractiveWidget | null)[]\\` declarations. vitest's esbuild\nstrips types without checking so the suite still ran, but \\`tsc\n--noEmit\\` would have failed — and \\`npm run lint\\` only checks src/,\nso the gap stayed hidden.\n\n* fix(widgets,screen): capture width once per frame so layout + paint can't drift\n\n\\`this.width\\` was read in multiple places (renderOptions.maxWidth,\nper-widget clipping, per-line clipping at paint time). Between the\nautorun's computeFrame and the microtask-scheduled draw, the terminal\ncan resize — bounds get computed for one width, lines clipped for\nanother. With the new defensive clipping landing this PR the divergence\nbecame more visible (per-widget bounds clip to width-1, paint clips to\nwidth-2 → off-by-N corruption).\n\nSnapshot \\`width\\` once at the top of computeFrame and store it on\nFrameLayout. draw() destructures it from the frame and uses the same\nvalue for the final line clipping. Layout, bounds, and paint all agree\neven if this.width changes mid-frame.\n\n* fix(widgets,screen): make hit-test z-order match paint z-order\n\nScreen's overlay pass painted overlay rows on top of the frame and\nexpanded the owner's bounds, but EventRouter.topmostHit independently\ntreated mount-order reverse as z-order. The two orders diverge whenever\na widget mounted AFTER an overlay owner happens to live under the\noverlay's region — clicks on overlay rows were silently delivered to\nthe widget underneath instead of the overlay owner.\n\nThe fix lifts the z-order discriminator into data the type carries:\n- computeFrame() collects each widget whose overlay produced visible\n  rows this frame into an activeOverlays Set, published on FrameLayout.\n- draw() copies it onto Screen._activeOverlays.\n- The widgets getter partitions widgetList into [base, overlay-active]\n  so EventRouter's topmostHit (unchanged) returns the overlay owner.\n\n[LAW:single-enforcer] Screen is now the sole authority on both paint\norder and hit-test order; the router consumes the result.\n\n[LAW:dataflow-not-control-flow] No new branches in the hit-tester. The\ndiscriminator (overlay-active or not) lives in the per-frame Set; the\nrouter iterates in reverse and returns the first containing widget,\nevery event.\n\nunmount() drops the widget from _activeOverlays so a getter call\nbetween unmount and next draw can't return a stale reference. stop()\nresets the Set alongside pendingFrame, matching the existing\nclean-baseline pattern.\n\nRegression coverage in test/widgets/screen.test.ts: mount-order-only\nwhen no overlays active, overlay-active widget sorted last, collapsing\nthe overlay returns to mount order, unmount drops the widget from\nz-order before the next draw."
        },
        {
          "sha": "d56a5805aa059a572c42e8c77df0ace7f08339be",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-09T04:20:08-06:00",
          "message": "feat(template-bindings): palette/theme/auto-contrast function bindings (#22)\n\n* feat(template-bindings): palette/theme/auto-contrast function bindings (rich-template-bindings-83q)\n\nAdds `paletteFuncs(resolver)` to the template binding layer:\n- Per-semantic-name functions for identifier-safe palette vars (primary, accent,\n  error, success, warning, secondary, background, foreground, surface)\n- `palette \"spec\" child` for any spec without background context (hyphenated\n  names like \"primary-muted\", darken/lighten modifiers)\n- `paletteOver \"spec\" \"#bgHex\" child` for alpha-compositing and auto-contrast specs\n- `auto \"#bgHex\" child` sugar for the common auto-contrast case\n\nBackground context is explicit (hex argument) rather than via scope side-channels —\nthe only stateless option given go-template-js functions don't access eval scope.\n\nTheme switching: consumers create paletteFuncs(newResolver) for a different theme\nand reconstruct their engine; the same template source then produces the new colors.\n\nExtracts applyStyleToFragment to shared helpers.ts to satisfy one-source-of-truth.\nSpec updated with palette/theme/auto-contrast subsection including design rationale.\n\n31 new tests; 8360 total passing.\n\n* test(template-bindings): add mid-luminance auto-contrast test per ticket verification\n\n* fix(template-bindings): preserve alpha, fix shared-helper error message, correct JSDoc\n\n- helpers.ts: error message 'style function expected' → 'template function expected'\n  since the helper is shared across both style and palette binding functions\n- palette-funcs.ts: fromRgb(r,g,b) → fromRgba(color) to preserve the full ColorRgba\n  including alpha; fromRgb forced alpha=1 and silently discarded any non-opaque\n  channel on resolved palette entries\n- palette-funcs.ts: JSDoc on resolveAndApply said 'Returns null' — function throws;\n  updated to describe the actual behavior\n\n* fix(template-bindings): correct import paths in docs/JSDoc, strengthen theme-switch test, clarify index docstring\n\n- Test: theme switching now compares full RGB triplet, not just red channel\n- palette-funcs.ts JSDoc: 'rich-js/themes' and 'rich-js/themes/paletteResolver' are\n  not exported subpaths; corrected to 'rich-js' (the root export) with all three\n  missing imports (createEngine, PaletteResolver, RichText, GRUVBOX) present\n- spec/template-bindings.md: same import path fix + add missing createEngine/RichText\n  imports so the snippet is runnable as-written\n- index.ts docstring: clarify that paletteFuncs is exported from this module but is\n  NOT registered by createRichTextEngine() — it requires a resolver and must be\n  merged separately by the consumer\n\n* docs(template-bindings): fix layer count in palette-funcs comment (three → four)\n\n* Potential fix for pull request finding\n\nCo-authored-by: Copilot Autofix powered by AI <175728472+Copilot@users.noreply.github.com>\n\n---------\n\nCo-authored-by: Copilot Autofix powered by AI <175728472+Copilot@users.noreply.github.com>"
        },
        {
          "sha": "74f03bfd849c21777461801717fcbe2f50b664c6",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-09T01:07:42-06:00",
          "message": "feat(widgets): Screen, EventRouter, Checkbox, Toggle, TextInput, Dropdown, Slider (#18)\n\n* feat(widgets): add interactive widget types, WidgetBase, and spec (rich-widgets-99r.1)\n\nIntroduces the widget framework foundation:\n- Event types (KeyEvent, WidgetMouseEvent, WidgetFocusEvent)\n- InteractiveWidget interface extending Renderable + Measurable\n- WidgetBase abstract class with MobX observable state, focus/blur,\n  hit-testing, and change/submit subscriptions\n- FocusManager and Screen interfaces for host implementations\n- spec/widgets.md with full architecture and widget specs\n\nAdds mobx as a dependency.\n\n* feat(widgets): implement DefaultFocusManager (rich-widgets-99r.2)\n\nFlat focus cycling over registered widgets with MobX observables:\n- register/unregister with auto-focus on first focusable widget\n- tab/shift-tab cycling (next/prev) that skips non-focusable/disabled\n- focus/blur dispatches WidgetFocusEvent to widgets\n- onChange subscriptions for host reactivity\n\n* feat(widgets): implement Button widget (rich-widgets-99r.5)\n\nButton with label, variant styling (default/primary/success/warning/danger),\nfocus/disabled states, and submit on enter/space/click. Implements\nInteractiveWidget directly with MobX observables.\n\n* feat(widgets): add interactive theme explorer demo\n\nrich-config demo exercises the full widget stack:\n- Button rendering with all variants\n- Focus cycling (tab/shift+tab) via DefaultFocusManager\n- Click and keyboard (space/enter) triggering\n- Mouse hover highlighting\n- MobX observable reactivity with autorun re-renders\n- Live theme preview: switches between rich-js TerminalThemes,\n  shows ANSI color grid, bg/fg swatches, sample styled text\n\nRun with: npm run config\n\nAlso fixes InteractiveWidget interface: state fields (focused,\ndisabled, visible, bounds) are writable via actions, not readonly.\n\n* chore: rename demo script from 'config' to 'widgets'\n\n* chore: rename demo script to 'demo-inputs'\n\n* chore: add justfile\n\n* fix: rich-colors Color.parse → ColorSpec.parse, add TTY guard to demo-inputs\n\n- Fix pre-existing bug in rich-colors example (Color → ColorSpec)\n- Add TTY check so demo-inputs fails gracefully when piped\n\n* fix(widgets): visible focus state, MobX strict mode crash, keyboard input\n\n- Button focus: reverse fg/bg colors + bold instead of subtle bold-only\n- Demo: wrap hover state mutations in runInAction() to fix MobX strict\n  mode errors that were crashing on mouse motion outside buttons\n- The strict mode errors were also likely swallowing keyboard events\n  (tab, space, enter) by disrupting the render loop\n\n* feat(widgets): four visual states + theme-aware colors + handleMouse\n\nBreaking interface changes:\n- handleClick → handleMouse (receives all mouse event types)\n- Added hovered, active observable states to InteractiveWidget\n\nButton improvements:\n- Four distinct visual states: normal, hover, focus, active\n- Theme-aware colors via ANSI palette index mapping\n  (variant → ANSI color idx, resolved from TerminalTheme)\n- setTheme() to update colors when active theme changes\n- Active state: reverse + bold + underline (mouse_down)\n- Focus state: reverse + bold\n- Hover state: lightened background\n- Normal state: variant-mapped ANSI colors\n\nDemo fixes:\n- Forward all mouse events to widgets (not just click)\n- MobX strict mode: hover mutations wrapped in runInAction\n- Theme propagation via autorun reaction\n\n* fix: keyboard input — parseKey intercepted tab/enter/space before KEY_NAMES\n\nRoot cause: parseKey checked charCode < 32 (ctrl+letter branch) before\nlooking up KEY_NAMES. Tab (0x09), enter (0x0D), and newline (0x0A) all\nmatched the ctrl+letter case, producing wrong key names like \"i\", \"m\".\nSpace (0x20) fell through to the printable case, producing key=\" \"\ninstead of key=\"space\".\n\nFix: check KEY_NAMES for single-character data before the ctrl+letter\nand printable branches. Now tab/enter/space/backspace all resolve to\ntheir expected key names.\n\nAlso: keyboard active state now flashes visually via setTimeout(80ms)\ninstead of being set-and-cleared in the same MobX action (which was\ninvisible to autorun).\n\n* fix(widgets): box-drawing border for focused state, multi-line rendering in demo\n\n- Button focused state now renders 3-row box-drawing border (╭─╮, │, ╰─╯)\n  instead of underline/reverse, matching Textual convention\n- Active state uses color reversal (fg/bg swap) without border\n- Fix duplicate this.id assignment in Button constructor\n- Demo renderWidget() uses Segment.splitLines() for multi-line output\n- Demo layout adjusted for 3-row focused button height\n- Tests updated for new rendering behavior\n\n* fix(widgets): inline focus brackets — same width in every state\n\nFocus uses `[ label ]` brackets instead of multi-line box-drawing border,\neliminating layout jumps. All four states (normal, hover, focus, active)\nrender as single-line segments with identical width (label.length + 4).\n\n* fix(demo): enable any-event mouse tracking for hover state\n\nSwitch from ?1002h (motion only during drag) to ?1003h (all motion)\nso mouse_move events fire without a button held, enabling hover detection.\n\n* feat(color): semantic palette system — Button uses theme palette instead of ANSI indices\n\n- Add buildPalette() factory: derives muted/text-*/surface keys from base\n  accent colors using Textual's formulas (blend 70% for muted, tint 66%\n  for text, 5% lift for surface)\n- Palette is now required on TerminalTheme (no fallbacks)\n- Button resolves colors from theme.palette via semantic keys:\n  normal=muted bg, hover=full accent, active=fg/bg swap\n- All three pre-built themes (Default, Monokai, SVG Export) include palettes\n\n* feat(themes): add 16 Textual themes (nord, gruvbox, dracula, etc.)\n\nAll themes use Textual's base colors with buildPalette() for derived\nsemantic keys. Demo now shows all 19 themes with wrapping button layout.\n\nAdded: Nord, Gruvbox, Dracula, Tokyo Night, Flexoki, Catppuccin\n(Mocha/Latte/Frappe/Macchiato), Solarized (Dark/Light), Rose Pine\n(Dawn/Moon/Dawn), Atom One (Dark/Light).\n\n* feat(demo): rich theme explorer with panels, table, tree, progress bars\n\nRedesigned demo layout to showcase rich-js components:\n- Panel with themed border showing current theme name\n- Semantic palette swatches (full + muted for each accent)\n- Table with component listing and header styles\n- Tree showing theme color hierarchy\n- Progress bars using all variant colors\n- Text style showcase (bold, dim, italic, underline, reverse)\n- ANSI palette grid\n\n* feat(widgets): Screen — render loop and ANSI output\n\nDefaultScreen owns widget lifecycle, composes FocusManager, and drives a\nMobX-reactive render loop. State changes push redraws (via autorun)\ndebounced through a microtask, instead of polling on an interval.\n\nANSI strategy: cursor-up + overwrite, never clear-and-redraw. Each\nframe emits a single \\x1b[<n>A to the top of the previous frame, then\n\\x1b[K (erase to end of line) per line as new content overwrites old.\nShrinking frames pad with extra erase sequences to wipe trailing rows.\n\nLayout: widgets stack top-to-bottom at column 0; bounds are written\nduring draw so mouse hit-testing has a single source of truth.\n\nCloses rich-widgets-99r.4. Unblocks rich-widgets-99r.3 (EventRouter).\n\n* feat(widgets): EventRouter — stdin escape sequence parser\n\nParses raw stdin into KeyEvent / WidgetMouseEvent values and dispatches\nthem via the Screen's FocusManager and per-widget bounds. Handles plain\nkeys, control bytes, CSI sequences (arrows, nav, F1-F12), SS3 F-keys,\nand SGR + X10 mouse encodings (press / release / motion / scroll).\n\nAdds a `widgets` getter to the Screen interface so consumers like the\nrouter can read the live widget list — single source of truth lives on\nthe screen, not duplicated in callers.\n\nTab navigation drives focusManager.next/prev; other keys go to the\nfocused widget. Mouse events go to the topmost widget under the\npointer; mouse_move additionally drives hover transitions across all\nvisible widgets.\n\nLone-ESC ambiguity is resolved by deferring emission to setImmediate,\nso a follow-up CSI chunk extends the sequence instead of producing a\nspurious escape event. Tests use PassThrough + a sync feed/flush API.\n\nCloses rich-widgets-99r.3.\n\n* refactor(widgets): Button extends WidgetBase (rich-widgets-99r.11)\n\nRemove duplicated observable accessors, handler sets, focus/blur/\nsetDisabled/containsPoint methods — these now come from WidgetBase.\nKeep Button-specific state (id, label, variant, theme), Button-specific\nbehaviour (handleKey/handleMouse, render, measure, palette resolution),\nand the setHovered/setActive mutators used by EventRouter's fast path.\n\n[LAW:one-type-per-behavior] shared widget infrastructure lives on one\ntype. Net -53 lines; all 25 button tests pass without modification.\n\n* feat(widgets): Checkbox — boolean toggle with label (rich-widgets-99r.6)\n\nExtends WidgetBase. Observable checked/label, semantic palette colours\n(primary when checked, foreground when unchecked), focus visualised by\nunderline, disabled dimmed. Space toggles + emitChange; enter emits\nsubmit; mouse_up toggles + emitChange. Width = label.length + 4.\nASCII fallback \"[x]\" / \"[ ]\" via options.asciiOnly.\n\n[LAW:dataflow-not-control-flow] one Segment of fixed width every render;\nindicator character and style come from observable state.\n[LAW:one-type-per-behavior] shared infrastructure inherited from WidgetBase.\n\n32 new tests in test/widgets/checkbox.test.ts mirror button.test.ts\ncoverage. Full suite: 1181 passed, lint clean.\n\n* feat(widgets): Toggle — on/off switch with variant colour (rich-widgets-99r.7)\n\nExtends WidgetBase. Observables: on, label, variant (\"default\" |\n\"primary\" | \"success\" | \"warning\" | \"danger\"). Constructor:\n{ label, on?, id?, disabled?, theme?, variant? }.\n\nBehaviour: space toggles + emitChange; enter emitSubmit (no toggle);\nmouse_up toggles + emitChange; disabled gates both.\n\nRender: 1 Segment of fixed width = label.length + 6. Indicators\n\"[ON] \" / \"[OFF]\" are both exactly 5 cells. ON uses full variant\naccent fg/bg; OFF uses muted variant background. Underline on focus,\ndim on disabled.\n\n[LAW:dataflow-not-control-flow] indicator and palette keys come from\nstate via lookup tables, not control-flow that skips work.\n[LAW:one-type-per-behavior] shared infrastructure inherited from\nWidgetBase.\n\n32 new tests in test/widgets/toggle.test.ts mirror Button/Checkbox\ncoverage. Full suite: 1213 passed, lint clean.\n\n* feat(widgets): TextInput — single-line editable text field (rich-widgets-99r.10)\n\nExtends WidgetBase. Observables: value, cursorPosition, placeholder.\nConstructor: { value?, placeholder?, id?, disabled?, theme?, maxLength?,\npassword? }.\n\nBehaviour:\n  - printable keys → insert at cursor + emitChange (respects maxLength)\n  - backspace/delete → remove neighbour char + emitChange (no-op at edges)\n  - left/right/home/end → move cursor, clamped\n  - enter → emitSubmit\n  - mouse_down → cursorPosition = clamp(event.x - bounds.x - 1)\n  - disabled gates all inputs\n\nRender: fixed-width \"[\" + content + \"]\". Content width derived from\ndata: floor MIN_CONTENT_WIDTH=8, ceiling maxWidth-2. Window scrolls to\nkeep cursor visible. When focused, exactly one cell is the cursor\n(primary-bg). Placeholder rendered dim when focused+empty. Password mode\nrenders \"•\" per char. Disabled dims the entire segment list.\n\n[LAW:dataflow-not-control-flow] cursor is a data position; rendering\nalways paints exactly one cell as cursor when focused.\n\n40 tests in test/widgets/text-input.test.ts cover insertion, deletion,\ncursor movement, click-to-position, placeholder, password masking,\ndisabled gating, submit, rendering invariants, measure.\n\n* feat(widgets): Dropdown — single-select with collapsed/expanded states (rich-widgets-99r.8)\n\nExtends WidgetBase. Observables: options, selectedIndex, expanded,\nhighlightedIndex (separate from selectedIndex until enter commits).\nConstructor: { options, selectedIndex?, id?, disabled?, theme? }.\n\nBehaviour:\n  - collapsed: enter/space → expand (highlightedIndex seeds from selected)\n  - expanded: up/down → move highlighted (clamped); enter → commit\n    selectedIndex = highlightedIndex, collapse, emitChange + emitSubmit;\n    escape → collapse without change\n  - mouse_up inside collapsed bounds → expand\n  - mouse_up while expanded: option row → select+collapse; header → just\n    collapse; outside bounds → collapse without change\n  - disabled gates everything\n\nRender: header row [label_padded ▾] (ASCII \"v\"), then if expanded an\n\"\\\\n\" + 1 row per option [ label_padded ]. Total width = maxLabelLen + 4\nfor every row. Highlighted row uses primary-muted bg; selected uses\nprimary bg; others use surface bg. Underline on focus, dim on disabled.\n\n[LAW:single-enforcer] Screen owns vertical layout — Dropdown emits \"\\\\n\"\nbetween rows and Screen splits via Segment.splitLines; widgets below\nshift down naturally on the @observable expanded change.\n\n29 tests in test/widgets/dropdown.test.ts cover expansion, navigation,\nselection, escape-cancel, click variants (option/header/outside),\ndisabled gating, render rows + arrow + ASCII fallback, measure.\n\n* feat(widgets): Slider — numeric value with min/max/step (rich-widgets-99r.9)\n\nExtends WidgetBase. Observables: value, min, max, step, width.\nConstructor: { value?, min?, max?, step?, width?, id?, disabled?, theme? }.\n\nBehaviour:\n  - left/right → value -= step / value += step (clamped + snapped)\n  - home/end → value = min / value = max\n  - mouse_down → jump value to fractional click position; mouse_move\n    while drag in progress → continuous update; mouse_up → emitSubmit\n  - emitChange after every value change; ignored when value didn't change\n  - disabled gates everything\n\nRender: exactly `width` cells. Marker index = round((value - min) /\nrange * (width - 1)). Filled portion left of marker uses primary fg,\nunfilled portion uses surface fg. Marker (●, ASCII \"*\") uses primary\nfg + bold. Underline on focus, dim on disabled.\n\n[LAW:dataflow-not-control-flow] every render emits exactly `width`\ncells; the marker position is data, not a branch.\n[LAW:single-enforcer] clampSnap() is the single mutation gate — all\nvalue changes route through it, so observable state is always in\nrange and on-step.\n\n33 tests in test/widgets/slider.test.ts cover keyboard adjustment,\nclamping, snap-to-step, click-to-jump, drag, disabled gating, render\nshape (single marker, marker at edges, ASCII fallback, fixed width),\nmeasure, hit-testing.\n\n* feat(widgets): demo + end-to-end integration test (rich-widgets-99r.12)\n\nTwo deliverables:\n\n1. examples/widgets-demo/index.ts — interactive showcase. Mounts one of\n   each widget (Button, Checkbox, Toggle, TextInput, Dropdown, Slider)\n   into a DefaultScreen and routes raw stdin through an EventRouter. A\n   StateRow widget reads each widget's observables in render(); MobX's\n   autorun in Screen subscribes to whatever was read on the last render\n   and refires when any tracked observable changes — no manual event\n   wiring. Tab navigates focus (FocusManager); Ctrl-C exits cleanly via\n   router.onKey + screen.stop()/router.stop(). Wired to npm run\n   widgets-demo (and just widgets-demo).\n\n2. test/widgets/integration.test.ts — drives the full pipeline\n   programmatically: DefaultScreen with a captured Writable + EventRouter\n   pointed at a PassThrough fake stdin. Mounts Button + Checkbox + Toggle\n   + TextInput, feeds crafted byte sequences (tab=0x09, shift+tab=ESC[Z,\n   space=0x20, CR=0x0d, ctrl+c=0x03, printable bytes, backspace=0x7f),\n   asserts both widget state transitions AND the ANSI output Screen\n   produced (cursor-up + erase-line on subsequent frames; no clear-screen\n   sequence; debounce of multi-input bursts into one frame).\n\nAlso exports the new widgets (Checkbox, Toggle, TextInput, Dropdown,\nSlider) and EventRouter from the package root src/index.ts so consumers\nimport from \"rich-js\" rather than reaching into src/widgets/.\n\n[LAW:verifiable-goals] the integration test is the machine-verifiable\nacceptance criterion — green means the whole stack agrees end to end.\n\n16 new tests; full suite 1331 passed. Lint clean.\n\nPre-existing TypeScript errors in examples/rich-config/index.ts (stale\nColorRgba/ColorSpec API usage) are unrelated to this work and predate\nthis commit.\n\n* refactor(demo): integrate new widgets into rich-config theme explorer (rich-widgets-99r.12 / .12.1)\n\nReplaces the parallel examples/widgets-demo/ I added in d0b63c4 with\nproper integration into the existing rich-config demo (the theme\nexplorer / 'demo-inputs' entrypoint). The 5 new widgets — Checkbox,\nToggle, Slider, TextInput, Dropdown — sit in an \"Interactive Widgets\"\nsection between the theme buttons and the existing palette / progress\nbars showcase. They re-theme on every theme switch via the same\nautorun that themes the buttons (single setTheme pass over allWidgets);\ntheir state changes are logged to the bottom log strip alongside the\nbutton submissions.\n\nAlso fixes the stale ColorRgba/ColorSpec API errors in rich-config\n(closes the rich-widgets-99r.12.1 follow-up):\n  - palette.get(...) returns ColorRgba; Style expects ColorSpec — wrap\n    via ColorSpec.fromRgba(...) at every call site (now via a\n    paletteColor() helper).\n  - Style.toAnsi was removed — replaced with the segmentsToString +\n    Segment([heading], style) flow.\n  - Dropped unused imports (Console, TerminalTheme, Rule, Table, Tree,\n    HEAVY_HEAD).\n  - Trimmed the content showcase (dropped Rule, Table, Tree, style row;\n    kept title panel, palette swatches, progress bars, ANSI palette)\n    to make room for the new widgets.\n\nRemoved:\n  - examples/widgets-demo/ — replaced by the rich-config integration.\n  - npm run widgets-demo + just widgets-demo — same.\n\nLayout is now dynamic: renderButtons + renderInteractiveWidgets each\nreturn the next free row, so adding/removing widgets shifts everything\nbelow without hard-coded row constants going stale. Dropdown reserves\nworst-case rows (1 + options.length) so its expansion never overlaps\ncontent.\n\n[LAW:single-enforcer] one autorun, one setTheme pass over every widget\nthat has setTheme — no per-widget-type branches.\n\nFull suite still 1331 passed; demo-build (tsc -p tsconfig.demo.json)\nnow compiles cleanly. Demo correctly errors out on non-TTY stdin.\n\n* fix(widgets,palette): readable fg on accent backgrounds via on-${accent} keys\n\nThe Toggle ON state, Button hover/active, Dropdown selected row, and\nTextInput cursor cell all paint a full accent colour as background.\nPreviously they paired that bg with text-${accent} (mostly the accent\nitself, ~66% blended) — same-hue clash, ~poor contrast. Button active\nmade it worse by swapping fg/bg (\"color reversal\"), which produced\nmostly-bg-tinted text on mostly-accent bg — same visual problem.\n\nThe fix is one new piece of palette data: an on-${accent} key for each\nof {primary, secondary, accent, success, warning, error}, computed once\nat palette-build time as contrastFor(accent) — pure black or pure white\nper WCAG luminance. Widgets read on-${accent} when the bg is the full\naccent; widgets read text-${accent} when the bg is mostly-bg-tinted\n(e.g. *-muted, surface) — that contrast already holds.\n\n[LAW:single-enforcer] contrastFor() is the only place that picks\nblack-vs-white for \"text on this accent\". No widget computes it ad-hoc;\nno widget inverts fg/bg to fake contrast.\n\nPer-widget changes:\n- Button: hover and active now share the same colour pair (full accent\n  bg + on-${accent} fg). Active is differentiated by bold, NOT by\n  swapping fg/bg. Removed the resolveFg helper that's no longer used.\n- Toggle: ON-state onFg switched from text-${accent} → on-${accent}.\n  OFF-state keeps text-${accent} (offBg is mostly-bg, contrast holds).\n- TextInput: cursor cell fg switched from \"background\" → on-primary.\n  Same effect for typical themes; consistent rule across widgets.\n- Dropdown: selected row fg switched from text-primary → on-primary.\n  Highlighted row keeps text-primary on primary-muted (contrast holds).\n- Checkbox, Slider: no changes (no full-accent-bg states).\n\nTests:\n- 4 new buildPalette tests assert on-${accent} keys exist, return pure\n  B/W, follow the accent's luminance (yellow→black, dark-red→white),\n  and are theme-bg-independent.\n- 2 new Button tests lock in: hover/active fg is on-${accent} (B/W),\n  active and hover use the same colour pair with bold differentiating\n  them. Existing \"color reversal\" test renamed and tightened.\n- 2 new Toggle tests assert ON fg is B/W and never equals ON bg.\n\nFull suite 1339 passed, lint + demo build clean.\n\n* fix(widgets): address PR review — capture, theme reactivity, dedup hover, double-render\n\nSix fixes triggered by PR #18 review threads. Each tightens a constraint so\nthe type carries variability the body was compensating for.\n\n- ESC ESC consumes both bytes (event-router.ts). Returning bytes:1 left the\n  second ESC in the buffer where the lone-ESC timer would emit a duplicate\n  escape event. The intent is one Escape per double-ESC, so consume both.\n  [LAW:dataflow-not-control-flow] gesture is data; the buffer head decides.\n\n- Removed the empty setEncoding branch (event-router.ts). We re-encode every\n  chunk to Buffer ourselves; the no-op block was dead and misleading.\n  [LAW:dataflow-not-control-flow] one path per chunk, no source-encoding mode.\n\n- Mouse capture (event-router.ts). On mouse_down, record the receiving\n  widget; route subsequent mouse_move and mouse_up to it regardless of\n  bounds until release. Fixes Slider drag-out-of-bounds (was leaving\n  _dragging=true forever) and makes Dropdown's outside-click collapse path\n  reachable. [LAW:single-enforcer] one place that knows cross-widget\n  routing. [LAW:dataflow-not-control-flow] capture target is data; the\n  pickMouseTarget helper is a pure function of (capture, hit, type).\n\n- setHovered lifted to WidgetBase (widget-base.ts, types.ts, all six\n  widgets). The router was probing for an optional setHovered method with\n  a fallback that didn't actually set hovered. That's two paths for one\n  operation. Now setHovered is a single canonical mutator on WidgetBase\n  (and required by InteractiveWidget); per-widget overrides removed; the\n  router calls widget.setHovered(value) unconditionally. The broken\n  fallback is gone. [LAW:single-enforcer] [LAW:one-source-of-truth]\n\n- Cached frame in DefaultScreen (screen.ts). The autorun called\n  computeFrame() to subscribe and draw() called it again — every reactive\n  tick did layout twice. Now the autorun stores pendingFrame and draw()\n  consumes it. One computeFrame per tick.\n  [LAW:dataflow-not-control-flow] same pipeline, one pass.\n\n- Theme observability (checkbox/toggle/text-input/dropdown/slider).\n  _theme was a plain field, so setTheme() didn't trigger the screen's\n  autorun unless another observable changed. Now @observable.ref + @action;\n  theme swaps redraw immediately. [LAW:dataflow-not-control-flow] palette\n  reads in render() are reactive dependencies.\n\nTests added:\n- ESC ESC produces exactly one escape event with no flush\n- Mouse capture: drag outside bounds still receives move/up; subsequent\n  gestures use hit-test again after release\n- setTheme on Checkbox/Toggle/TextInput triggers a screen redraw\n\n* fix(widgets): correct screen cursor-up off-by-one, forbid empty Dropdown and width<1 Slider at the boundary\n\n[LAW:types-are-the-program] After writing N lines (N-1 inter-line `\\n`,\nno trailing newline), cursor lands at end of line N. Returning to the\ntop of the frame is `N-1` rows up. Screen.draw() emitted `\\x1b[NA`,\nwhich lands one line above the frame and overwrites unrelated content\nwhen the screen isn't at the top of the terminal. Move up `N-1`, and\nskip the move entirely when N==1 (the cursor is already on the only\nline).\n\n[LAW:types-are-the-program] Dropdown with zero options is an illegal\nstate — selectedIndex has no valid value, navigation can drive\nhighlightedIndex to -1. Throw on empty options at construction so every\ndownstream callsite can assume non-empty. Out-of-range selectedIndex\nnow also throws at construction.\n\n[LAW:types-are-the-program] Slider width <= 0 makes the render contract\n(\"emits exactly `width` cells\") unsatisfiable since the marker\nalready occupies one cell. Throw at construction.\n\nUpdates two existing tests that previously asserted the off-by-one\n(`\\x1b[2A`, `\\x1b[4A`) to the corrected magnitudes, and adds three new\nregression tests: single-line frame emits no cursor-up, two-line frame\nmoves up one, plus the Dropdown/Slider validation cases.\n\n* fix(widgets): cell-aware measure/render and doc/comment accuracy\n\n- measure() in Checkbox, Toggle, TextInput, Dropdown now uses cellLen()\n  instead of .length so wide glyphs (CJK/emoji) report correct terminal\n  cell width. [LAW:one-source-of-truth] cellLen is the single authority.\n- TextInput render() windowing fully cell-aware: rawDisplayCells,\n  cursorCells, splitText for window extraction, setCellSize for padding,\n  splitText for cursor cell split. cursorPosition stays as code-unit\n  index (editing ops require it) and is converted to cells once.\n- Dropdown header/option padding switched from .padEnd (code units) to\n  setCellSize (cell-accurate). maxLabelLen() uses cellLen per label.\n- event-router consumeEscape: correct comment (emits Escape, re-processes\n  b1 on next call — does not silently drop the ESC byte).\n- slider.ts doc: unfilled track uses surface foreground colour with\n  terminal-default background (not \"surface bg / foreground\").\n- test/core/color.test.ts: remove duplicate ANSI_COLOR_NAMES import\n  (was a TS compilation error)."
        },
        {
          "sha": "9944472c5b1a6a9eb8cc15d645798bf6844d7235",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-08T23:38:34-06:00",
          "message": "feat(template-bindings): style functions + link with multi-cell contract (#21)\n\n* feat(template-bindings): register link function with multi-cell contract (rich-template-bindings-5xd)\n\nAdds the `link` function to richTextFuncs(): `{{ link URL child }}` sets\nthe wrapper Style's link slot, exactly mirroring `Style.parse(\"link URL\")`.\nThe implementation is one more entry in the existing factory pattern —\nno special-case AST node, no LinkFragment subclass, no second emitter\npath. The cell-boundary signal consumers walk is `fragment.style.link`\nbeing truthy.\n\nWhy one signal is enough — the engine returns one fragment per top-level\ntemplate expression, and Style.add propagates `link` from inner to outer\nfragment with right-wins-on-conflict semantics:\n\n- {{ link \"u1\" \"a\" }} {{ link \"u2\" \"b\" }} → 3 fragments, 2 with link\n  set, the literal \" \" between them as inter-cell joiner.\n- {{ link \"outer\" (link \"inner\" \"x\") }} → 1 fragment, link = \"outer\"\n  (outer-wins).\n- {{ bold (link \"u\" \"x\") }} → 1 fragment, both bold and link set —\n  link survives the outer wrapping, so it still qualifies as a cell.\n- {{ red \"hello\" }} → 1 fragment, no link → no cells, all joiner.\n\nspec/template-bindings.md gains a \"link function and multi-cell contract\"\nsection documenting the cell-boundary signal and the consumer-side\ncell-splitting algorithm in enough detail that a renderer can be\nimplemented from the spec alone.\n\nTests: 6 new link-function tests (composition, nesting, equivalence with\nStyle.parse) plus 6 multi-cell-contract tests using a small splitCells\nhelper that implements the documented consumer algorithm. Smoke test's\nspot-check inventory updated to include `link`.\n\n* chore(markup-spec): cross-reference markup and template-bindings specs\n\nBoth approaches are shipped and independent. Adds a \"when to use each\"\nnote to each spec so readers know the alternative exists before diving\ninto syntax details. Closes rich-markup-spec-8q7.\n\n* Update agent guidance\n\n* fix(template-bindings): retire stale spec constraint + bump engines.node to >=20\n\nThe bootstrap spec had a time-box constraint ('do not document specific\nstyle functions yet') that is now stale — epics 4p4 and 5xd have landed\nand their function inventories are documented. Replace with a structural\nrule that stays true forever: each new function family belongs in its own\nsubsection under 'Registered styling functions'.\n\nAlso bumps engines.node from >=18 to >=20 to match the floor declared by\n@promptctl/go-template-js, which this package now depends on."
        },
        {
          "sha": "3de09f5c8ac1f0edbdfebfc63b728e32e9a2b961",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-08T14:35:34-06:00",
          "message": "feat(themes,color): semantic palette + 16 Textual themes (#17)\n\n* feat(widgets): add interactive widget types, WidgetBase, and spec (rich-widgets-99r.1)\n\nIntroduces the widget framework foundation:\n- Event types (KeyEvent, WidgetMouseEvent, WidgetFocusEvent)\n- InteractiveWidget interface extending Renderable + Measurable\n- WidgetBase abstract class with MobX observable state, focus/blur,\n  hit-testing, and change/submit subscriptions\n- FocusManager and Screen interfaces for host implementations\n- spec/widgets.md with full architecture and widget specs\n\nAdds mobx as a dependency.\n\n* feat(widgets): implement DefaultFocusManager (rich-widgets-99r.2)\n\nFlat focus cycling over registered widgets with MobX observables:\n- register/unregister with auto-focus on first focusable widget\n- tab/shift-tab cycling (next/prev) that skips non-focusable/disabled\n- focus/blur dispatches WidgetFocusEvent to widgets\n- onChange subscriptions for host reactivity\n\n* feat(widgets): implement Button widget (rich-widgets-99r.5)\n\nButton with label, variant styling (default/primary/success/warning/danger),\nfocus/disabled states, and submit on enter/space/click. Implements\nInteractiveWidget directly with MobX observables.\n\n* feat(widgets): add interactive theme explorer demo\n\nrich-config demo exercises the full widget stack:\n- Button rendering with all variants\n- Focus cycling (tab/shift+tab) via DefaultFocusManager\n- Click and keyboard (space/enter) triggering\n- Mouse hover highlighting\n- MobX observable reactivity with autorun re-renders\n- Live theme preview: switches between rich-js TerminalThemes,\n  shows ANSI color grid, bg/fg swatches, sample styled text\n\nRun with: npm run config\n\nAlso fixes InteractiveWidget interface: state fields (focused,\ndisabled, visible, bounds) are writable via actions, not readonly.\n\n* chore: rename demo script from 'config' to 'widgets'\n\n* chore: rename demo script to 'demo-inputs'\n\n* chore: add justfile\n\n* fix: rich-colors Color.parse → ColorSpec.parse, add TTY guard to demo-inputs\n\n- Fix pre-existing bug in rich-colors example (Color → ColorSpec)\n- Add TTY check so demo-inputs fails gracefully when piped\n\n* fix(widgets): visible focus state, MobX strict mode crash, keyboard input\n\n- Button focus: reverse fg/bg colors + bold instead of subtle bold-only\n- Demo: wrap hover state mutations in runInAction() to fix MobX strict\n  mode errors that were crashing on mouse motion outside buttons\n- The strict mode errors were also likely swallowing keyboard events\n  (tab, space, enter) by disrupting the render loop\n\n* feat(widgets): four visual states + theme-aware colors + handleMouse\n\nBreaking interface changes:\n- handleClick → handleMouse (receives all mouse event types)\n- Added hovered, active observable states to InteractiveWidget\n\nButton improvements:\n- Four distinct visual states: normal, hover, focus, active\n- Theme-aware colors via ANSI palette index mapping\n  (variant → ANSI color idx, resolved from TerminalTheme)\n- setTheme() to update colors when active theme changes\n- Active state: reverse + bold + underline (mouse_down)\n- Focus state: reverse + bold\n- Hover state: lightened background\n- Normal state: variant-mapped ANSI colors\n\nDemo fixes:\n- Forward all mouse events to widgets (not just click)\n- MobX strict mode: hover mutations wrapped in runInAction\n- Theme propagation via autorun reaction\n\n* fix: keyboard input — parseKey intercepted tab/enter/space before KEY_NAMES\n\nRoot cause: parseKey checked charCode < 32 (ctrl+letter branch) before\nlooking up KEY_NAMES. Tab (0x09), enter (0x0D), and newline (0x0A) all\nmatched the ctrl+letter case, producing wrong key names like \"i\", \"m\".\nSpace (0x20) fell through to the printable case, producing key=\" \"\ninstead of key=\"space\".\n\nFix: check KEY_NAMES for single-character data before the ctrl+letter\nand printable branches. Now tab/enter/space/backspace all resolve to\ntheir expected key names.\n\nAlso: keyboard active state now flashes visually via setTimeout(80ms)\ninstead of being set-and-cleared in the same MobX action (which was\ninvisible to autorun).\n\n* fix(widgets): box-drawing border for focused state, multi-line rendering in demo\n\n- Button focused state now renders 3-row box-drawing border (╭─╮, │, ╰─╯)\n  instead of underline/reverse, matching Textual convention\n- Active state uses color reversal (fg/bg swap) without border\n- Fix duplicate this.id assignment in Button constructor\n- Demo renderWidget() uses Segment.splitLines() for multi-line output\n- Demo layout adjusted for 3-row focused button height\n- Tests updated for new rendering behavior\n\n* fix(widgets): inline focus brackets — same width in every state\n\nFocus uses `[ label ]` brackets instead of multi-line box-drawing border,\neliminating layout jumps. All four states (normal, hover, focus, active)\nrender as single-line segments with identical width (label.length + 4).\n\n* fix(demo): enable any-event mouse tracking for hover state\n\nSwitch from ?1002h (motion only during drag) to ?1003h (all motion)\nso mouse_move events fire without a button held, enabling hover detection.\n\n* feat(color): semantic palette system — Button uses theme palette instead of ANSI indices\n\n- Add buildPalette() factory: derives muted/text-*/surface keys from base\n  accent colors using Textual's formulas (blend 70% for muted, tint 66%\n  for text, 5% lift for surface)\n- Palette is now required on TerminalTheme (no fallbacks)\n- Button resolves colors from theme.palette via semantic keys:\n  normal=muted bg, hover=full accent, active=fg/bg swap\n- All three pre-built themes (Default, Monokai, SVG Export) include palettes\n\n* feat(themes): add 16 Textual themes (nord, gruvbox, dracula, etc.)\n\nAll themes use Textual's base colors with buildPalette() for derived\nsemantic keys. Demo now shows all 19 themes with wrapping button layout.\n\nAdded: Nord, Gruvbox, Dracula, Tokyo Night, Flexoki, Catppuccin\n(Mocha/Latte/Frappe/Macchiato), Solarized (Dark/Light), Rose Pine\n(Dawn/Moon/Dawn), Atom One (Dark/Light).\n\n* feat(demo): rich theme explorer with panels, table, tree, progress bars\n\nRedesigned demo layout to showcase rich-js components:\n- Panel with themed border showing current theme name\n- Semantic palette swatches (full + muted for each accent)\n- Table with component listing and header styles\n- Tree showing theme color hierarchy\n- Progress bars using all variant colors\n- Text style showcase (bold, dim, italic, underline, reverse)\n- ANSI palette grid\n\n* refactor(themes): break core/color <-> themes/buildPalette cycle\n\n`core/color.ts` runtime-imported `buildPalette` from `themes/buildPalette.ts`\nto construct preset `TerminalTheme` constants, while `buildPalette.ts`\nruntime-imports `ColorRgba`/`blendRgb` from `core/color.ts`. ESM tolerated\nthe cycle today but it's fragile (partially-initialized exports if either\nside adds top-level work) and complicates bundling/tree-shaking.\n\n[LAW:one-way-deps] Make the dependency strictly themes/* -> core/color:\n\n- New `src/themes/terminalThemes.ts` owns DEFAULT_TERMINAL_THEME plus the\n  18 Textual-derived presets. All of them now live alongside the rest of\n  the theming layer.\n- `core/color.ts` keeps a private `INTERNAL_DEFAULT_THEME` (STANDARD_TABLE\n  + black/white + empty Palette) for the `getTruecolor` fallback. Runtime-\n  importing the Palette class from `themes/palette.ts` is safe: that\n  module only `import type`s from core/color, so no runtime back-edge.\n- `core/style.ts` no longer imports DEFAULT_TERMINAL_THEME — the substrate\n  fallback (black) is inlined as a local `SURFACE_BLACK` constant.\n  Decouples the style layer from preset theme constants entirely.\n- `widgets/button.ts` now imports DEFAULT_TERMINAL_THEME from themes/.\n- `src/index.ts` re-exports preset themes from the new module.\n- Tests updated to import preset themes from `themes/terminalThemes.js`.\n\nAlso document on `TerminalTheme` that `ansiColors` is the canonical ANSI\nLUT, not a per-theme override — preset themes share STANDARD_TABLE; theme\nidentity lives in `palette` (matches Textual's design).\n\n[LAW:single-enforcer] Button color resolution collapsed to a single\n`resolveColors()` helper that returns `{ fg, bg, bgHover }` unconditionally;\nrender() picks one bg by state. Removes the dead-code `bgKey: \"bg\" | \"hover\"`\nparameter (only ever called with \"bg\") and the `resolveFg()` duplicate.\n[LAW:dataflow-not-control-flow] same resolution path runs every render;\nthe state selects which already-computed color to apply.\n\n* refactor(themes,demo): defineTheme helper to dedupe bg/fg, fix demo tree label and unused imports\n\n[LAW:one-source-of-truth] Each preset theme stated `background`/`foreground`\ntwice — once as `TerminalTheme` constructor args, once inside the\n`buildPalette` base — and could drift if either copy was edited. Introduce a\n`defineTheme({ name, isDark, bg, fg, primary, secondary, accent, success,\nwarning, error })` helper that takes one set of named colours and forwards\nthem to both. Each preset collapses to one call with no duplication.\n\nAlso tighten the file header: `core/color → themes/palette` is the only\nruntime edge into themes/ (used by `INTERNAL_DEFAULT_THEME`); the cycle\nthat was broken was the reverse one through `TerminalTheme` presets.\n\nDemo: `Tree` doesn't interpret ANSI escapes in its label, so the explorer\nwas printing literal `\\x1b[1m` around the theme name. Swap to a styled\n`RichText`. Drop unused `ColorSpec`, `Console`, and `TerminalTheme` imports\n(`tsconfig.demo.json` has `noUnusedLocals: true`, so these break the\ndemo build)."
        },
        {
          "sha": "4d34770f07782d70fde87ccfbd81de362a7eccff",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-08T14:27:12-06:00",
          "message": "feat(widgets): framework foundation — types, WidgetBase, FocusManager, Button (#16)\n\n* feat(widgets): add interactive widget types, WidgetBase, and spec (rich-widgets-99r.1)\n\nIntroduces the widget framework foundation:\n- Event types (KeyEvent, WidgetMouseEvent, WidgetFocusEvent)\n- InteractiveWidget interface extending Renderable + Measurable\n- WidgetBase abstract class with MobX observable state, focus/blur,\n  hit-testing, and change/submit subscriptions\n- FocusManager and Screen interfaces for host implementations\n- spec/widgets.md with full architecture and widget specs\n\nAdds mobx as a dependency.\n\n* feat(widgets): implement DefaultFocusManager (rich-widgets-99r.2)\n\nFlat focus cycling over registered widgets with MobX observables:\n- register/unregister with auto-focus on first focusable widget\n- tab/shift-tab cycling (next/prev) that skips non-focusable/disabled\n- focus/blur dispatches WidgetFocusEvent to widgets\n- onChange subscriptions for host reactivity\n\n* feat(widgets): implement Button widget (rich-widgets-99r.5)\n\nButton with label, variant styling (default/primary/success/warning/danger),\nfocus/disabled states, and submit on enter/space/click. Implements\nInteractiveWidget directly with MobX observables.\n\n* feat(widgets): add interactive theme explorer demo\n\nrich-config demo exercises the full widget stack:\n- Button rendering with all variants\n- Focus cycling (tab/shift+tab) via DefaultFocusManager\n- Click and keyboard (space/enter) triggering\n- Mouse hover highlighting\n- MobX observable reactivity with autorun re-renders\n- Live theme preview: switches between rich-js TerminalThemes,\n  shows ANSI color grid, bg/fg swatches, sample styled text\n\nRun with: npm run config\n\nAlso fixes InteractiveWidget interface: state fields (focused,\ndisabled, visible, bounds) are writable via actions, not readonly.\n\n* chore: rename demo script from 'config' to 'widgets'\n\n* chore: rename demo script to 'demo-inputs'\n\n* chore: add justfile\n\n* fix: rich-colors Color.parse → ColorSpec.parse, add TTY guard to demo-inputs\n\n- Fix pre-existing bug in rich-colors example (Color → ColorSpec)\n- Add TTY check so demo-inputs fails gracefully when piped\n\n* fix(widgets): visible focus state, MobX strict mode crash, keyboard input\n\n- Button focus: reverse fg/bg colors + bold instead of subtle bold-only\n- Demo: wrap hover state mutations in runInAction() to fix MobX strict\n  mode errors that were crashing on mouse motion outside buttons\n- The strict mode errors were also likely swallowing keyboard events\n  (tab, space, enter) by disrupting the render loop\n\n* feat(widgets): four visual states + theme-aware colors + handleMouse\n\nBreaking interface changes:\n- handleClick → handleMouse (receives all mouse event types)\n- Added hovered, active observable states to InteractiveWidget\n\nButton improvements:\n- Four distinct visual states: normal, hover, focus, active\n- Theme-aware colors via ANSI palette index mapping\n  (variant → ANSI color idx, resolved from TerminalTheme)\n- setTheme() to update colors when active theme changes\n- Active state: reverse + bold + underline (mouse_down)\n- Focus state: reverse + bold\n- Hover state: lightened background\n- Normal state: variant-mapped ANSI colors\n\nDemo fixes:\n- Forward all mouse events to widgets (not just click)\n- MobX strict mode: hover mutations wrapped in runInAction\n- Theme propagation via autorun reaction\n\n* fix: keyboard input — parseKey intercepted tab/enter/space before KEY_NAMES\n\nRoot cause: parseKey checked charCode < 32 (ctrl+letter branch) before\nlooking up KEY_NAMES. Tab (0x09), enter (0x0D), and newline (0x0A) all\nmatched the ctrl+letter case, producing wrong key names like \"i\", \"m\".\nSpace (0x20) fell through to the printable case, producing key=\" \"\ninstead of key=\"space\".\n\nFix: check KEY_NAMES for single-character data before the ctrl+letter\nand printable branches. Now tab/enter/space/backspace all resolve to\ntheir expected key names.\n\nAlso: keyboard active state now flashes visually via setTimeout(80ms)\ninstead of being set-and-cleared in the same MobX action (which was\ninvisible to autorun).\n\n* fix(widgets): box-drawing border for focused state, multi-line rendering in demo\n\n- Button focused state now renders 3-row box-drawing border (╭─╮, │, ╰─╯)\n  instead of underline/reverse, matching Textual convention\n- Active state uses color reversal (fg/bg swap) without border\n- Fix duplicate this.id assignment in Button constructor\n- Demo renderWidget() uses Segment.splitLines() for multi-line output\n- Demo layout adjusted for 3-row focused button height\n- Tests updated for new rendering behavior\n\n* fix(widgets): inline focus brackets — same width in every state\n\nFocus uses `[ label ]` brackets instead of multi-line box-drawing border,\neliminating layout jumps. All four states (normal, hover, focus, active)\nrender as single-line segments with identical width (label.length + 4).\n\n* fix(demo): enable any-event mouse tracking for hover state\n\nSwitch from ?1002h (motion only during drag) to ?1003h (all motion)\nso mouse_move events fire without a button held, enabling hover detection.\n\n* fix(widgets): address PR review threads on widgets foundation\n\nRefactor Button to extend WidgetBase, fix dead change-notification path,\ncorrect measurement against terminal cell width, harden focus-manager\nregistration, align spec with the exported types, and unblock\ntype-checking of widget tests.\n\n- Button now extends WidgetBase [LAW:one-source-of-truth,\n  LAW:one-type-per-behavior]: shared state fields, subscriptions,\n  hit-testing, focus/blur, and emitChange live in the base. Button keeps\n  only label/variant/theme state and its specific event/render logic.\n- WidgetBase actions (focus, blur, setHovered, setActive, setDisabled,\n  handleFocus) emit change exactly once each, so the onChange contract\n  is no longer a dead promise. setTheme on Button is observable and\n  emits change.\n- Button.handleKey clears `active` after emitting submit so a\n  keyboard-activated button no longer stays visually pressed.\n- Button.measure uses cellLen for terminal cell width so wide /\n  combining characters do not corrupt layout [LAW:one-source-of-truth].\n- Button header comment rewritten to describe the actual single-row\n  rendering (spaces vs `[`/`]` brackets on focus) and the four-state\n  precedence the code implements.\n- DefaultFocusManager.register throws on duplicate registration\n  [LAW:dataflow-not-control-flow] — silent dedupe would corrupt focus\n  cycling and break unregister later. Test added.\n- StubWidget in widget-base.test.ts exposes triggerChange/triggerSubmit\n  test-only wrappers; emitChange/emitSubmit stay protected on the base\n  and are no longer accessed across the class boundary.\n- spec/widgets.md aligned with src/widgets/types.ts: WidgetMouseEvent /\n  WidgetFocusEvent / WidgetBounds names, mutable observable hovered /\n  active / bounds, handleMouse instead of handleClick.\n\n* fix(widgets): collapse focus dispatch to single enforcer, surface keyboard active flash, doc/spec drift\n\n[LAW:single-enforcer] FocusManager and WidgetBase double-dispatched every\nfocus transition: the manager called both `widget.focus()`/`blur()` AND\n`widget.handleFocus({type})`, and each path independently set `focused`\nand emitted change. Collapse `focus()`/`blur()` into delegates that call\n`handleFocus({type})` so all transitions converge on one mutation site.\nDrop the redundant calls from FocusManager.\n\n[LAW:dataflow-not-control-flow] Button.handleKey toggled `active`\ntrue→false inside one `@action`, which is invisible to MobX autorun\nobservers — they only see the post-action state. Schedule the clear on\n`queueMicrotask` so the action exits with `active=true` (one reaction\ncycle) and the clear runs in its own action (second cycle).\n\n[LAW:one-source-of-truth] InteractiveWidget.onChange/onSubmit now reuse\nthe existing `Unsubscribe` alias instead of redeclaring the shape.\n\nDemo: rich-config stored `bounds.x = col` even though `writeAt` uses\n1-based columns and mouse events use 0-based — fix to `col - 1` so\nhit-testing aligns with `WidgetMouseEvent.x`. Stale comment in\nrich-colors updated to `ColorSpec.parse()`. Spec/widgets.md bullet\nupdated from non-existent `handleClick`/`focus`/`blur` to actual\n`handleMouse`/`handleFocus`. Test imports cleaned up.\n\nAdds two regression tests: one asserts focus()/blur() emits exactly once\nper transition (single-enforcer); the other asserts keyboard activation\nmakes active=true observable across a microtask boundary."
        },
        {
          "sha": "7265f8ee22bd4dfbadc1e3a57ad317d661a14b45",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-08T14:18:07-06:00",
          "message": "feat(template-bindings): bootstrap engine + register fg/attribute/bg style functions (#20)\n\n* feat(template-bindings): bootstrap go-template-js binding module (rich-template-bindings-eeg)\n\nAdds go-template-js as the styling-template engine. Exposes a new\nsrc/template-bindings module whose public surface is a factory for\nEngine<RichText> with rich-js's style functions registered, plus a\nFuncMap factory consumers can merge into their own engine.\n\nThe bootstrap registration set is intentionally empty — follow-up epics\non the template-bindings topic populate it (foreground/attribute/\nbackground, link, palette, auto-contrast). T is committed to RichText\nso every future style function inherits one trust boundary: fromString\nlifts string literals, toString flattens to .plain for printf %s slots.\n\nThe new spec/template-bindings.md documents the function-registration\ncontract at the API level — output is RichText, no silent flattening,\ncomposition is plain function nesting — without naming any specific\nfunction, so subsequent epics extend it incrementally.\n\nA smoke test parses + evaluates a literal-only template through the\nbinding entry point and asserts RichText fragment shape end-to-end.\n\n* chore(template-bindings): rename go-template-js → @promptctl/go-template-js\n\nThe engine package was published to npm under the scoped name\n@promptctl/go-template-js@0.1.0, replacing the file:../go-template-js\nlocal dependency.\n\nUpdates package.json, the binding-module import, the public re-export\ncomment in src/index.ts, and the spec doc to reference the published\nname. package-lock.json still resolves go-template-js → file:../ and\nwill be regenerated in the follow-up commit once the registry serves\nthe published version (currently 404 — propagation in progress).\n\n* chore: gitignore .claude/*.lock harness runtime artifacts\n\n* feat(template-bindings): register fg / attribute / bg style functions (rich-template-bindings-4p4)\n\nPopulate richTextFuncs() with the colour and attribute primitives that\nfollow-up consumer templates need:\n\n- Named foreground colours, generated from ANSI_COLOR_NAMES so the table\n  is the single source of truth for the inventory.\n- Generic foreground forms: color (palette index), hex, rgb.\n- Background via a single `on` function that accepts any colour spec.\n- All thirteen text attributes plus their not_<attr> negations, plus the\n  short aliases (b/i/u/s/r/o/d/uu) from spec/style.md.\n\nComposition follows Style.add — outer (most recently applied) style wins\non conflicts. A template-built fragment round-trips through Style.parse\nwithout semantic drift; the spec documents this as the binding contract\nand the tests assert it directly.\n\nEngine-side note: the binding declares child slots as the new `liftable`\nArgType (added in @promptctl/go-template-js@0.1.1), so string literals\nflow through the engine's fromString bridge into RichText before the\nfunction body runs — no per-function lifting in the binding.\n\nBumps the engine dep to ^0.1.1. Updates spec/template-bindings.md with\nthe function inventory, naming choices, and composition rules. Replaces\nthe eeg-era empty-FuncMap assertion in the smoke test with a populated\nspot-check; the exhaustive inventory lives in style-funcs.test.ts.\n\n* chore(template-bindings): regenerate lockfile against @promptctl/go-template-js@^0.1.1\n\nResolves package-lock to the published registry entry for the template-bindings\ndep so this PR can land independently of the widget stack.\n\n* refactor(template-bindings): canonicalise attribute lists and align node engine with deps\n\nAddress PR #20 review feedback:\n\n- Export `ATTRIBUTE_NAMES` and `ATTRIBUTE_SHORT_ALIASES` (renamed from\n  in-file `SHORT_ALIASES`) from `src/core/style.ts`. The template\n  binding's `style-funcs.ts` now imports these instead of carrying its\n  own duplicate copies — a single canonical inventory drives both\n  `Style.parse`/`Style.toString` and the template registration set.\n  [LAW:one-source-of-truth]\n\n- Bump `engines.node` to `>=20` in `package.json` and update the same\n  claim in `README.md` and `CLAUDE.md`. The `@promptctl/go-template-js`\n  dependency declares `engines.node >=20`, so the previous `>=18` floor\n  was a manifest fiction the dep tree forbade. [LAW:one-source-of-truth]\n\n- Refresh the `template-bindings/index.ts` module-header docstring.\n  It claimed the bootstrap registration set was empty, but this PR's\n  `richTextStyleFuncs()` registers fg / attribute / bg. The doc now\n  describes what the engine actually provides today, and what the\n  follow-up epics still owe. [LAW:one-source-of-truth]\n\n- Reword `spec/template-bindings.md` to drop the bogus `FuncMap<RichText>`\n  type parameter (`FuncMap` is not generic; the fragment type lives on\n  `Engine<T>` / `EngineConfig<T>`), and reconcile the Constraints section\n  with the body — fg / attribute / bg are now in-scope and documented;\n  link / palette / theme / auto-contrast remain deferred to follow-ups.\n  Also drop the stale \"bootstrap registration set is empty by design\"\n  line in the function-registration-contract section.\n\n* fix(template-bindings): narrow hex to its advertised domain, clarify FuncMap T contract\n\n[LAW:types-are-the-program] `hex` advertised `#RRGGBB` / `#RRGGBBAA` but\ndelegated to `ColorSpec.parse`, which silently accepted named colours,\n`rgb(...)`, and `color(N)` — `hex \"red\"` would succeed and route through\nthe wrong branch. Validate against the hex pattern at the call site so\nmisuse fails loudly with a `RangeError` naming the expected form.\n\nThe `richTextFuncs()` and spec docstrings claimed the map could be merged\ninto any `EngineConfig<T>.funcs` regardless of `T`. The map type-checks\nagainst any `T`, but the registered functions return `RichText` and\n`instanceof`-check their lifted children — so `T ≠ RichText` compiles\nand fails at evaluation time. Reword both to state the runtime\nconstraint explicitly."
        },
        {
          "sha": "55c4653085f5a55dc1488be4f1e4e2b4b2bf7403",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-03T17:12:41-06:00",
          "message": "feat(color): unify ColorTriplet + ColorQuad into ColorSpec/ColorRgba with alpha at render (#12)\n\n* refactor(color): finish Color→ColorSpec, ColorTriplet→ColorRgba renames\n\nVSCode rename-symbol left several call sites broken (5 type errors plus\nhalf-renamed test files and examples). This commit completes the mechanical\nsweep across src/core, src/themes, tests, and examples so npm run lint and\nthe full test suite are green.\n\nNo behavior change. ColorSystem and the field 'triplet' on ColorSpec are\nintentionally untouched — they get addressed in follow-up commits per the\nunification plan.\n\n* refactor(color): collapse ColorSystem into ColorDepth — single fidelity enum\n\nColorSystem and ColorDepth shared values STANDARD/EIGHT_BIT/TRUECOLOR/WINDOWS\nwith identical names; ColorDepth additionally had DEFAULT=0. They were one\nordinal axis (which palette / how much fidelity), not two — per\n[LAW:one-type-per-behavior], that's one type with two valid subsets.\n\n- Delete enum ColorSystem; all 178 references now point to ColorDepth.\n- Delete the ColorSpec.system getter, which existed only to translate\n  DEFAULT→STANDARD for the now-deleted ColorSystem enum. Internal callers\n  use .type directly; the downgrade short-circuit for DEFAULT was already\n  covered by an earlier early-return.\n- performDowngrade: the DEFAULT case (depth=0) previously could not be\n  reached because ColorSystem had no DEFAULT; with ColorDepth.DEFAULT now\n  a valid target, downgrading to DEFAULT returns ColorSpec.default()\n  (drops color, emits 39/49 SGR).\n\nThe colorSystem field/option names on Console / RenderToStringOptions\nare intentionally untouched here — they remain typed against ColorDepth\nbut keep the colorSystem identifier until the next commit handles the\nColorSystemSpec / SPEC_TABLE / FORCE_COLOR_TABLE collapse.\n\n* refactor(color): collapse SPEC_TABLE / FORCE_COLOR_TABLE / TERM_PROGRAM_TABLE into one STRING_TO_DEPTH\n\nThree separate string-keyed tables and a TRUECOLOR_TERMS Set all mapped\ndistinct strings onto the same ColorDepth axis. The keys were disjoint\nacross sources (CLI specs, FORCE_COLOR values, TERM_PROGRAM names, exact\nTERM names), so per [LAW:one-source-of-truth] one table is the honest\nrepresentation.\n\n- Drop ColorSystemSpec type. Public option types accept 'string | ColorDepth | null'\n  directly; the parser handles unrecognized strings by throwing\n  ColorParseError instead of silently falling back. Loud failure beats\n  hidden compatibility paths (per scripting-discipline guidance).\n- Drop SPEC_TABLE, FORCE_COLOR_TABLE, TERM_PROGRAM_TABLE, TRUECOLOR_TERMS.\n  All folded into STRING_TO_DEPTH, with sections grouped by source.\n- detectColorSystem looks up TERM, TERM_PROGRAM, and FORCE_COLOR through\n  the unified table — same lookup site for every source.\n\n* feat(color): add alpha to ColorRgba; rename ColorSpec.triplet→value, fromTriplet→fromRgba\n\nColorRgba now actually carries alpha — the rename from ColorTriplet only\nchanged the class name without adding the channel. Alpha defaults to 1\n(fully opaque), so all existing call sites continue to work unchanged.\n\n- ColorRgba constructor validates rgb in [0,255] and alpha in [0,1];\n  throws RangeError on out-of-range. [LAW:single-enforcer] — one validation\n  site for value invariants. Float-arithmetic callers (HSL roundtrips,\n  blends) round before construction; the constructor refuses to silently\n  mask their bugs.\n- compositeOver(bg) — per-channel lerp by alpha; alpha=1 short-circuits\n  to 'this' so callers can invoke unconditionally [LAW:dataflow-not-control-flow].\n- hex emits 6-char #RRGGBB when alpha=1, 8-char #RRGGBBAA otherwise.\n  Single getter, data-driven branch on alpha === 1.\n- rgb emits rgb(r,g,b) when alpha=1, rgba(r,g,b,a) otherwise.\n- normalized returns 4-tuple [r/255, g/255, b/255, alpha].\n\nColorSpec.triplet field renamed to .value — property names should not\nencode channel count. ColorSpec.fromTriplet → fromRgba for the same\nreason. Internal local variables named 'triplet' (in performDowngrade,\nparseSingle) are kept since they're block-scoped descriptive names, not\npublic API.\n\nColorTable.match cache key now includes alpha so two values that differ\nonly in alpha don't collide; the distance metric still uses RGB only\n(alpha is not a perceptual distance dimension).\n\nparseRgbaHex(hex) added for 8-char input. parseSingle accepts\n'#rrggbbaa' literals (8-char regex takes precedence over 6-char to avoid\nprefix matching).\n\nNote: this commit does NOT make the render pipeline alpha-aware. fg/bg\nemission still drops alpha at the SGR layer. The full sweep across\nconsumers (color math, render pipeline, palette resolver, etc.) is\ndeferred to a separate plan.\n\n* feat(color): composite alpha at render boundary, finishing color-unify (rich-color-unify-dg9)\n\nAdds `ColorSpec.flattenAlpha(bg)` and wires it into `Style.render` so that\ntruecolor alpha is flattened against the known bg *before* downgrade and\nANSI emission. Closes the \"flatten then lose alpha\" path that downgrading\na translucent truecolor silently exposed: palette matching now sees the\nalready-composited RGB, not the pre-composite one.\n\nThe render path resolves a layered substrate every call (surface →\nflattened bg → fg) — opaque colors short-circuit inside `compositeOver`,\nso the call shape is unconditional ([LAW:dataflow-not-control-flow]).\n`flattenAlpha` is the single enforcement site for alpha-collapse on the\nrender path ([LAW:single-enforcer]).\n\nAlso drops the last `ColorTriplet` reference (a stale doc comment in\npaletteResolver).\n\nTests: 7 new render-boundary cases in style.test.ts cover opaque\npass-through, fg-over-bg compositing, fg-over-surface-when-no-bg,\ntranslucent-bg flattening, layered fg+bg, the pre-downgrade ordering\ninvariant, and palette-fg passthrough; 5 new flattenAlpha unit cases in\ncolor.test.ts.\n\n* fix(color): address review — stale docs, prototype-chain lookup, example renames\n\n- palette.ts: update doc comment from \"parse to triplets\" to \"parse to\n  ColorRgba\" after the ColorTriplet→ColorRgba rename.\n- color.ts: replace `spec in STRING_TO_DEPTH` with `Object.hasOwn()` to\n  avoid prototype-chain false positives (e.g. \"toString\" matching).\n- color-math.ts: replace remaining `Color.fromRgb` → `ColorSpec.fromRgb`\n  (examples excluded from tsc, so the build didn't catch these).\n- shell.ts: replace remaining `Color.parse` → `ColorSpec.parse` (same).\n\n* ci: add CI workflow for typecheck and tests on PR/push\n\nAlso includes package.json exports for themes/data and themes/registry.\n\n* fix(color): integer-only channels, remove dead WINDOWS downgrade\n\nassertChannel now rejects non-integer values — hex2() can't produce\nvalid hex from floats like 10.5 (\"a.8\"). Also removes the unreachable\nWINDOWS case from performDowngrade (it's a detection result, not a\ndowngrade target)."
        },
        {
          "sha": "c06ad4ae22535bbfbd5b0f55ccd4b440fd7c1902",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-05-03T16:42:53-06:00",
          "message": "Add ./themes/data/ mapping (#13)"
        },
        {
          "sha": "e67c38ad330d44cef66796886dd977a2b4645212",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-30T02:58:12-06:00",
          "message": "feat(themes): PaletteResolver — Textual-style spec language (rich-theming-foundation-lq3.3) (#8)\n\nResolves spec strings against a Palette into ColorTriplets:\n\n  spec = name (\"-darken-\" N | \"-lighten-\" N)? (\" \" NN \"%\")?\n  name = \"auto\" | <palette var name>\n\n  \"primary\"                 → palette[\"primary\"]\n  \"primary-background\"      → palette[\"primary-background\"]    (hyphenated names work)\n  \"primary-darken-3\"        → darken(primary, 3)\n  \"primary 50%\"             → alphaBlend(primary, against, 0.5)\n  \"auto 33%\"                → alphaBlend(contrastFor(against), against, 0.33)\n  \"primary-darken-3 50%\"    → modifier first, then composite\n\nReturns null for: invalid syntax, missing var, or alpha/auto without\nrequired `against`. Pure (no internal state).\n\nThe modifier regex is right-anchored — `.+` is greedy — so var names that\nthemselves contain hyphens (like `primary-background`) parse correctly:\nthe trailing `-darken-N` / `-lighten-N` segment is treated as the modifier\nand everything before it is the var name.\n\n`auto` is modeled as a synthetic var whose lookup returns\n`contrastFor(ctx.against)`. The modifier→alpha pipeline downstream is\nidentical for `auto` and named vars; the only branch is at the lookup\nseam.\n\nFoundation complete for layer-1 of the theming redesign per\ndesign-docs/STATUSLINE-THEMING.md. Layers 2 (PaletteMapping) and 3\n(cascade) live in claude-powerline.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-authored-by: Claude <noreply@anthropic.com>\nCo-authored-by: Happy <yesreply@happy.engineering>"
        },
        {
          "sha": "a0949ac096affa96d2788a727d80747cf43f2ac2",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-30T02:39:25-06:00",
          "message": "refactor(render): drop endWithNewline option from renderToString (rich-render-newline-default-zwo) (#7)\n\nThe flag was a redundant second source of truth for trailing newlines: a\nconsumer that wants one writes `renderToString(x) + \"\\n\"`, and the\nrenderable's own segment stream is already authoritative for everything\nemitted before that. Removing the option eliminates a quiet footgun for\nembedded one-shot rendering (e.g. statuslines) where the implicit\nnewline was breaking output.\n\n[LAW:one-source-of-truth] segments → bytes; no parallel newline channel.\n[LAW:no-mode-explosion] dropped a knob with no defensible use case."
        },
        {
          "sha": "4de857bca47b1287d212a6ffbbaabda4e2c026cb",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-30T02:28:46-06:00",
          "message": "chore: gitignore .mcp.json (project-local MCP config) (#6)"
        },
        {
          "sha": "22b74b291150aed027c113cd44838ca0c5d164b8",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-30T01:47:08-06:00",
          "message": "Core improvements: color resolver, progress markup fix, strip enhancements (#4)\n\n* feat(color): add resolveColorSystem + detectColorSystem (rich-color-compat-resolver-3o7)\n\nLifts terminal color capability detection out of Console's private impl into\ncanonical exported functions. Consumers (Console, renderToString, downstream\nCLIs like claude-powerline) all delegate to a single resolver — one source of\ntruth for spec-string → ColorSystem mapping and one source of truth for\nenv+TTY auto-detection.\n\n- New `ColorSystemSpec`: \"auto\" | \"truecolor\" | \"256\" | \"ansi\" | \"none\"\n- `resolveColorSystem(spec, options?)` — string → ColorSystem | null\n- `detectColorSystem(options?)` — env + isTTY → ColorSystem | null\n  - Honors NO_COLOR (any non-empty value, per no-color.org)\n  - Honors FORCE_COLOR (0/1/2/3/true/false)\n  - TERM=dumb/unknown, COLORTERM=truecolor/24bit\n  - Known truecolor terminals (kitty, ghostty, wezterm, alacritty, foot, contour)\n  - TERM_PROGRAM map (iTerm.app, Apple_Terminal, vscode, Tabby)\n  - TERM regex patterns for -256color and generic xterm/screen/etc\n- Console.colorSystem option widened to `ColorSystemSpec | ColorSystem | null`;\n  string vocab unified on the new spec (\"standard\"→\"ansi\", drop \"windows\" from\n  strings — reachable via ColorSystem.WINDOWS enum). Per no-back-compat-shim.\n- renderToString.colorSystem accepts the same union; strings route through\n  resolveColorSystem.\n- 33 new tests in test/core/colorSystem.test.ts covering all spec strings,\n  env precedence, TTY behavior, and renderToString integration.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* fix(progress): TextColumn parses markup instead of leaking tags as literals (rich-core-y80)\n\nDefault Progress column format is \"[progress.description]{task.description}\".\nPreviously TextColumn substituted the description but emitted the surrounding\nmarkup tags as literal text — output line started with the literal string\n\"[progress.description]compile\" instead of a styled \"compile\".\n\nFix routes the substituted format through `renderMarkup(...).render(options)`,\nmatching the canonical string-with-tags → styled-segments path used everywhere\nelse. Task descriptions are escaped before substitution so user content cannot\ninject stray markup tags (e.g. a description of \"[red]boom[/]\" now renders as\nthe literal text \"[red]boom[/]\", not a styled \"boom\").\n\n5 new regression tests in test/renderables/progress.test.ts.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* docs(strip): document single-style invariant for StyledRenderable (rich-strip-single-style-invariant-41m)\n\nStrengthens the JSDoc on `StyledRenderable` to explicitly state the\nload-bearing invariant joiners depend on: every Segment yielded by `render()`\nmust have `bgcolor` equal to `this.style.bgcolor` or undefined. Inline fg and\ntext-attribute variation is fine; only `bgcolor` is constrained, because\nthat's the only field joiners read.\n\nAdds:\n- \"## Single-style invariant\" section in docs/strip.md with the rule, two\n  concrete bad examples (inline-bg Segment, Panel wrapper), and the safe-path\n  guidance pointing to `StripCell`.\n- Strengthened JSDoc on `StripCell` calling it the canonical safe\n  implementation.\n\nNo code change — `StripCell` already satisfies the invariant by construction\nand is the recommended helper. No new API surface added (per\n[LAW:no-mode-explosion]); the implicit convention is now explicit contract.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* feat(strip): StripCell parts form for inline fg variation (rich-strip-cell-inline-fg-9z7)\n\nAdds a parts-array construction shape to `StripCell` so consumers can express\ninline foreground / attribute variation under a single cell-level bg without\nwriting a custom `StyledRenderable`. Real driver: claude-powerline's git\nstatus segment, where `S`/`+3` render green and `-2` renders red, all on the\nsame blue cell.\n\nAPI:\n\n  new StripCell(text, style)        // unchanged single-run shape\n  new StripCell(parts, style)       // new — parts: { text, style? }[]\n\nCell-level `style` always governs the joiner-visible bg per the\nsingle-style invariant on `StyledRenderable`. Parts may overlay fg and text\nattributes only; passing a part whose `style` sets `bgcolor` throws at\nconstruction — `make it impossible` over `policy + hope`.\n\nRender walk is identical for both shapes (a string is just a one-element\nparts list internally), keeping the dataflow uniform — variability lives in\nthe parts data, not in branching.\n\nIncludes:\n- `StripCellPart` interface, exported from public index\n- Behavior-level tests for parts form, joiner-visible bg, validation, and\n  the empty-parts edge case\n- Docs: new \"Inline foreground variation — `StripCell` parts form\" section\n  in docs/strip.md\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* fix(core): plug three correctness gaps caught in PR #4 review\n\n- `renderToString` accepts optional `env` and `isTTY` so callers using\n  `colorSystem: \"auto\"` can keep rendering deterministic. Default\n  behavior unchanged: with no options, `\"auto\"` still consults ambient\n  process state. Header docstring updated to acknowledge the\n  caveat ([LAW:dataflow-not-control-flow] — detect options are passed\n  unconditionally; `resolveColorSystem` ignores them for non-`\"auto\"`).\n- `Console` now computes the effective `isTTY` of the actual output\n  target (stdout / stderr / custom file / forceTerminal) and forwards\n  it to color resolution. Previously, `new Console({ stderr: true })`\n  could disable color when stdout was redirected even though stderr\n  was a TTY (and vice versa). The new helper mirrors the `isTerminal`\n  getter so the cached `_colorSystem` agrees with runtime\n  `isTerminal` ([LAW:single-enforcer]).\n- `TextColumn.render` uses a replacement callback so task descriptions\n  containing `$&`, `$1`, `$$`, etc. are inserted literally rather than\n  reinterpreted as `String.replace` patterns.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* Theming foundation: ColorTable rename, Palette type, color math (#5)\n\n* refactor(color): rename Palette → ColorTable to free name for theming (rich-rename-palette-to-color-table-v6m)\n\nThe existing Palette class is a quantization LUT (integer-indexed\nColorTriplet table with nearest-match for downgrade math), not a\npalette in the colloquial sense. The theming foundation work\n(rich-theming-foundation-lq3) needs the name 'Palette' for the\nsemantic name → color concept. ColorTable is honest — it's a\nlookup table — and frees the name.\n\n- class Palette → ColorTable\n- STANDARD_PALETTE / EIGHT_BIT_PALETTE / WINDOWS_PALETTE → *_TABLE\n- build256Palette / buildWindowsPalette → build256Table / buildWindowsTable\n- Updated TerminalTheme.ansiColors type, Color.downgrade callsites\n- Updated index.ts exports + tests\n\nBreaking change for direct importers of these symbols. Only known\nconsumer (claude-powerline) doesn't import Palette directly.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* feat(themes): Palette type — semantic name → ColorTriplet (rich-theming-foundation-lq3.1)\n\nFoundation for the theming work. Palette is the layer-1 type from\ndesign-docs/STATUSLINE-THEMING.md (in claude-powerline): a named map\nfrom semantic variable name (primary, accent, error, ...) to\nColorTriplet, plus a dark/light hint.\n\nDistinct from ColorTable (just renamed) — that's the integer-indexed\nquantization LUT used by the downgrade pipeline. Palette is aesthetic\nintent; ColorTable is terminal-spec mechanism.\n\nSpec resolution (modifier, alpha, auto-contrast) lives in\nPaletteResolver in a follow-up ticket; this type is just the data\nshape.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* feat(themes): color math primitives — darken/lighten/alphaBlend/contrastFor (rich-theming-foundation-lq3.2)\n\nFoundation for the spec language consumed by PaletteResolver:\n- darken(color, levels): HSL lightness reduction by 10% per level\n- lighten(color, levels): inverse of darken (negative levels)\n- alphaBlend(fg, bg, alpha): linear RGB compositing, alpha clamped to [0,1]\n- contrastFor(bg): WCAG relative-luminance threshold (0.179) → black or white\n\nAlgorithm choices documented in JSDoc. May need tuning to match\nTextual's exact output once theme data lands and hex-equivalence\ntesting reveals drift; for now matches common conventions.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* fix(themes,color): tighten invariants flagged in PR #5 review\n\n- `hslToRgb` now clamps each channel to `[0, 255]` after rounding.\n  Float HSL math at the boundaries can land at -1 or 256 even after\n  `Math.round`, which would have produced an invalid `ColorTriplet`.\n- `Palette` defensive-copies the `vars` map in the constructor.\n  `ReadonlyMap` is a compile-time aliasing constraint, not a runtime\n  one — a caller mutating the original `Map` would have silently\n  changed the palette. Copying makes immutability real at the seam.\n- `ColorTable.match` docstring says \"table index\" instead of \"palette\n  index\" so it doesn't collide with the new semantic `Palette` type.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n---------\n\nCo-authored-by: Claude <noreply@anthropic.com>\nCo-authored-by: Happy <yesreply@happy.engineering>\n\n---------\n\nCo-authored-by: Claude <noreply@anthropic.com>\nCo-authored-by: Happy <yesreply@happy.engineering>"
        },
        {
          "sha": "6a8aa414169b1a481cc989141f496eeed9d412e4",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-29T03:38:29-06:00",
          "message": "demo(rich-dash): pluggable dashboard runtime (#3)\n\n* demo(rich-dash): add pluggable dashboard runtime (rich-demo-87y)\n\nLive, alt-screen dashboard whose cells are widget modules implementing\n{ id, init, tick, render }. The runtime owns one Live, one tick scheduler,\nand one immutable state map; layout cells are bound to widget ids by name,\nso adding a widget = one new file in widgets/ + one entry in config.ts.\n\nv1 ships three widgets: sysinfo (Table over node:os), notes (Markdown of\nREADME.md), and job (looping fake build pipeline driving Progress as a\npure Renderable). Run with `npm run dash`.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* fix(rich-dash): keep alt-screen from scrolling on overflow\n\nLive appends a trailing newline after each refresh; in alt-screen mode\nthat scrolls the saved buffer one row per frame whenever rendered output\nfills the terminal. The dashboard's content was 59 lines into a 24-row\nviewport, so every frame scrolled and the demo looked like inline output\neven though Live had entered the alt buffer.\n\nTwo adapters in runtime/clip.ts contain the fix without touching core\nLive: InjectMaxHeight pushes terminal-height-minus-one into the root\nRenderOptions so Layout's column splits distribute correctly, and\nClipHeight slices each panel body to its allotted rows (reserving 2 for\npanel borders). Same code path every frame; variability lives in the\nheight value.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* fix(rich-dash): runtime hardening — fail loudly on bad input, own only the tick loop\n\nFive fixes from PR #3 review, all driven by the same idea — make the\nruntime trustworthy at its trust boundary and stop hiding problems:\n\n- buildLayout throws when children non-empty but split is missing,\n  enforcing the LayoutSpec doc invariant (\"Required iff children\n  non-empty\") instead of silently defaulting to splitColumn.\n- DashboardRuntime validates fps in the constructor (must be a finite\n  number > 0) so an invalid value can never produce NaN/Infinity\n  scheduler intervals.\n- DashboardRuntime checks widget id uniqueness in the constructor;\n  duplicate ids would silently overwrite entries in the canonical\n  _state map ([LAW:one-source-of-truth]).\n- DashboardRuntime stores the setInterval handle on the instance,\n  clears it in stop(), and throws on double-start. Previously stop()\n  could not actually stop the timer because the handle was a closure\n  local.\n- Signal handling and process.exit move from runtime.start() into the\n  demo entrypoint (examples/rich-dash/index.ts) so the runtime stays\n  embeddable in tests and other CLIs ([LAW:locality-or-seam]). The\n  entrypoint stops the runtime in a try/catch and on SIGINT/SIGTERM.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* docs(rich-dash): tighten widget contract docs and dedupe notes import\n\nThe WidgetSpec docstring promised \"pure\" tick and \"no I/O, no mutation\"\nrender, but at least one widget (`job`) mutates state and the embedded\nProgress mutates internal task state during render. Rather than tighten\nall widgets to a contract the runtime never actually enforced, soften\nthe docstring to match what the runtime really requires: same-reference\nreturns are fine, and render must not perform screen/runtime I/O. This\nalso covers the related review thread on widgets/job.ts — its\nrandomized advance is intentional and consistent with the relaxed\ncontract.\n\nCombine the two `node:path` imports in widgets/notes.ts into one.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n---------\n\nCo-authored-by: Claude <noreply@anthropic.com>\nCo-authored-by: Happy <yesreply@happy.engineering>"
        },
        {
          "sha": "d71e3b5b1fd7c049c4d27bf0465af7faf5ee6ede",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-29T02:29:49-06:00",
          "message": "Strip joiners polish + FlexStrip + renderToString + markup plugin tags (#2)\n\n* feat(strip): add GradientJoiner for colour-interpolating transitions\n\nGradientJoiner implements Joiner over the existing Strip protocol,\nemitting `steps` glyph cells whose foregrounds are midpoint-sampled\nRGB blends of `left.style.bgcolor` and `right.style.bgcolor` (via\nthe existing `blendRgb`). Endpoints — and items lacking a bgcolor —\nrender empty: a gradient needs two anchors and the data, not a\ncontrol-flow flag, decides when there's nothing to draw.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* feat(examples): add rich-strip demo showcasing all four joiners\n\nOne-shot, non-interactive demo that prints the same four-cell strip\nthrough PowerlineJoiner, CapsuleJoiner, PlainJoiner, and GradientJoiner\nso the visual differences between joiners are obvious side-by-side.\nWired up as `npm run strip` and listed alongside the existing demos.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* fix(strip): GradientJoiner paints cell background, not foreground\n\nThe original implementation painted the interpolated colour as a\nforeground glyph (default U+258E, a left-three-eighths block) on the\nterminal's default background. Visually that produced a row of thin\nvertical stripes against black, not a gradient — the cells were\nmostly empty space between anchors.\n\nPaint the interpolated colour into the cell *background* instead, with\na space glyph by default so the fade fills the row solidly. Consumers\nwho want a textured gradient can pass a partial-block glyph plus a\nforeground `style`; the option's `style` rides on top of the bg fill.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* fix(strip): PowerlineJoiner emits no leading arrow at the start\n\nThe previous symmetric start/end behaviour rendered a right-pointing\narrow before the first segment with fg=first.bg, no bg — a stranded\nglyph with no source segment to its left to bleed out *from*. Visually\nawkward and not what any classic powerline (vim-airline, tmux-powerline,\nclaude-powerline) does.\n\nDrop the start cap entirely: the strip just begins with the first\nsegment. The end cap remains — that's the visually meaningful one,\nletting the last segment fade out into the terminal background.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* demo(strip): add a full-width unbounded GradientJoiner row\n\nSizes the gradient to fill (consoleOut.width - anchor widths) cells\nbetween two anchor cells, demonstrating that `steps` has no hard cap\nand that the gradient self-limits at the largest RGB channel delta\nbetween the anchors.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* feat(strip): GradientJoiner uses half-block dithering for 2x smoothness\n\nEach gradient cell now emits the half-block glyph (▌, U+258C) carrying\ntwo colour samples — fg for the left half, bg for the right half. So\n`steps` cells produce `2 * steps` colour samples between the anchors,\ndoubling perceived smoothness at the same width with no cost in cells\nconsumed.\n\nRemoves the speculative `glyph` and `style` overlay knobs: with\ndithering both fg and the glyph are load-bearing for the gradient\nitself, so a textured overlay isn't expressible without breaking the\nblend. They were added without a consumer; cut on the way through.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* feat(flex-strip): add FlexStrip wrap-to-width horizontal layout\n\nFlexStrip packs StyledRenderable items greedily onto each line,\nbreaking to a new line when the next item would overflow `maxWidth`.\nIt composes with the existing Strip + Joiner protocol — every line\nis treated as its own sub-strip, so endpoint joins fire at every\nline boundary without any line-break special case in the joiner.\n\nWidth accounting tracks the line's open width separately from its\nprojected closed width (with end-cap added), so the packer always\nasks the right question — \"if I close now, do I fit?\" — instead of\ndiscovering overflow after committing. Items wider than the terminal\nland on their own line as a graceful fallback.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* demo(rich-strip): showcase FlexStrip wrap-to-width packing\n\nAdds two FlexStrip showcases to the existing rich-strip demo: a\nPowerlineJoiner-wrapped tag list and a gap-spaced tag cloud. Both use\nthe same pool of 19 styled cells, so the only difference visible is\nthe layout primitive — line breaks fall in different places and the\njoiner endpoints fire at every line boundary.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* feat(render): add stateless renderToString helper\n\nrenderToString turns any Renderable into an ANSI-encoded string with\nno Console instance, no terminal queries, no I/O side effects. Pure\nfunction: same inputs produce byte-identical output, so it composes\ninto worker threads, caches, tests, and daemon hot paths.\n\nConsole._renderSegment now delegates to the same segmentToString\nencoder, making the new helper and the existing terminal-output path\nshare a single source of truth for \"Segment + ColorSystem -> bytes.\"\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* feat(markup): add plugin tag registry for custom handlers\n\nMarkupRegistry lets consumers register tag handlers without forking\nthe markup parser. When the parser sees [name attrs...]inner[/name]\nand `name` is registered, it parses key=value attrs (including quoted\nvalues), recursively parses the inner markup as a child Renderable,\ncalls the handler, and splices the returned Renderable into the\noutput. Returns a Group when plugin tags fire, or a plain RichText\nwhen no registered tags appear — same shape as the legacy parser.\n\nReserved-name detection delegates to Style.parse: if a name parses as\na built-in style, registering over it throws. There is no second\nlist of \"reserved names\" to keep in sync with the style dialect.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* demo(rich-markup-plugins): showcase MarkupRegistry tag plugins\n\nAdds a one-shot demo that registers two custom tags with a scoped\nMarkupRegistry — `[click href=URL]` (OSC 8 hyperlink) and\n`[badge kind=warning|error|info|ok]` (styled inline pill) — and renders\nmarkup that mixes them with built-in style tags, including nested\nplugin-inside-plugin to demonstrate composition.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* review: tighten renderToString defaults, justify packing, regex removal\n\nAddress PR #2 review feedback:\n\n- renderToString: distinguish explicit `null` (strip color) from explicit\n  `undefined` (default to truecolor). Previous `in` check accepted any\n  present field as authoritative, which leaked `undefined` into\n  Style.render.\n- FlexStrip justify: track item block positions on each line and\n  precompute per-slot fill widths as data, so the emit loop is the same\n  shape every render. The variability lives in `slotFills[]` and\n  `padLeft`, never in branching code paths — fill can no longer land\n  between joiner halves.\n- FlexStrip test: drop dummy `expect(Segment.getShape ? true : true)`\n  scaffolding and the now-unused Segment import.\n- Markup plugin tests: replace magic `1` with `ColorSystem.STANDARD`\n  and drop the `void Style` unused-import shim.\n- Markup plugin parser: replace the two new regexes (PLUGIN_NAME_RE,\n  ATTR_RE) with hand-written character walkers. Real parsing, not\n  regex.\n- Demo: drop `ctx.raw.replace(/\\[[^\\]]*\\]/g, \"\")` regex tag-stripping\n  in favour of `plainTextOf(ctx.children)` — the children Renderable is\n  already the parsed inner content, no re-parsing needed.\n- Re-export the legacy text-only `render` as `parseMarkup` so callers\n  needing a guaranteed `RichText` (with `.stylize`, `.plain`, span\n  edits) have a stable export.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n* review: collapse renderMarkup return type to RichText always\n\nAddress PR #2 review feedback in one architectural move: the previous\nrenderMarkup widened its return to Renderable (RichText | Group |\nsingle child) based on whether plugin tags fired. That violated\none-type-per-behavior and dataflow-not-control-flow — same operation,\nvarying return shape — and forced consumers to type-narrow with\ninstanceof, which silently circumvents the type system.\n\nCollapse the contract:\n\n- MarkupTagHandler returns RichText (was Renderable).\n- renderMarkup returns RichText, always. No Group, no single-child\n  short-circuit, no parallel parseMarkup export.\n- The parser appends every fragment (built-in styled text) and every\n  handler output (RichText) into one accumulator via RichText.append,\n  which carries text + spans through cleanly. Same operation, same\n  shape every render.\n- Demo handlers updated to wrap ctx.children with append + stylize,\n  preserving inner spans beneath the layered link/badge styling\n  (nested [click][badge] now renders with both effects visible).\n- Drop Group and Renderable imports from markup.ts; drop them from\n  the test where outer-tag composition was previously a Group.\n\nAlso fold in the other review items addressed in the prior commit:\n- renderToString: explicit `null` strips color, `undefined` defaults\n  to truecolor (was: `in` check accepted any present field).\n- FlexStrip justify: precompute per-slot fill widths as data so the\n  emit loop has no per-mode branch and fill cannot land between\n  joiner halves.\n- Drop dummy expect() and unused Segment import in flexStrip test.\n- Use ColorSystem.STANDARD instead of magic 1; drop unused Style\n  import in markup-plugins test.\n- Replace the new PLUGIN_NAME_RE / ATTR_RE regexes with hand-written\n  character walkers; replace `ctx.raw.replace(/\\[…\\]/g, \"\")` in the\n  demo with `ctx.children` (already parsed inner content).\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n\n---------\n\nCo-authored-by: Claude <noreply@anthropic.com>\nCo-authored-by: Happy <yesreply@happy.engineering>"
        },
        {
          "sha": "894b89d49e711232201174604a864a8dd16e31bf",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-28T11:33:10-06:00",
          "message": "feat(strip): add Strip + Joiner edge-aware horizontal layout primitive\n\nA `Strip<T extends StyledRenderable>` lays out a horizontal sequence of\nstyled items where the transition between adjacent items is itself a\nstylable renderable whose style is a function of its left and right\nneighbours. The walk\n  joiner(null, items[0]), items[0],\n  joiner(items[0], items[1]), items[1], ...,\n  joiner(items[N-1], null)\nmakes endpoint joins explicit positions in the protocol — joiners name\nwhat an endpoint looks like rather than the strip guessing.\n\nThree reference joiners ship with the core:\n\n- `PowerlineJoiner`: classic arrow glyphs. fg = left.bg, bg = right.bg.\n  Endpoints render fg-only so the strip blends into terminal default.\n- `CapsuleJoiner`: rounded pills. Middle emits close-cap + separator +\n  open-cap; endpoints emit a single cap inheriting the neighbour's bg.\n- `PlainJoiner`: fixed styled separator between items, empty endpoints.\n\n`StripCell` is a minimal `StyledRenderable` for the common single-style\ntext case; consumers with richer items can implement `StyledRenderable`\n(a `Renderable` plus `style: Style`) directly.\n\nCloses rich-strip-joiner-duc.1.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n"
        },
        {
          "sha": "56f0faf95b01606a4ac9635d1d1648591d849835",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-28T10:29:24-06:00",
          "message": "fix(live,text,console,demo): tighten output target and error handling from PR #1 review\n\n- Live now writes through `Console.file` (new public getter) instead of\n  hard-coding `process.stdout`, so a custom `file`/stderr Console honors\n  alt-screen control sequences and ANSI output.\n- Live skips `Style.render` when `colorSystem === null` to match\n  `Console._renderSegment`'s NO_COLOR / dumb-terminal policy.\n- `RichText.resolveStyle` narrows its catch to `StyleSyntaxError`; other\n  errors propagate so genuine bugs aren't swallowed.\n- rich-colors: hold the status-message timeout handle and clear it on each\n  new status so timers don't stack (the prior `\"timeout\" in state` guard\n  was always false).\n- rich-colors status-indicator docstring rewritten — it renders a static\n  emoji, never a Spinner.\n- README: Tree fix description corrected; \"not yet exercised\" table pruned\n  to reflect coverage from rich-explore / claude-sessions / rich-colors.\n\nGenerated with [Claude Code](https://claude.ai/code)\nvia [Happy](https://happy.engineering)\n\nCo-Authored-By: Claude <noreply@anthropic.com>\nCo-Authored-By: Happy <yesreply@happy.engineering>\n"
        },
        {
          "sha": "e9e77b9068eca38699b5e374cfc8a638079e34ea",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-18T01:32:20-06:00",
          "message": "docs(readme): document the three demos and the modules they exercise\n\nAdds a Demos section covering rich-explore, claude-sessions, and\nrich-colors with run commands, key bindings, and per-demo tables of\nwhich library modules each exercises (incremental coverage between\ndemos). Includes a coverage summary listing core/renderable modules\nexercised across all three plus the bug fixes those demos surfaced\nin Live, Progress, Tree, and Spinner.\n"
        },
        {
          "sha": "b2ab18456536901f27afdb6a8c1142cad3283a15",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-18T01:32:14-06:00",
          "message": "refactor(rich-colors): expand interactive palette generator\n\nSubstantial rework of the rich-colors demo: state machine grows to\ncover more interaction modes, view shell rebuilt for the expanded\ncontrols, app and keymap track the new interactions, status indicator\nthreads through. Builds on the earlier compact-layout work without\ngiving up the 24-line terminal target.\n"
        },
        {
          "sha": "f2cef9466590220f75268307fc2c8035b3cd9d57",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-18T01:32:09-06:00",
          "message": "feat(claude-sessions): add global cross-file search and viewer polish\n\nNew global-search engine indexes every JSONL session under a project\nand returns ranked matches across files. New global-results view\nrenders matches with project/session/turn context. Search-bar gains\na global mode (S binding); state.ts grows the search workflow,\nresult navigation (n/N), and result-to-session jump.\n\nBlock renderers and the viewer get smaller polish: better hidden-\nblock reveal, improved tool-call summaries, and tightened error/\nsubagent rendering.\n"
        },
        {
          "sha": "bf0b8c247f236851793698d263116d2b358f4739",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-18T01:31:59-06:00",
          "message": "feat(rich-explore): add coverage renderer for module exercise table\n\nNew views/renderers/coverage.ts renders a table of which library\nmodules are exercised by the demo and how. Wired into the preview\npane as a dedicated view alongside the existing tree/markdown/syntax\npreviews. Small adjustments to app/state/keymap to surface the new\nview.\n"
        },
        {
          "sha": "1a799aa863ab235e246914efb03f06ea8c649d33",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-18T01:31:53-06:00",
          "message": "feat(live): add alt-screen mode and preserve ANSI styles on refresh\n\nTwo changes driven by full-screen demo work:\n\n- altScreen option: when set, start() writes the alt-screen-enter\n  sequence (\\x1b[?1049h), refresh() cursor-homes (with full clear on\n  first refresh) instead of clearing N lines, and stop() restores the\n  original buffer. Eliminates flicker for full-screen TUI layouts.\n\n- refresh() now applies each segment's style via style.render(text,\n  colorSystem) before joining lines. Previously bare s.text was\n  written, stripping all ANSI styles from anything flowing through\n  Live (Status, Progress, Spinner, etc).\n"
        },
        {
          "sha": "4340c9b4a0f64d799883e8f1ad949caa942213a3",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-18T01:31:45-06:00",
          "message": "fix(spinner): default name to DEFAULT_SPINNER when undefined\n\nSpinnerColumn (used by Progress) passed an optional string|undefined\ninto the required name parameter, which TypeScript rejected and which\nwould have thrown the unknown-spinner error at runtime. Make name\noptional and fall back to DEFAULT_SPINNER.\n"
        },
        {
          "sha": "d118b93f24d2a45334c12a1a9cf8c1fb1d321647",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-18T01:31:40-06:00",
          "message": "fix(progress): preserve column styles when building cells\n\nCells were built with segs.map(s => s.text).join(\"\"), discarding\nevery segment's style. Progress percentage, timing, and spinner\ncolumns lost their styling on render.\n\nNow appends each segment with its style via RichText.append, so\ncolumn-level styles flow through to the final output.\n"
        },
        {
          "sha": "242fa9db8f37b05e93912ec2d239fc9875250e53",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-18T01:31:35-06:00",
          "message": "fix(tree): document explicit row-boundary ownership\n\nPairs with the RichText newline change: Tree owns its row boundaries\nby yielding Segment.line() after each label, rather than depending on\nthe label renderable to emit a trailing newline. Comment annotates\nthe invariant for future readers.\n"
        },
        {
          "sha": "87a1be0e8ad230d82fcd8a4414d6de96bb517ace",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-18T01:31:22-06:00",
          "message": "feat(highlighter): support literal baseStyle alongside namespace mode\n\nbaseStyle was always treated as a namespace prefix joined with the\nnamed-group name (\"repr.\" + \"url\" → \"repr.url\"). Now if baseStyle\ndoes not end in \".\" it is used as the literal style applied to every\nmatch (e.g. \"bold red\" applies that style to all named groups).\n\nLets simple highlighters skip defining a theme namespace and just\ndeclare the style they want inline.\n"
        },
        {
          "sha": "f0fd58f8fa5132b9f283328eff59659f08e96f56",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-18T01:31:13-06:00",
          "message": "fix(text): tighten newline emission and tolerate unknown style strings\n\nTwo robustness fixes surfaced by demo integration:\n\n- resolveStyle wraps Style.parse in try/catch and degrades to\n  NULL_STYLE on StyleSyntaxError, so a bad style name from a\n  highlighter or theme lookup produces unstyled output instead of\n  crashing the render pipeline. Single-enforcer at the trust boundary\n  where external style strings enter the system.\n\n- RichText.render only emits a trailing Segment.line() when the input\n  text ended in a newline (or the line is not the last). Previously\n  every render appended an extra newline, which compounded into double\n  blank lines for renderables like Tree that own their own row\n  boundaries.\n"
        },
        {
          "sha": "910ed7c892bc14e3b1bf3998bd4955e56dec23c3",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-18T01:31:06-06:00",
          "message": "chore: add tsconfig.demo.json and ignore dist-demo output\n\nDemo build (npm run demo/sessions/colors) compiles src+examples\ntogether to dist-demo/ via a separate tsconfig. Adds the config and\ngitignores the build output.\n"
        },
        {
          "sha": "cab4dd89dc096b960eb8497560d33833ecf1286b",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-08T04:51:49-06:00",
          "message": "refactor: Optimize rich-colors for compact screen layout\n\nReduce vertical space usage to fit on standard 24-line terminals:\n\nLayout Changes:\n- Compact 1-row header with title + mode indicator (was 2 rows)\n- Removed dedicated status row (save 1 row)\n- Consolidate footer with compact keybindings (Tab→mod, C→sys, etc)\n- Simplified input panel from 5 rows to 2 rows max\n- More efficient space usage: header(1) + content(flex) + footer(1)\n\nVisual Improvements:\n- Header: 💫 rich-colors [palette-mode]\n- Footer: keybindings | status (e.g., 🎨 256-color)\n- Input mode: ⏳ Awaiting input notification\n- Maintains Spinner and Status via compact indicators\n\nResult: Demo now works on 24-line terminals with plenty of space\nfor content (20+ lines available). Full alt-screen mode support.\n"
        },
        {
          "sha": "88ea23118c09d7e7e39556a982d7656debbfdf8d",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-08T04:50:45-06:00",
          "message": "feat: Integrate Spinner and Status into rich-colors demo\n\nAdd visual status indicators and spinner animations:\n- Status row showing current palette mode with emoji indicators\n- Spinner simulation in input mode (awaiting color)\n- Status bar enhanced with mode/system info display\n- New status-indicator.ts module for status display logic\n- Demonstrates Spinner, Status from rich-js API\n\nStatus Display Features:\n- Shows 🎨 icon with current palette mode name\n- Displays color system (truecolor/256/16/windows)\n- In input mode: shows ⏳ indicator for awaiting input\n- Interactive mode: shows palette mode and system info\n\nBrings demo coverage to Status and Spinner components,\ncompleting integration of all major rich-js features.\n"
        },
        {
          "sha": "ab5d22337e9bc4d1b72527f9ab51bd3b604fcafa",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-08T04:42:34-06:00",
          "message": "feat: Add rich-colors demo — interactive palette generator\n\nImplement a visually striking interactive color palette generator that exercises all color-related functionality in rich-js plus extensive library features:\n\nDemo Features:\n- Input color parsing (hex, rgb, named colors, indexed)\n- Palette generation modes: complementary, analogous, triadic, tetradic, square, monochromatic, shades, tints\n- Color system downgrading visualization (truecolor → 256 → 16 → Windows)\n- Terminal theme support (DEFAULT, MONOKAI, SVG_EXPORT)\n- Interactive keybindings for palette mode cycling, color system selection, theme switching\n- Real-time palette updates and color detail display\n- Immutable reducer-based state management (follows rich-explore pattern)\n\nRich-js Color APIs Exercised:\n- Color.parse() with all input formats\n- Color.fromRgb(), Color.getTruecolor()\n- Color.downgrade() for cross-system rendering\n- ColorTriplet, Palette, TerminalTheme (all themes)\n- ANSI_COLOR_NAMES, color palettes\n- blendRgb() for tint/shade generation\n\nOther Rich Functionality:\n- Layout with splitRow/splitColumn, ratios, responsive design\n- Panel with titles and padding\n- RichText styling with Style\n- Rule for dividers\n- State management with immutable reducers\n- Terminal I/O with raw mode\n\nImplementation:\n- examples/rich-colors/ - complete demo with state, views, color math\n- Color space conversion (RGB ↔ HSL) for palette generation\n- Pure functional architecture following CLAUDE.md laws\n- npm run colors - launch the demo\n\n[LAW:dataflow-not-control-flow] State mutations centralized in reducers\n[LAW:one-source-of-truth] AppState is the single authority\n[LAW:single-enforcer] All palette generation delegated to generatePalette()\n"
        },
        {
          "sha": "cfc629c1bdc3c7701e027ec9e249a46ee8dcd7ec",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-04-07T18:15:22-06:00",
          "message": "Update social link\n"
        },
        {
          "sha": "e8b93949018f35bb8e2690d85a9bd75037b93245",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-03-30T01:59:31-06:00",
          "message": "feat(the whole shebang)\n"
        },
        {
          "sha": "d140427dfc4a2f21cf76c2c79dcd45e96cbd66a5",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-03-17T20:30:21-06:00",
          "message": "Fix CI: trigger on master not main\n"
        },
        {
          "sha": "f32ccba715bc0a1c108b344ce90f59e99c1e1f10",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-03-17T20:27:17-06:00",
          "message": "Add VitePress documentation site with GitHub Pages deployment\n\n- 22 doc pages covering all spec topics (console, styles, markup, text,\n  highlighting, tables, panel, tree, columns, group, padding, progress,\n  live display, layout, syntax, markdown, tracebacks, logging, prompts,\n  pretty printing, renderable protocol)\n- VitePress 1.6 with rich-purple brand theme and terminal-styled code blocks\n- GitHub Actions workflow deploying to GitHub Pages on push to main\n- docs:dev / docs:build / docs:preview scripts in package.json\n"
        }
      ]
    }
  ]
}
