{
  "schema": 2,
  "versions": [
    {
      "version": "pr-124",
      "ref": "refs/pull/124/merge",
      "sha": "1e0035b12dbf38810b56a1c9886ab82c275c775f",
      "timestamp": "2026-09-11T12:50:04.492Z",
      "commits": [
        {
          "sha": "1e0035b12dbf38810b56a1c9886ab82c275c775f",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-09-11T12:49:35Z",
          "message": "Merge 5316ac79de4ea47b9d7301a4a4e666bd3b94103b into 883b8e57fc2199b44d88c601460f268a184c49a5\n"
        },
        {
          "sha": "5316ac79de4ea47b9d7301a4a4e666bd3b94103b",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-09-11T06:49:22-06:00",
          "message": "docs(packaging): stop claiming a release step verifies the Node floor\n\nBoth the `ci.yml` comment and its CLAUDE.md copy said the floor \"is verified by\npacking a tarball and installing it under the lowest admitted Node, which is a\nrelease step\". There is no such step. `publish.yml` pins Node 22, runs\nlint/test/build, and publishes; nothing in this repository packs a tarball and\ninstalls it under the floor.\n\nThe check was run by hand, once, on 48a80043, and is recorded in PR #124.\nAutomating it is rich-packaging-8b7, which carries the proven procedure.\n`test/seam/node-floor.ts`'s header had it right all along — \"done by hand on the\ncommit that set this floor and is recorded in its PR\" — so this is two copies of\none fact being brought back to the answer the third already gave.\n\nA comment promising a safety net is worse than the gap it papers over, because\nit stops the next reader from going to look for one. The gap is real and now\nstated where each file has room for it: the gate compares declarations and never\nexecutes `src/`, so the day `src/` reaches for a Node 22 API every check stays\ngreen while the package breaks on 20 — and CI cannot catch it, because\nsetup-node resolves a range to its newest satisfying version and has never once\nrun the declared floor.\n\nCLAUDE.md's sentence naming which documents the prose half reads gains\n`CLAUDE.md` alongside the other two, following f5dd8d9a putting it on the list.\n"
        },
        {
          "sha": "f5dd8d9afd6b3fb917db18777b46281da9b51fc6",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-09-11T06:47:48-06:00",
          "message": "fix(seam): stop excluding required peers, and narrow what a red run blames\n\nFour findings from the PR #124 review, all in the node-floor gate.\n\n`peer` is no longer an exclusion flag. npm 7 and later install a required peer\ndependency as part of a default install, so its `engines` binds the floor like\nany other package's; dropping it understates the floor and raises no violation\nwhile doing so — the same leak as admitting a devDependency's range, pointed the\nother way, and the direction that fails silently. The review proposed narrowing\nthe check to `peer && optional`; removing the flag is the smaller fix and the\nmore honest one, because npm does not install an optional peer at all, so such a\npackage has no lockfile entry unless something else pulled it in, and that\nsomething else carries its own flag. A required peer now lands in\n`admittedByFlags` and not in `reachableFromRoot`, which is a `disagreement` — a\nstop-and-decide, where before it was a silent omission. The message for that arm\nnames required peers as the first thing to check.\n\nNothing changes for today's tree, and the review's supporting detail was wrong:\n`mobx` and `@promptctl/go-template-js` are recorded as plain `dev: true`, and the\nsole `peer: true` entry is `search-insights`, which is also `dev: true` and\ndeclares no `engines`.\n\n`treeFloor` now reports only the ranges that actually conflict when no range is\nstrictest, rather than the whole tree. With two packages those are the same set,\nwhich is why the existing fixture could not see the difference; a third package\ndeclaring `*` contains everything, constrains nothing, and had no business in a\nmessage telling someone which manifests to go read.\n\n`packageNameFromKey` replaces the key-slicing that was written out twice.\n[LAW:single-enforcer] — two copies of a parse are two answers to \"what is this\npackage called\" the day either one meets a lockfile shape the other has not.\n\n`lockfileVersion` was declared on the test's `Lockfile` type and never read;\ndeleted rather than explained.\n\nCLAUDE.md joins `DOCUMENTED_IN`. It was left off on the premise that it \"points\nat the field rather than naming a version\" — it does point at the field, and\nnames the version in the same breath (\"targeting Node.js >= 20; `package.json#engines`\nis the authority\"). Three copies were checked and the fourth was excused on a\npremise anyone could have read and disproved, which is how this fact came to have\nthree different answers to begin with.\n"
        },
        {
          "sha": "d4324ecd16ed8b19638fbafd95cefa29175cdfc0",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-09-11T06:35:21-06:00",
          "message": "fix(seam): derive the production install set twice, not from npm's flags alone\n\nThe PR #124 review found `devOptional` missing from the node-floor gate. npm\nsets that flag *instead of* `dev` and `optional` together, for a package\nreachable only as an optional dependency of a development one, so an entry\ncarrying it alone passed a filter that looked for the other three and found\nnone. That is a devDependency's Node requirement reaching the published floor —\ncharacter for character the bug this rule exists to prevent, arriving through\nthe rule itself.\n\nThe finding's concrete scenario was wrong, and checking it is what changed the\nfix. `fsevents` in this lockfile carries `dev` and `optional` together, both\nalready excluded, and `devOptional` appears zero times across all 257 entries.\nThe gap is real regardless — the flag was absent from the `LockEntry` type, so\nit would have been invisible had it ever appeared — but the comment claiming\n\"the three ways npm records on-disk-but-not-for-them\" was a completeness claim\nnobody can make about a schema npm owns and changes without asking.\n\nSo the fix is not a fourth flag. `productionKeys` now derives the install set\ntwice — `admittedByFlags` from npm's flags, `reachableFromRoot` by walking\nrequired dependency edges from the root, nested copies resolved before hoisted\nones and optional edges not followed — and returns a `disagreement` variant\nrather than picking a winner. A flag npm invents next year fools the first\nderivation and not the second, and the gate stops instead of leaking the entry\ninto the floor.\n\nFixtures pin each half separately as well as the pair, against lockfiles this\nrepository does not have. It cannot have the one that matters: `devOptional` is\nnever in our tree, so every assertion about the flag that caused the finding\nlives in a fixture or nowhere. Each was checked by mutation — dropping the flag,\nhoisting the nested resolution, following optional edges, and picking a winner\neach turn the relevant fixture red.\n"
        },
        {
          "sha": "21d1b97710d5dd868d7ae8136161cf9d601195da",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-09-11T06:29:37-06:00",
          "message": "test(seam): pin the prose half of the node-floor rule from both sides\n\n`claimViolations` was exercised only by the live README.md and\ndocs/introduction.md, which always contain a claim — so `noClaim`, the arm that\nfires when the sentence has been reworded past the pattern and the gate is\nwatching nothing, had never run. That is the arm whose whole job is to turn\nsilence into a failure, and it shipped untested.\n\nAdds fixtures for both arms plus the two live sentence shapes, and for\n`nodeRange`'s empty-string case, which is separate from a malformed range and\nwas likewise only asserted by absence.\n"
        },
        {
          "sha": "48a800436df9b6ab0bbeb58ac67ce9bddedae060",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-09-11T06:26:34-06:00",
          "message": "fix(packaging): declare the Node floor the install tree actually requires (rich-packaging-k9t)\n\n`package.json#engines.node` read `^20.19.0 || >=22.12.0` — character for\ncharacter the range `vite` declares. `vite` is a devDependency, so no consumer\nreceives it, and the published package was refusing Node 20.0-20.18, all of\n21.x, and 22.0-22.11 on the authority of a build tool those consumers never\ninstall. It now reads `>=20`, which is what `string-width` declares — the only\nhard runtime dependency, and the only package in the default install tree\nasking for more than Node 18.\n\nThe floor is now derived rather than asserted. `test/seam/node-floor.ts` reads\n`package-lock.json`, npm's own record of which packages a default install puts\non disk and what each declares, finds the strictest range among them, and\nrequires `engines.node` to denote exactly that set. It has no allow-list and no\n`why` field, unlike the seam rules around it, because there is no human claim\nleft for one to justify.\n\nThe comparison is `semver.subset`, not a check on minimum versions, and that\ndecides a real case: `subset(\">=20.19.0\", \"^20.19.0 || >=22.12.0\")` is false,\nbecause 21.x satisfies the first and not the second. Comparing minimums calls\nthose equivalent and passes — on exactly the disjoint range shape that caused\nthis bug. `semver` and `@types/semver` join devDependencies for it; the\nproduction tree is unchanged and `string-width` remains the only hard\ndependency.\n\nFour copies of this one fact existed and gave three answers: the field as\nabove, README.md 20, docs/introduction.md 18, and CLAUDE.md 20 while correctly\nnaming the field as the authority. `docs/introduction.md` is corrected, and the\nrule now reads both documents back and checks the version they name against the\nfield. A fifth copy turned up in `.github/workflows/ci.yml`, which restated the\nold range in a comment and credited it to a transitive dependency — both halves\nfalse by the time anyone read them, and both removed.\n\nThat workflow takes its Node from this field via `node-version-file`, so the\nchange was checked against a real run rather than reasoned about: setup-node\nresolves a range to its newest satisfying version, logging `Resolved\npackage.json as ^20.19.0 || >=22.12.0` and then taking 24.20.0. Widening the\nfloor does not lower CI's Node, and CI never exercised the floor in the first\nplace.\n\nVerified by packing the tarball and installing it into an empty project under\nNode 20.18.0 with `engine-strict=true`: the old range fails EBADENGINE, the new\none installs and renders a Table and a Panel with the expected text. CI cannot\nstand in for that check, which is filed as rich-packaging-8b7.\n\nlint clean on all three passes; 2454 tests across 84 files; docs:build clean.\n"
        }
      ]
    },
    {
      "version": "master",
      "ref": "refs/heads/master",
      "sha": "883b8e57fc2199b44d88c601460f268a184c49a5",
      "timestamp": "2026-09-11T12:03:16.571Z",
      "commits": [
        {
          "sha": "883b8e57fc2199b44d88c601460f268a184c49a5",
          "author_name": "Brandon Fryslie",
          "author_email": "530235+brandon-fryslie@users.noreply.github.com",
          "timestamp": "2026-09-11T06:02:41-06:00",
          "message": "perf(packaging)!: make the template engine an optional peer (rich-packaging-1xv.dpo) (#123)\n\n@promptctl/go-template-js was a hard runtime dependency, so every consumer\ninstalled it and its transitive @noble/hashes whether or not they ever\nauthored a template. The template-bindings subpath already kept it out of\nthe default bundle; this takes it out of the default install. Same move\nrich-packaging-1xv.5 made for mobx, and the last child of that epic.\n\nIt is now a peer dependency marked optional in peerDependenciesMeta, the\npair that makes npm neither fetch the package nor warn about its absence.\noptionalDependencies would not have: npm installs those. A bare peer would\nnot have either, since npm 7+ auto-installs peers. It stays in\ndevDependencies so this checkout's tests and demos still run.\n\nMeasured by packing a tarball into an empty project rather than by reading\npackage.json: the installed tree drops from 8 entries to 6, the two that\nleave being @promptctl/go-template-js and its transitive @noble/hashes, with\nno peer warning in either state. The barrel renders a Table with both peers\nabsent; @promptctl/rich-js/template-bindings fails at import with\nERR_MODULE_NOT_FOUND until the engine is installed, and renders bold-green\noutput through createRichTextEngine once it is. All 10 demo bundles still\nboot in Chromium under playwright, and the 130 template-bindings tests that\nimport the engine directly still pass.\n\nPEER_PROVIDERS gains one row naming @promptctl/rich-js/template-bindings.\nThe gate was confirmed red before being trusted, both ways it should go:\nwith the row missing it named src/template-bindings/index.ts:37, and with an\nimport of the engine staged into src/core/measure.ts it named the file, the\nline, and the chain src/index.ts -> src/core/measure.ts. Note it reported\none reach, not five — the other four imports of the engine are `import\ntype` and erase, which is the runtime-graph distinction working.\n\nThe \"what each published subpath costs to install\" block stated mobx's\ncontract concretely and would have under-described the new one. Its two\ncases are now one it.each over a literal table, plus a pin asserting that\ntable covers every optional peer package.json declares, so a third peer has\nto arrive with its contract written out instead of silently undescribed.\nThe values stay hand-written on purpose: deriving them from PEER_PROVIDERS\nwould restate the gate above rather than state the contract it protects.\n\nThree prose claims this falsifies, none of which any test reads — nothing in\nthe suite globs README.md, and symbol-existence.test.ts checks symbols, not\ndependency claims:\n\n  - docs/template-bindings.md: \"The engine is a dependency of rich-js, so\n    there is nothing extra to install.\"\n  - README.md: \"a plain dependency and arrives with the package, so that\n    subpath needs nothing extra.\"\n  - README.md: \"The widget layer's MobX is the one dependency you install\n    yourself\" — now false because there are two. The ticket had not spotted\n    this one.\n\nThe two subpaths are symmetric as of this change, which lifts the reason\nthe Entry points table deliberately had no \"what you must install\" column:\nthe asymmetry a row shape could have flattened is gone, so the table gains\nthe column and answers the question per row.\n\nCLAUDE.md's optional-peer section named mobx alone, which would have told an\nagent the engine was a plain dependency it could import from src/core/ —\nexactly the hazard that gate exists to catch, and invisible here because the\npackage is still in devDependencies. It now covers both peers with one rule\nrather than two copies of it. Its \"Node >= 20 (transitively required by\n@promptctl/go-template-js)\" attribution is also dropped: the engine no\nlonger binds a default install at all. What the real floor should be is a\nseparate question — package.json#engines is character-for-character vite's,\na devDependency no consumer installs, while string-width, now the only hard\ndependency, asks only for >=20 — and is filed rather than resolved here.\n\nBREAKING CHANGE: consumers of @promptctl/rich-js/template-bindings must now\ninstall @promptctl/go-template-js themselves — `npm install\n@promptctl/rich-js @promptctl/go-template-js`."
        }
      ]
    }
  ]
}
